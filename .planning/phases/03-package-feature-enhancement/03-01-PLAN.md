---
phase: 03-package-feature-enhancement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/filament-admin/src/Services/StubGenerator.php
  - packages/filament-admin/src/Commands/PublishCommand.php
autonomous: true
requirements: [FEAT-03]
must_haves:
  truths:
    - "PublishCommand 重构后调用 StubGenerator，公开接口（--model/--resource/--all/--only/--except/--path/--force）行为与文案完全不变"
    - "StubGenerator 提供 renderStub/writeFile/validatePath/pluralize/toSnakeCase/deriveModelNamespace/deriveResourceNamespace/derivePanelPrefix 共享方法"
    - "Phase 1 的 PublishCommandTest 10 个测试在重构后继续全绿（回归护栏）"
  artifacts:
    - path: "packages/filament-admin/src/Services/StubGenerator.php"
      provides: "共享 stub 渲染/命名空间推导/路径校验服务（D-28）"
      contains: "class StubGenerator"
    - path: "packages/filament-admin/src/Commands/PublishCommand.php"
      provides: "重构为委托 StubGenerator 的批量发布命令，公开契约不变"
      contains: "StubGenerator"
  key_links:
    - from: "packages/filament-admin/src/Commands/PublishCommand.php"
      to: "packages/filament-admin/src/Services/StubGenerator.php"
      via: "构造注入 protected StubGenerator $generator + 委托调用"
      pattern: "StubGenerator"
---

<objective>
从 Phase 1 已交付的 `PublishCommand.php`（558 行）中抽取共享 `StubGenerator` 服务类（D-28），并把 `PublishCommand` 重构为构造注入 `StubGenerator` 后委托调用。这是 FEAT-03 CRUD 生成器的底层基石——四个 `make:*` 命令（在 03-02 实现）将与重构后的 `PublishCommand` 共用同一套渲染逻辑，实现零重复。

Purpose: 消除 stub 渲染逻辑重复，建立单一修改点；为 03-02 的四命令提供可注入的服务依赖。
Output: `StubGenerator.php`（新建）+ 重构后的 `PublishCommand.php`（公开契约不变）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-package-feature-enhancement/03-CONTEXT.md
@.planning/phases/03-package-feature-enhancement/03-RESEARCH.md
@.planning/phases/03-package-feature-enhancement/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 抽取 StubGenerator 服务类（移动不改变）</name>
  <files>packages/filament-admin/src/Services/StubGenerator.php</files>
  <read_first>
    - packages/filament-admin/src/Commands/PublishCommand.php（重构基线，方法行号：renderStub=312、writeFile=340、validatePath=360、deriveModelNamespace=384、deriveResourceNamespace=404、derivePanelPrefix=424、pluralize=445、toSnakeCase=455）
    - packages/filament-admin/src/Services/ActivityLogger.php（同目录服务类的命名空间/PHPDoc 风格参照）
    - .planning/phases/03-package-feature-enhancement/03-PATTERNS.md §`src/Services/StubGenerator.php`（方法签名映射表 + 参数化差异说明）
    - .planning/phases/01-package-release-compliance/01-CONTEXT.md §D-11（stub fallback 逻辑契约）
  </read_first>
  <behavior>
    - renderStub('Model', ['namespace'=>'App\\Models','class'=>'Product','table'=>'products']) 返回替换 {{ namespace }}/{{ class }}/{{ table }} 后的字符串
    - renderStub 对不存在的 stub 名返回空字符串 ''（保持 PublishCommand 原行为）
    - renderStub 优先读 base_path('stubs/vendor/filament-admin/{name}.stub')，不存在时 fallback 到包内 stubs/（D-11）
    - writeFile(path, content, force=false)：文件已存在且 force=false 时返回 false 且不写入；force=true 或文件不存在时写入并返回 true（不再调用 $this->warn，IO 由调用方负责）
    - validatePath('../etc') 返回 false；validatePath('/abs') 返回 false；validatePath('C:\\x') 返回 false；validatePath('app') 与 validatePath('app/Filament/Reseller') 返回 true；非 app 前缀返回 false
    - pluralize('Product') 返回 'Products'；toSnakeCase('AdminUser') 返回 'admin_user'
    - deriveModelNamespace(null) 返回 'App\\Models'；deriveModelNamespace('Reseller') 返回 'App\\Models\\Reseller'
  </behavior>
  <action>
    新建 `FilamentAdmin\Services\StubGenerator` 类，命名空间 `FilamentAdmin\Services`，导入 `Illuminate\Support\Facades\File`。将 PublishCommand 的八个 protected 方法体逐一移入并改为 public，遵循"移动不改变"原则——签名等价，仅去除对 `$this->option()` / `$this->warn()` / `$this->error()` 的耦合，把这些值改为方法参数：
    - `renderStub(string $stubName, array $vars): string`（逻辑与 PublishCommand 第 312-332 行等价，含 D-11 用户 stub fallback）
    - `writeFile(string $targetPath, string $content, bool $force = false): bool`（第 340-353 行；`$force` 从 `$this->option('force')` 改为传入参数；命中跳过时直接 return false，不输出文案）
    - `validatePath(string $path): bool`（第 360-376 行；去掉 `$this->error()`，纯返回 bool；保留 `..` / 绝对路径 / `C:\\` / 非 app 前缀的拒绝逻辑——V5 输入验证 + Tampering 防护）
    - `pluralize(string $name): string`（第 445-448 行，追加 s）
    - `toSnakeCase(string $name): string`（第 455-458 行，PascalCase→snake_case）
    - `deriveModelNamespace(?string $panelPrefix = null): string`（第 384-393 行；把内部 `$this->option('with-models')`/`derivePanelPrefix()` 读取改为接收 `$panelPrefix` 参数）
    - `deriveResourceNamespace(string $name, string $path = 'app/Filament/Resources'): string`（第 404-416 行；把内部 `$this->option('path')` 改为 `$path` 参数）
    - `derivePanelPrefix(string $path = ''): string`（第 424-440 行；把内部 `$this->option('path')` 改为 `$path` 参数）
    所有 public 方法必须有中文 PHPDoc（含 `@param array<string,string>` 等标量/数组类型标注，满足 phpstan level 6）。类头 PHPDoc 注明"从 PublishCommand 抽取（D-28），供 PublishCommand 与四个 make:* 命令共用"。
  </action>
  <verify>
    <automated>cd packages/filament-admin && vendor/bin/phpstan analyse src/Services/StubGenerator.php --level=6 && vendor/bin/pint --test src/Services/StubGenerator.php</automated>
  </verify>
  <acceptance_criteria>
    - `packages/filament-admin/src/Services/StubGenerator.php` 存在且声明 `namespace FilamentAdmin\Services;` 与 `class StubGenerator`
    - 八个 public 方法签名存在：`grep -c 'public function \(renderStub\|writeFile\|validatePath\|pluralize\|toSnakeCase\|deriveModelNamespace\|deriveResourceNamespace\|derivePanelPrefix\)' src/Services/StubGenerator.php` 返回 8
    - 类内无 `$this->option(` / `$this->warn(` / `$this->error(` 调用：`grep -c '$this->\(option\|warn\|error\)(' src/Services/StubGenerator.php` 返回 0
    - phpstan level 6 退出码 0，pint --test 退出码 0
    - 每个 public 方法上方有 `/**` 中文 PHPDoc 块
  </acceptance_criteria>
  <done>StubGenerator 含八个参数化 public 方法，phpstan/pint 双绿，无命令 IO 耦合</done>
</task>

<task type="auto">
  <name>Task 2: 重构 PublishCommand 委托 StubGenerator（保持公开契约）</name>
  <files>packages/filament-admin/src/Commands/PublishCommand.php</files>
  <read_first>
    - packages/filament-admin/src/Commands/PublishCommand.php（待重构全文：handle 第 56-116 行、publishModel 第 185、publishResource 第 211、publishMigration 第 258、publishFeatureTest 第 281、被移走的八方法 312-458；$signature 第 29-37、BUILTIN_NAMES 第 51）
    - packages/filament-admin/src/Services/StubGenerator.php（Task 1 产物，委托目标）
    - packages/filament-admin/tests/Unit/PublishCommandTest.php（10 个回归测试，断言文案 "已生成:" / "Skipped:" / namespace 字符串——第 141/187/192/215/234 行）
    - .planning/phases/03-package-feature-enhancement/03-PATTERNS.md §`src/Commands/PublishCommand.php`（委托模式 publishModel 重构示例 + 不变项清单）
  </read_first>
  <action>
    给 PublishCommand 添加构造函数 `public function __construct(protected StubGenerator $generator) { parent::__construct(); }` 并 `use FilamentAdmin\Services\StubGenerator;`。删除已移入 StubGenerator 的八个 protected 方法体（renderStub/writeFile/validatePath/pluralize/toSnakeCase/deriveModelNamespace/deriveResourceNamespace/derivePanelPrefix），将 publishModel/publishResource/publishMigration/publishFeatureTest/handle 中对这些方法的 `$this->xxx()` 调用改为 `$this->generator->xxx(...)`，并把原先方法内部从 `$this->option()` 读取的值（`path`、`with-models`、`force`）在调用点显式传入：
    - `deriveModelNamespace($this->option('with-models') ? $this->generator->derivePanelPrefix((string)($this->option('path') ?? '')) : null)`
    - `deriveResourceNamespace($name, (string)($this->option('path') ?: 'app/Filament/Resources'))`
    - `writeFile($targetPath, $content, (bool)$this->option('force'))`
    - validatePath 校验失败时由 PublishCommand 自己输出 `$this->error()` 并返回 FAILURE（IO 留在命令层）
    严格不变项（回归护栏，D-28 / Pitfall 5）：`$signature` 字符串（第 29-37 行）逐字不变；`BUILTIN_NAMES` 常量不变；handle() 分支逻辑不变；写文件成功输出 `$this->info("已生成: {$targetPath}")`、跳过输出 `$this->warn("Skipped: {$targetPath} (use --force to overwrite)")` 文案逐字不变（writeFile 现返回 bool，由命令层据此输出 info/warn）；FilamentAdminPlugin 绑定示例打印逻辑不变。
    `buildListPageContent`/`buildCreatePageContent`/`buildEditPageContent`（第 467-545 行 Resource 页面生成）本任务暂不迁移（仍留在 PublishCommand）；**已锁定**由 03-02 将这三方法迁入 `StubGenerator` 并改 public，届时 PublishCommand 同步改为委托（D-28 单一来源）——本任务对这三方法不动。
  </action>
  <verify>
    <automated>cd packages/filament-admin && vendor/bin/phpunit --filter PublishCommandTest && vendor/bin/phpstan analyse src/Commands/PublishCommand.php --level=6 && vendor/bin/pint --test src/Commands/PublishCommand.php</automated>
  </verify>
  <acceptance_criteria>
    - `vendor/bin/phpunit --filter PublishCommandTest` 全部通过（10 个测试，0 失败，0 错误）——回归护栏
    - PublishCommand 构造注入存在：`grep -c 'protected StubGenerator \$generator' src/Commands/PublishCommand.php` 返回 1
    - 八个方法体已移除：`grep -c 'protected function \(renderStub\|writeFile\|validatePath\|pluralize\|toSnakeCase\|deriveModelNamespace\|deriveResourceNamespace\|derivePanelPrefix\)(' src/Commands/PublishCommand.php` 返回 0
    - `$signature` 含 `make:`? 否——命令名仍为 `filament-admin:publish`，`grep -c "filament-admin:publish" src/Commands/PublishCommand.php` ≥ 1
    - 输出文案保留：`grep -c '已生成:' src/Commands/PublishCommand.php` ≥ 1 且 `grep -c 'Skipped:' src/Commands/PublishCommand.php` ≥ 1
    - phpstan level 6 退出码 0，pint --test 退出码 0
  </acceptance_criteria>
  <done>PublishCommand 委托 StubGenerator，PublishCommandTest 10 测试全绿，公开签名与输出文案逐字不变</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI 参数 → 文件系统 | `--path` 用户输入决定写文件位置，是路径上溯攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | StubGenerator::validatePath | mitigate | 迁移 PublishCommand 第 360-376 行路径校验，拒绝 `..` / 绝对路径 / `C:\\` / 非 `app/` 前缀（V5 输入验证）；逻辑等价迁移，不放宽 |
| T-03-02 | Tampering | 重构破坏回归测试 | mitigate | "移动不改变"原则 + PublishCommandTest 10 测试作为强制门禁（Pitfall 5） |
| T-03-SC | Tampering | composer 安装 | mitigate | 本 plan 不引入新 composer 包（纯内部重构），无供应链面 |
</threat_model>

<verification>
- `cd packages/filament-admin && vendor/bin/phpunit --filter PublishCommandTest` 10 测试全绿（回归）
- `vendor/bin/phpstan analyse --level=6` 全包零错误
- `vendor/bin/pint --test` 零格式错误
</verification>

<success_criteria>
- StubGenerator 提供八个参数化 public 方法，零命令 IO 耦合
- PublishCommand 委托 StubGenerator，`--model/--resource/--all/--only/--except/--path/--force` 公开契约与输出文案逐字不变
- PublishCommandTest 10 个回归测试继续全绿
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

- 新类：`FilamentAdmin\Services\StubGenerator`
- 新方法（public）：`StubGenerator::renderStub`、`writeFile`、`validatePath`、`pluralize`、`toSnakeCase`、`deriveModelNamespace`、`deriveResourceNamespace`、`derivePanelPrefix`
- 修改：`PublishCommand` 构造函数新增 `StubGenerator $generator` 注入（公开 CLI 契约不变）
</artifacts_produced>

<output>
Create `.planning/phases/03-package-feature-enhancement/03-01-SUMMARY.md` when done
</output>
