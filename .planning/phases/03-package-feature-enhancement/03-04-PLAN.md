---
phase: 03-package-feature-enhancement
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - config/scramble.php
  - app/Providers/AppServiceProvider.php
autonomous: false
requirements: [FEAT-02]
user_setup: []
must_haves:
  truths:
    - "演示项目根 composer.json require-dev 含 dedoc/scramble"
    - "访问 /docs/api 在非生产环境返回 200，展示 OpenAPI 3.0 文档界面"
    - "文档化范围限于 api/v1/* 路由（admin 登录/me/logout），不含 Filament 内部路由"
    - "Scramble 与现有 knuckleswtf/scribe 共存，Scribe 未被移除"
  artifacts:
    - path: "composer.json"
      provides: "dedoc/scramble ^0.13 仅装演示项目 require-dev（D-33）"
      contains: "dedoc/scramble"
    - path: "config/scramble.php"
      provides: "api_path=api/v1 + RestrictedDocsAccess 中间件（D-35）"
      contains: "api_path"
  key_links:
    - from: "config/scramble.php"
      to: "routes/api.php 的 api/v1/admin/* 路由"
      via: "api_path => 'api/v1' 路由过滤"
      pattern: "api/v1"
    - from: "app/Providers/AppServiceProvider.php"
      to: "Dedoc\\Scramble\\Scramble"
      via: "Scramble::routes() 回调精确过滤 api/v1 前缀（Pitfall 4）"
      pattern: "Scramble::"
---

<objective>
实现 FEAT-02 Scramble API 自动文档——在演示项目根（非主包，D-33）引入 `dedoc/scramble ^0.13`（研究已通过 Packagist + GitHub 验证支持 Laravel 13），零配置自动文档化现有 Sanctum API 路由（`api/v1/admin/login|me|logout`），访问 `/docs/api` 看 OpenAPI 3.0 界面。配置 `api_path => 'api/v1'` 并用 `Scramble::routes()` 回调精确过滤（Pitfall 4，避免文档化 Filament 内部/Sanctum token 路由），默认 `RestrictedDocsAccess` 中间件限非生产可访问（D-35）。与现有 `knuckleswtf/scribe` 共存、职责分工（D-34），不移除 Scribe。

Purpose: 给演示项目提供零配置实时 OpenAPI 文档，作为下游"如何启用 Scramble"的参考样例（包文档说明，不替下游强绑依赖）。
Output: 根 composer.json require-dev + config/scramble.php + AppServiceProvider 路由过滤回调。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-package-feature-enhancement/03-CONTEXT.md
@.planning/phases/03-package-feature-enhancement/03-RESEARCH.md
@.planning/phases/03-package-feature-enhancement/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 引入 dedoc/scramble + config/scramble.php + 路由过滤</name>
  <files>composer.json, config/scramble.php, app/Providers/AppServiceProvider.php</files>
  <read_first>
    - composer.json（根，require-dev 段第 45 行起：现有 knuckleswtf/scribe ^5.10，新增 dedoc/scramble 处；D-34 共存不移除 Scribe）
    - config/scribe.php（同类 API 文档工具配置结构参照——config/scramble.php 无直接 analog，参照其文件头/return 数组风格）
    - routes/api.php（根：被文档化目标——POST /api/v1/admin/login、GET /api/v1/admin/me、DELETE /api/v1/admin/logout）
    - app/Providers/AppServiceProvider.php（boot() 注册 Scramble::routes() 回调处）
    - .planning/phases/03-package-feature-enhancement/03-RESEARCH.md §Standard Stack FEAT-02 + §Package Legitimacy Audit（dedoc/scramble ^0.13 已 Approved，slopcheck SLOP 为 npm 误判）+ Pattern 3（Scramble::routes 过滤）+ Code Examples FEAT-02（config 关键项）+ Pitfall 4（不加过滤会文档化全部 api/* 路由）
    - .planning/phases/03-package-feature-enhancement/03-PATTERNS.md §`config/scramble.php`（完整配置模板：api_path/middleware/RestrictedDocsAccess）
  </read_first>
  <action>
    在根 `composer.json` 的 `require-dev` 段添加 `"dedoc/scramble": "^0.13"`（per D-33 仅装演示项目根，不进主包；研究已 Packagist 1026万下载 + GitHub illuminate/contracts ^13.0 双重验证；slopcheck [SLOP] 为 npm 误判非阻塞）。执行 `composer update dedoc/scramble` 安装。

    创建 `config/scramble.php`（参照 03-PATTERNS.md 模板）：
    - `'api_path' => 'api/v1'`（D-35 / Pitfall 4：只匹配 api/v1/* 前缀，避免文档化 Filament 内部 + Sanctum token 路由）
    - `'api_domain' => null`、`'export_path' => 'api.json'`（默认）
    - `'middleware' => ['web', \Dedoc\Scramble\Http\Middleware\RestrictedDocsAccess::class]`（D-35：限非生产环境访问）
    - `'extensions' => []`
    导入 `use Dedoc\Scramble\Http\Middleware\RestrictedDocsAccess;` 于配置文件头部以便引用类常量。

    在 `app/Providers/AppServiceProvider::boot()` 添加 `Scramble::configure()->routes(fn (\Illuminate\Routing\Route $route): bool => str_starts_with($route->uri(), 'api/v1/'));`（Pattern 3 / Pitfall 4 精确过滤，与 api_path 双保险），导入 `use Dedoc\Scramble\Scramble;`。保留 AppServiceProvider 现有 boot 逻辑不动。
    不移除、不修改 `config/scribe.php` 与 `knuckleswtf/scribe`（D-34 共存）。
  </action>
  <verify>
    <automated>composer show dedoc/scramble >/dev/null 2>&1 && php -l config/scramble.php && php -l app/Providers/AppServiceProvider.php && php artisan route:list --path=docs 2>/dev/null | grep -c docs/api</automated>
    <automated>php artisan serve --port=8123 >/tmp/scramble-serve.log 2>&1 & SERVE_PID=$!; sleep 3; CODE=$(curl -s -o /tmp/scramble-docs.html -w '%{http_code}' http://127.0.0.1:8123/docs/api); kill $SERVE_PID 2>/dev/null; echo "docs/api HTTP=$CODE"; test "$CODE" = "200" && grep -qi 'api/v1/admin/login' /tmp/scramble-docs.html</automated>
    <note>第二条 verify 临时起 `php artisan serve` 并用 curl 实测 /docs/api 返回 200 且含 api/v1/admin/login 端点（验证 Scramble 真正渲染而非仅路由注册，APP_ENV 须为 local 以通过 RestrictedDocsAccess）；若执行环境已有 `composer dev` 在跑，可改用其端口。HTTP 200 的人工确认仍由 Task 2 checkpoint 兜底。</note>
  </verify>
  <acceptance_criteria>
    - 根 composer.json require-dev 含 scramble：`grep -c 'dedoc/scramble' composer.json` ≥ 1
    - `composer show dedoc/scramble` 显示已安装 ^0.13
    - Scribe 未被移除（D-34）：`grep -c 'knuckleswtf/scribe' composer.json` ≥ 1
    - `config/scramble.php` 存在且 `grep -c "api/v1" config/scramble.php` ≥ 1 且含 RestrictedDocsAccess
    - AppServiceProvider 含路由过滤：`grep -c "Scramble::configure()->routes\|str_starts_with(\$route->uri(), 'api/v1/')" app/Providers/AppServiceProvider.php` ≥ 1
    - `php -l config/scramble.php` 与 `php -l app/Providers/AppServiceProvider.php` 均无语法错误
    - `php artisan route:list --path=docs` 显示 docs/api 路由已注册
    - 实测 /docs/api 返回 200 且含 api/v1 端点：临时 `php artisan serve`（APP_ENV=local）后 `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8123/docs/api` 返回 `200`，且响应体 `grep -qi 'api/v1/admin/login'` 命中（不仅验证路由注册，更验证 Scramble 实际渲染文档）
  </acceptance_criteria>
  <done>dedoc/scramble 装入根 require-dev，config/scramble.php 限定 api/v1 + 非生产访问，AppServiceProvider 路由过滤就位，Scribe 共存未动，curl 实测 /docs/api 返回 200 并含 api/v1/admin 端点</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: 人工验证 /docs/api 可访问且仅含 api/v1 端点</name>
  <what-built>已在演示项目根引入 dedoc/scramble，配置 api_path=api/v1 + RestrictedDocsAccess + AppServiceProvider 路由过滤回调，文档化 api/v1/admin/* 三个 Sanctum 路由。FEAT-02 的 /docs/api 可访问性是 Manual 验证项（研究 Phase Requirements → Test Map 标注为 Manual smoke）。</what-built>
  <action>人工验证步骤（无自动化代码改动，本任务为人工确认 Task 1 产物可用）：按 how-to-verify 步骤启动本地服务并访问 /docs/api，确认返回 200、仅含 api/v1 端点、Scribe 共存未受影响。</action>
  <how-to-verify>
    1. 确保本地 .env 为非生产环境：`APP_ENV=local`
    2. 启动开发服务（项目根）：`php artisan serve` 或 `composer dev`
    3. 浏览器访问 `http://localhost:8000/docs/api`（或对应本地域名）
    4. 期望：返回 200，展示 Scramble 生成的 OpenAPI 3.0 文档界面（Success Criteria 2）
    5. 期望：文档中出现 `POST /api/v1/admin/login`、`GET /api/v1/admin/me`、`DELETE /api/v1/admin/logout` 三个端点
    6. 期望：文档中**不出现** Filament livewire 内部路由、Sanctum token 管理路由等非 api/v1 端点（Pitfall 4 验证）
    7. 可选：访问 `http://localhost:8000/docs/api.json` 返回 OpenAPI JSON
    8. 确认现有 Scribe 静态文档（`.scribe/` 产物）未受影响（D-34 共存）
  </how-to-verify>
  <resume-signal>确认 /docs/api 返回 200 且只含 api/v1 端点后输入 "approved"；若文档化了非预期路由或 404，描述现象</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 公网访问 → /docs/api | 生产环境暴露 API 结构是信息泄露面 |
| composer 安装 dedoc/scramble | 第三方包供应链面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-08 | Information Disclosure | /docs/api 生产暴露 | mitigate | D-35：默认 `RestrictedDocsAccess` 中间件限 local/非生产可访问，生产无法访问（V4 访问控制） |
| T-03-09 | Information Disclosure | 文档化非预期路由 | mitigate | api_path=api/v1 + Scramble::routes() 回调双保险，只文档化 api/v1/* （Pitfall 4） |
| T-03-SC | Tampering | dedoc/scramble 安装 | mitigate | 研究 §Package Legitimacy Audit 已 Approved（Packagist 1026万下载 + GitHub illuminate/contracts ^13.0 双重核实；slopcheck [SLOP] 为 npm 误判，非阻塞）；仅 require-dev，不进主包 |
</threat_model>

<verification>
- `composer show dedoc/scramble` 已安装
- `php -l config/scramble.php` 与 AppServiceProvider 无语法错误
- `php artisan route:list --path=docs` 含 docs/api
- 人工 checkpoint：/docs/api 返回 200 + 仅 api/v1 端点（Success Criteria 2）
</verification>

<success_criteria>
- 访问 /docs/api 非生产环境返回 200，展示 OpenAPI 3.0 文档界面，含 api/v1/admin 三路由
- 文档化范围限于 api/v1/*，不含 Filament 内部路由
- Scramble 与 Scribe 共存，Scribe 未移除（D-34）
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

- 新依赖：`dedoc/scramble ^0.13`（演示项目根 require-dev，不进主包，D-33）
- 新文件：`config/scramble.php`（api_path=api/v1 + RestrictedDocsAccess）
- 新路由：`/docs/api`、`/docs/api.json`（Scramble 自动注册）
- 修改：`app/Providers/AppServiceProvider::boot()` 新增 Scramble::routes() 过滤回调
</artifacts_produced>

<output>
Create `.planning/phases/03-package-feature-enhancement/03-04-SUMMARY.md` when done
</output>
