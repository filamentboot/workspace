---
phase: 03-package-feature-enhancement
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/filament-admin/composer.json
  - packages/filament-admin/src/Filament/Resources/AdminUsers/AdminUserResource.php
  - packages/filament-admin/src/Listeners/ImpersonationListener.php
  - packages/filament-admin/src/FilamentAdminServiceProvider.php
  - packages/filament-admin/tests/Unit/ImpersonationListenerTest.php
autonomous: true
requirements: [FEAT-01]
user_setup: []
must_haves:
  truths:
    - "超管在 AdminUserResource 列表页可见'模拟登录'Action（非超管行可见、超管行不可见）"
    - "点击模拟切换为目标 admin guard 身份，顶栏显示模拟横幅（结束模拟）"
    - "结束模拟后回到超管会话"
    - "EnterImpersonation/LeaveImpersonation 事件经 ImpersonationListener 写入 activity log（impersonate.enter / impersonate.leave）"
  artifacts:
    - path: "packages/filament-admin/composer.json"
      provides: "require stechstudio/filament-impersonate ^5.5（随主包安装）"
      contains: "stechstudio/filament-impersonate"
    - path: "packages/filament-admin/src/Listeners/ImpersonationListener.php"
      provides: "模拟事件接入 ActivityLogger（D-32）"
      contains: "class ImpersonationListener"
    - path: "packages/filament-admin/src/Filament/Resources/AdminUsers/AdminUserResource.php"
      provides: "列表页 Impersonate Action（仅超管可见）"
      contains: "Impersonate::make()"
  key_links:
    - from: "packages/filament-admin/src/Filament/Resources/AdminUsers/AdminUserResource.php"
      to: "STS\\FilamentImpersonate\\Actions\\Impersonate"
      via: "recordActions 注入 Impersonate::make()->guard('admin')"
      pattern: "Impersonate::make\\(\\)->guard\\('admin'\\)"
    - from: "packages/filament-admin/src/FilamentAdminServiceProvider.php"
      to: "packages/filament-admin/src/Listeners/ImpersonationListener.php"
      via: "registerListeners() Event::listen(EnterImpersonation/LeaveImpersonation)"
      pattern: "ImpersonationListener::class"
    - from: "packages/filament-admin/src/Listeners/ImpersonationListener.php"
      to: "packages/filament-admin/src/Services/ActivityLogger.php"
      via: "构造注入 ActivityLogger + ->log(causer, subject, action)"
      pattern: "ActivityLogger"
---

<objective>
实现 FEAT-01 User Impersonation——引入已验证兼容 Filament 5 与 `admin` guard 的 `stechstudio/filament-impersonate ^5.5`（D-31，研究已通过 Packagist + GitHub 双重核实），在 AdminUserResource 列表页注入仅超管可见的"模拟登录"Action，并通过新建 `ImpersonationListener` 把模拟开始/结束事件接入主包现有 `ActivityLogger`（D-32），满足"操作写入 activity log"。横幅由插件经 `panels::body.start` render hook 自动注册，无需手写。

Purpose: 补齐 kaido-kit 已有的 Impersonation 能力，且模拟操作纳入主包统一审计。
Output: composer.json require 新增 + Impersonate Action + ImpersonationListener + registerListeners 扩展 + 监听器单测。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-package-feature-enhancement/03-CONTEXT.md
@.planning/phases/03-package-feature-enhancement/03-RESEARCH.md
@.planning/phases/03-package-feature-enhancement/03-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 引入插件 + AdminUserResource Impersonate Action（仅超管可见）</name>
  <files>packages/filament-admin/composer.json, packages/filament-admin/src/Filament/Resources/AdminUsers/AdminUserResource.php</files>
  <read_first>
    - packages/filament-admin/composer.json（require 段现状，新增 stechstudio/filament-impersonate 处）
    - packages/filament-admin/src/Filament/Resources/AdminUsers/AdminUserResource.php（recordActions 第 149-153 行：EditAction/DeleteAction/RestoreAction；use 块第 6-22 行；table() 方法签名）
    - packages/filament-admin/src/Models/AdminUser.php（HasRoles trait 第 55 行，hasRole 方法来源）
    - .planning/phases/03-package-feature-enhancement/03-RESEARCH.md §Standard Stack FEAT-01 + §Package Legitimacy Audit（stechstudio/filament-impersonate ^5.5 已 Approved，slopcheck SLOP 为 npm 误判）+ Pitfall 1/3（guard('admin') 必须显式指定；canImpersonate 与 Gate::before 交互）
    - .planning/phases/03-package-feature-enhancement/03-PATTERNS.md §`AdminUserResource.php`（recordActions 修改后形态 + 可见性逻辑）
  </read_first>
  <action>
    在 `packages/filament-admin/composer.json` 的 `require` 段添加 `"stechstudio/filament-impersonate": "^5.5"`（per D-31，研究已 Packagist + GitHub 双重验证 Filament `^4.0|^5.0` 兼容、`admin` guard 通过 `->guard('admin')` 传入；slopcheck [SLOP] 为 npm 误判，不阻塞）。执行 `composer update stechstudio/filament-impersonate` 安装（在 packages/filament-admin/ 目录）。

    在 `AdminUserResource.php` 顶部 `use` 块（按现有 use 排序约定插入）添加 `use STS\FilamentImpersonate\Actions\Impersonate;`。在 `table()` 的 `recordActions([...])` 数组首位插入：
    `Impersonate::make()->guard('admin')->visible(fn ($record): bool => (auth('admin')->user()?->hasRole(config('filament-admin.super_admin_role', 'super_admin')) ?? false) && ! $record->hasRole(config('filament-admin.super_admin_role', 'super_admin')))`
    可见性双条件（Pitfall 3 / V4 访问控制）：当前用户是超管才看到按钮；且不对超管行显示（超管不模拟超管）。务必显式 `->guard('admin')`，不依赖默认值（Pitfall 1：默认 guard 取自 panel，若误用 web 退出模拟会 403）。不在 AdminUser 上添加 `canImpersonate()` 方法（保持 Model 纯净，可见性由 Action visible 控制）。保留现有 EditAction/DeleteAction/RestoreAction 不动。
  </action>
  <verify>
    <automated>cd packages/filament-admin && composer show stechstudio/filament-impersonate >/dev/null 2>&1 && vendor/bin/phpstan analyse src/Filament/Resources/AdminUsers/AdminUserResource.php --level=6 && vendor/bin/pint --test src/Filament/Resources/AdminUsers/AdminUserResource.php</automated>
  </verify>
  <acceptance_criteria>
    - `composer.json` require 含插件：`grep -c 'stechstudio/filament-impersonate' packages/filament-admin/composer.json` ≥ 1
    - `composer show stechstudio/filament-impersonate` 显示已安装版本 ^5.x
    - Action 显式指定 guard：`grep -c "Impersonate::make()->guard('admin')\|Impersonate::make()\s*$" src/Filament/Resources/AdminUsers/AdminUserResource.php` ≥ 1，且文件含 `->guard('admin')`
    - 可见性限制超管：`grep -c "hasRole(config('filament-admin.super_admin_role'" src/Filament/Resources/AdminUsers/AdminUserResource.php` ≥ 1
    - 不在 AdminUser 添加 canImpersonate：`grep -c 'function canImpersonate' src/Models/AdminUser.php` 返回 0
    - phpstan level 6 退出码 0，pint --test 退出码 0
  </acceptance_criteria>
  <done>插件随主包 require 安装，AdminUserResource 列表页含仅超管可见、显式 admin guard 的 Impersonate Action，phpstan/pint 双绿</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ImpersonationListener 接入 ActivityLogger + registerListeners 注册</name>
  <files>packages/filament-admin/src/Listeners/ImpersonationListener.php, packages/filament-admin/src/FilamentAdminServiceProvider.php, packages/filament-admin/tests/Unit/ImpersonationListenerTest.php</files>
  <read_first>
    - packages/filament-admin/src/Listeners/LogAdminLogin.php（监听器风格参照：命名空间、guard 过滤、PHPDoc）
    - packages/filament-admin/src/Services/ActivityLogger.php（log() 签名第 37-43 行：log(Model $causer, Model $subject, string $action, array $before = [], array $after = []): void）
    - packages/filament-admin/src/FilamentAdminServiceProvider.php（registerListeners 第 145-148 行：Event::listen(Login/Failed, LogAdminLogin)；use 块第 16-17 行 Auth Events）
    - packages/filament-admin/tests/Unit/PublishCommandTest.php（测试隔离骨架参照）
    - .planning/phases/03-package-feature-enhancement/03-RESEARCH.md §Code Examples FEAT-01（EnterImpersonation/LeaveImpersonation 事件构造签名：impersonator 必填、LeaveImpersonation::$impersonated 为 ?Authenticatable 可空）+ Pitfall（避免假设 impersonated 非空）
    - .planning/phases/03-package-feature-enhancement/03-PATTERNS.md §`ImpersonationListener.php` + §`FilamentAdminServiceProvider.php`（registerListeners 扩展 + use 新增）
  </read_first>
  <behavior>
    - handleEnter(EnterImpersonation $event)：impersonator 是 AdminUser 时调 ActivityLogger::log(impersonator, impersonated, 'impersonate.enter')
    - handleLeave(LeaveImpersonation $event)：impersonator 是 AdminUser 且 impersonated 非 null 且是 AdminUser 时调 log(..., 'impersonate.leave')；impersonated 为 null 时不抛异常（防御）
    - impersonator 非 AdminUser（如 web guard 普通用户）→ 不记录日志（instanceof 过滤）
    - registerListeners() 后 EnterImpersonation 触发能路由到 handleEnter
  </behavior>
  <action>
    创建 `FilamentAdmin\Listeners\ImpersonationListener`，导入 `FilamentAdmin\Models\AdminUser`、`FilamentAdmin\Services\ActivityLogger`、`STS\FilamentImpersonate\Events\EnterImpersonation`、`STS\FilamentImpersonate\Events\LeaveImpersonation`。构造注入 `public function __construct(protected ActivityLogger $logger) {}`。两个方法：
    - `handleEnter(EnterImpersonation $event): void`：`if (! $event->impersonator instanceof AdminUser) return;` 后 `$this->logger->log($event->impersonator, $event->impersonated, 'impersonate.enter');`
    - `handleLeave(LeaveImpersonation $event): void`：先 `if (! $event->impersonator instanceof AdminUser) return;`，再 `if (! $event->impersonated instanceof AdminUser) return;`（防御 LeaveImpersonation::$impersonated 为 ?Authenticatable 可空，Anti-Pattern），最后 `$this->logger->log($event->impersonator, $event->impersonated, 'impersonate.leave');`
    所有类与公开方法中文 PHPDoc。

    在 `FilamentAdminServiceProvider` 顶部 use 块新增 `use STS\FilamentImpersonate\Events\EnterImpersonation;` 与 `use STS\FilamentImpersonate\Events\LeaveImpersonation;`（按现有 use 排序约定）。在 `registerListeners()` 现有两条 Event::listen 之后追加：
    `Event::listen(EnterImpersonation::class, [Listeners\ImpersonationListener::class, 'handleEnter']);`
    `Event::listen(LeaveImpersonation::class, [Listeners\ImpersonationListener::class, 'handleLeave']);`

    创建 `ImpersonationListenerTest`（Orchestra Testbench TestCase，getPackageProviders 返回 [FilamentAdminServiceProvider::class]）：用 Mockery mock ActivityLogger，断言 handleEnter 触发 `log()` 被调用一次且 action='impersonate.enter'、causer/subject 为 AdminUser 实例；断言 handleLeave 同理 action='impersonate.leave'；断言 impersonated 为 null 时 handleLeave 不调 log 且不抛异常；断言 impersonator 非 AdminUser 时 handleEnter 不调 log。
  </action>
  <verify>
    <automated>cd packages/filament-admin && vendor/bin/phpunit --filter ImpersonationListenerTest && vendor/bin/phpstan analyse src/Listeners/ImpersonationListener.php src/FilamentAdminServiceProvider.php --level=6 && vendor/bin/pint --test src/Listeners/ImpersonationListener.php src/FilamentAdminServiceProvider.php</automated>
  </verify>
  <acceptance_criteria>
    - `ImpersonationListener.php` 存在，含 `handleEnter` 与 `handleLeave` 方法：`grep -c 'function handleEnter\|function handleLeave' src/Listeners/ImpersonationListener.php` 返回 2
    - 监听器构造注入 ActivityLogger：`grep -c 'protected ActivityLogger \$logger' src/Listeners/ImpersonationListener.php` 返回 1
    - handleLeave 对可空 impersonated 防御：`grep -c 'instanceof AdminUser' src/Listeners/ImpersonationListener.php` ≥ 2
    - ServiceProvider 注册两事件：`grep -c 'ImpersonationListener::class' src/FilamentAdminServiceProvider.php` 返回 2
    - 现有登录监听未破坏：`grep -c 'LogAdminLogin::class' src/FilamentAdminServiceProvider.php` 返回 2
    - `vendor/bin/phpunit --filter ImpersonationListenerTest` 通过，phpstan level 6 退出码 0，pint --test 退出码 0
  </acceptance_criteria>
  <done>ImpersonationListener 把模拟开始/结束事件写入 ActivityLogger（含 null 防御），两事件在 registerListeners 注册，监听器单测全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后台用户 → 模拟会话切换 | 模拟切换身份是权限提升面，必须限超管 |
| composer 安装 stechstudio/filament-impersonate | 第三方包供应链面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Elevation of Privilege | Impersonate Action visible() | mitigate | `->visible()` 双条件限 super_admin 角色 + 不模拟超管（V4 访问控制 + V2 认证）；不依赖 Gate::before |
| T-03-06 | Authentication / Session | 模拟会话切换 | mitigate | 显式 `->guard('admin')`（Pitfall 1）；插件 v5.x 已处理 session regeneration + clearImpersonationForGuard（V2/V3） |
| T-03-07 | Repudiation | 模拟操作无审计 | mitigate | ImpersonationListener 写 ActivityLogger（impersonate.enter/leave），满足"activity log 中有记录" |
| T-03-SC | Tampering | stechstudio/filament-impersonate 安装 | mitigate | 研究 §Package Legitimacy Audit 已 Approved（Packagist 330万下载 + GitHub composer.json Filament ^5.0 双重核实；slopcheck [SLOP] 为 npm 误判，非阻塞） |
</threat_model>

<verification>
- `cd packages/filament-admin && vendor/bin/phpunit` 全套绿（含 ImpersonationListenerTest）
- `vendor/bin/phpstan analyse --level=6` 零错误
- `vendor/bin/pint --test` 零格式错误
- 手测（用户）：超管登录后台 → AdminUserResource 列表页对非超管行可见"模拟登录"→ 点击切换 + 顶栏横幅 → 结束回超管 → activity_log 有 impersonate.enter/leave 记录（满足 Success Criteria 1）
</verification>

<success_criteria>
- 超管在 AdminUserResource 列表页见"模拟登录"按钮，点击切换 admin guard 身份，顶栏显示模拟横幅（结束模拟），结束后回超管会话
- 模拟操作在 activity log 有记录（impersonate.enter / impersonate.leave）
- 非超管不可见 Action（V4 访问控制）
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

- 新依赖：`stechstudio/filament-impersonate ^5.5`（主包 require，随用户 composer require 安装）
- 新类：`FilamentAdmin\Listeners\ImpersonationListener`（方法 handleEnter / handleLeave）
- 新测试类：`ImpersonationListenerTest`
- 修改：`AdminUserResource::table()` recordActions 新增 Impersonate Action；`FilamentAdminServiceProvider::registerListeners()` 新增两条 Event::listen
- 新 activity log action 值：`impersonate.enter`、`impersonate.leave`
</artifacts_produced>

<output>
Create `.planning/phases/03-package-feature-enhancement/03-03-SUMMARY.md` when done
</output>
