---
phase: 03-package-feature-enhancement
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - packages/filament-admin/src/Services/StubGenerator.php
  - packages/filament-admin/src/Commands/MakeFilamentAdminModelCommand.php
  - packages/filament-admin/src/Commands/MakeFilamentAdminResourceCommand.php
  - packages/filament-admin/src/Commands/MakeFilamentAdminMigrationCommand.php
  - packages/filament-admin/src/Commands/MakeFilamentAdminTestCommand.php
  - packages/filament-admin/src/FilamentAdminServiceProvider.php
  - packages/filament-admin/tests/Unit/MakeModelCommandTest.php
  - packages/filament-admin/tests/Unit/MakeResourceCommandTest.php
  - packages/filament-admin/tests/Unit/MakeMigrationCommandTest.php
  - packages/filament-admin/tests/Unit/MakeTestCommandTest.php
autonomous: true
requirements: [FEAT-03]
must_haves:
  truths:
    - "执行 make:filament-admin-model Product 生成 app/Models/Product.php，命名空间 App\\Models 正确"
    - "执行 make:filament-admin-resource ProductResource 生成 Filament Resource 文件（含 3 个 Page）"
    - "执行 make:filament-admin-migration 生成 database/migrations 下 create_{table}_table 迁移 stub"
    - "执行 make:filament-admin-test 生成 tests/Feature 下 Feature Test stub"
    - "四个命令各有 PHPUnit Feature Test 覆盖（生成/命名空间/skip/force/非法名拒绝）"
    - "四个命令均在 FilamentAdminServiceProvider::registerCommands() 注册"
  artifacts:
    - path: "packages/filament-admin/src/Commands/MakeFilamentAdminModelCommand.php"
      provides: "make:filament-admin-model 命令（D-28 薄包装，调用 StubGenerator）"
      contains: "make:filament-admin-model"
    - path: "packages/filament-admin/src/Commands/MakeFilamentAdminResourceCommand.php"
      provides: "make:filament-admin-resource 命令"
      contains: "make:filament-admin-resource"
    - path: "packages/filament-admin/src/Commands/MakeFilamentAdminMigrationCommand.php"
      provides: "make:filament-admin-migration 命令"
      contains: "make:filament-admin-migration"
    - path: "packages/filament-admin/src/Commands/MakeFilamentAdminTestCommand.php"
      provides: "make:filament-admin-test 命令"
      contains: "make:filament-admin-test"
  key_links:
    - from: "packages/filament-admin/src/Commands/MakeFilamentAdminModelCommand.php"
      to: "packages/filament-admin/src/Services/StubGenerator.php"
      via: "构造注入 StubGenerator + renderStub/writeFile 委托"
      pattern: "StubGenerator"
    - from: "packages/filament-admin/src/Commands/MakeFilamentAdminResourceCommand.php"
      to: "packages/filament-admin/src/Services/StubGenerator.php"
      via: "委托 StubGenerator::buildListPageContent/buildCreatePageContent/buildEditPageContent 生成 3 个 Page（D-28 单一来源）"
      pattern: "generator->buildListPageContent"
    - from: "packages/filament-admin/src/FilamentAdminServiceProvider.php"
      to: "四个 Make*Command"
      via: "registerCommands() 的 $this->commands([...]) 注册"
      pattern: "MakeFilamentAdmin.*Command::class"
---

<objective>
实现 FEAT-03 CRUD 生成器的四个 artisan 命令——`make:filament-admin-model` / `make:filament-admin-resource` / `make:filament-admin-migration` / `make:filament-admin-test`。每个命令是薄包装（D-28），构造注入 03-01 的 `StubGenerator`，各管一种类型的 stub 渲染与文件写入；输出路径复用 publish 策略（D-30）；只生成可运行空骨架，不做字段交互（D-29）。四个命令在 `registerCommands()` 注册，并各配一个 PHPUnit Feature Test（Wave 0 测试与实现合并在本 plan，因依赖 03-01 服务存在）。

Purpose: 给下游开发者提供对齐 Filament 原生 `make:filament-resource` UX 的单类型生成命令，补齐 kaido-kit 已有能力。
Output: 4 个命令类 + 4 个测试类 + ServiceProvider registerCommands 扩展。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-package-feature-enhancement/03-CONTEXT.md
@.planning/phases/03-package-feature-enhancement/03-RESEARCH.md
@.planning/phases/03-package-feature-enhancement/03-PATTERNS.md
@.planning/phases/03-package-feature-enhancement/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 四个 make:* 命令类（薄包装委托 StubGenerator）</name>
  <files>packages/filament-admin/src/Commands/MakeFilamentAdminModelCommand.php, packages/filament-admin/src/Commands/MakeFilamentAdminResourceCommand.php, packages/filament-admin/src/Commands/MakeFilamentAdminMigrationCommand.php, packages/filament-admin/src/Commands/MakeFilamentAdminTestCommand.php, packages/filament-admin/src/Services/StubGenerator.php, packages/filament-admin/src/Commands/PublishCommand.php</files>
  <read_first>
    - packages/filament-admin/src/Services/StubGenerator.php（03-01 产物，被注入的服务）
    - packages/filament-admin/src/Commands/PublishCommand.php（参照：publishModel 第 185、publishResource 第 211、publishMigration 第 258、publishFeatureTest 第 281；Resource 页面生成 buildListPageContent 第 467/buildCreatePageContent 第 493/buildEditPageContent 第 519）
    - packages/filament-admin/src/Services/StubGenerator.php（03-01 产物；本任务将 PublishCommand 的 buildListPageContent/buildCreatePageContent/buildEditPageContent 三方法迁入此处并改 public，PublishCommand 同步改为委托）
    - packages/filament-admin/src/Commands/CleanActivityLogs.php（命令类风格 + signature/description 中文写法参照）
    - .planning/phases/03-package-feature-enhancement/03-PATTERNS.md §`MakeFilamentAdminModelCommand.php`（命令签名、handle 结构、四命令差异表）
    - packages/filament-admin/stubs/Model.stub, Resource.stub, Migration.stub, FeatureTest.stub（占位符：Model={{namespace}}/{{class}}/{{table}}；Resource={{namespace}}/{{class}}/{{model}}/{{modelNamespace}}/{{modelLabel}}/{{pluralClass}}；Migration={{table}}；FeatureTest={{namespace}}/{{resourceNamespace}}/{{resource}}/{{model}}/{{modelLabel}}/{{appModelNamespace}}）
  </read_first>
  <behavior>
    - make:filament-admin-model Product → 生成 app/Models/Product.php，含 `namespace App\Models;` 与 `class Product extends Model`，table=products
    - make:filament-admin-resource Product → 生成 app/Filament/Resources/Products/ProductResource.php（namespace App\Filament\Resources\Products）+ Pages/ListProducts.php + CreateProduct.php + EditProduct.php
    - make:filament-admin-migration Product → 生成 database/migrations/{timestamp}_create_products_table.php
    - make:filament-admin-test Product → 生成 tests/Feature/ProductResourceTest.php
    - 任意命令传非 PascalCase 名（如 'product' 或 '1Foo'）→ error + 返回 FAILURE
    - 目标文件已存在且无 --force → 输出含 'Skipped:' + 返回 SUCCESS + 文件未改
    - 目标文件已存在且带 --force → 覆盖 + 输出含 '已生成:'
    - --path 含 '..' → error '不允许 .. 路径上溯' + 返回 FAILURE
  </behavior>
  <action>
    创建四个命令类，命名空间 `FilamentAdmin\Commands`，各 `use FilamentAdmin\Services\StubGenerator;` + `use Illuminate\Console\Command;` + `use Symfony\Component\Console\Command\Command as CommandExitCode;`，构造注入 `public function __construct(protected StubGenerator $generator) { parent::__construct(); }`。命令名严格按 Success Criteria 3 字面（kebab-case，不可变）。

    **MakeFilamentAdminModelCommand**：`$signature = 'make:filament-admin-model {name} {--path=} {--force}'`，description 中文。handle()：preg_match('/^[A-Z][A-Za-z0-9]*$/', $name) 校验（非法→error+FAILURE，V5 输入验证）；--path 非空时调 `$this->generator->validatePath()` 校验（失败→error+FAILURE）；调 `renderStub('Model', ['namespace'=>deriveModelNamespace(), 'class'=>$name, 'table'=>toSnakeCase(pluralize($name))])`；空内容→error+FAILURE；`writeFile(base_path('app/Models/'.$name.'.php'), $content, (bool)option('force'))`；written→info("已生成: ...")，否则 warn("Skipped: ... (use --force to overwrite)")；返回 SUCCESS。

    **MakeFilamentAdminMigrationCommand**：`$signature = 'make:filament-admin-migration {name} {--path=} {--force}'`（per Open Question 2 / Pitfall 6：name 为参数，命令名严格 `make:filament-admin-migration` 无后缀）。渲染 'Migration' stub（vars=['table'=>toSnakeCase(pluralize($name))]），目标 `database/migrations/'.date('Y_m_d_His').'_create_'.$table.'_table.php'`（对齐 PublishCommand 第 269-271 行）。

    **MakeFilamentAdminTestCommand**：`$signature = 'make:filament-admin-test {name} {--path=} {--force}'`。渲染 'FeatureTest' stub，vars 对齐 PublishCommand 第 285-291 行（namespace='Tests\\Feature'、resourceNamespace=deriveResourceNamespace($name)、resource=$name.'Resource'、model=$name、modelLabel=$name、appModelNamespace=deriveModelNamespace()），目标 `tests/Feature/'.$name.'ResourceTest.php'`。

    **MakeFilamentAdminResourceCommand**：`$signature = 'make:filament-admin-resource {name} {--path=} {--force}'`。接受 `Product` 或 `ProductResource` 形式 name——若以 Resource 结尾则剥离得到 baseName（与 PublishCommand --resource 语义一致）。渲染 'Resource' stub，vars 含 namespace（deriveResourceNamespace(baseName, path)）、class=baseName.'Resource'、model=baseName、modelNamespace=deriveModelNamespace()、modelLabel=baseName、pluralClass=pluralize(baseName)；目标 `app/Filament/Resources/{pluralBase}/{baseName}Resource.php`（path 受 --path 覆盖）。同时生成 3 个 Page 文件（写入 Pages/ 子目录）。**页面构建方法归属（锁定，per D-28 零重复逻辑、消除 03-01 遗留歧义）：** 将 PublishCommand 第 467-545 行的 `buildListPageContent` / `buildCreatePageContent` / `buildEditPageContent` 三个方法迁入 `StubGenerator`，改为 public 方法（签名等价、逻辑等价，遵循 03-01 的“移动不改变”原则），命名 `StubGenerator::buildListPageContent(string $resourceClass, string $modelClass): string` 等（参数化原先从命令读取的值）。迁移后：本 `MakeFilamentAdminResourceCommand` 与重构后的 `PublishCommand` **均通过 `$this->generator->buildListPageContent(...)` 委托同一来源**，PublishCommand 内不再保留这三个方法体（删除并改为委托）。不允许在命令内重写或保留这三个方法的副本（单一来源）。

    所有命令类与 public 方法须中文 PHPDoc；phpstan level 6 友好（返回类型 `: int`，参数类型完整）。
  </action>
  <verify>
    <automated>cd packages/filament-admin && vendor/bin/phpstan analyse src/Commands src/Services/StubGenerator.php --level=6 && vendor/bin/pint --test src/Commands src/Services/StubGenerator.php && vendor/bin/phpunit --filter PublishCommandTest</automated>
  </verify>
  <acceptance_criteria>
    - 四个命令文件存在，各声明 `namespace FilamentAdmin\Commands;`
    - 命令名字面正确：`grep -l 'make:filament-admin-model' src/Commands/MakeFilamentAdminModelCommand.php` 命中；resource/migration/test 同理（命令名无多余后缀）
    - 四命令均构造注入 StubGenerator：`grep -c 'protected StubGenerator \$generator' src/Commands/MakeFilamentAdmin*Command.php` 返回 4
    - 四命令均含 PascalCase 校验：`grep -c "preg_match('/\^\[A-Z\]" src/Commands/MakeFilamentAdmin*Command.php` 返回 4
    - 命令内不重写 renderStub/writeFile 实现（仅委托）：`grep -c 'function renderStub\|function writeFile' src/Commands/MakeFilamentAdmin*Command.php` 返回 0
    - Resource 命令委托 StubGenerator 生成页面（不重写页面构建逻辑）：`grep -c 'generator->buildListPageContent\|generator->buildCreatePageContent\|generator->buildEditPageContent' src/Commands/MakeFilamentAdminResourceCommand.php` ≥ 3；且 `grep -c 'function buildListPageContent\|function buildCreatePageContent\|function buildEditPageContent' src/Commands/MakeFilamentAdminResourceCommand.php` 返回 0
    - 三个页面构建方法已迁入 StubGenerator：`grep -c 'public function buildListPageContent\|public function buildCreatePageContent\|public function buildEditPageContent' src/Services/StubGenerator.php` 返回 3
    - phpstan level 6 退出码 0，pint --test 退出码 0
  </acceptance_criteria>
  <done>四个 make:* 命令委托 StubGenerator 生成对应类型骨架，参数校验与输出文案对齐 publish，phpstan/pint 双绿</done>
</task>

<task type="auto">
  <name>Task 2: 注册四命令 + 四个 PHPUnit Feature Test</name>
  <files>packages/filament-admin/src/FilamentAdminServiceProvider.php, packages/filament-admin/tests/Unit/MakeModelCommandTest.php, packages/filament-admin/tests/Unit/MakeResourceCommandTest.php, packages/filament-admin/tests/Unit/MakeMigrationCommandTest.php, packages/filament-admin/tests/Unit/MakeTestCommandTest.php</files>
  <read_first>
    - packages/filament-admin/src/FilamentAdminServiceProvider.php（registerCommands 第 74-83 行：现有 $this->commands([PublishCommand, CleanActivityLogs, CleanLoginLogs]) 数组）
    - packages/filament-admin/tests/Unit/PublishCommandTest.php（测试骨架全量参照：getPackageProviders/getApplicationBasePath/setUp 目录创建 第 60-76 行/tearDown deleteDirectoryNative 第 99-126 行）
    - .planning/phases/03-package-feature-enhancement/03-PATTERNS.md §`测试文件`（测试类骨架 + 四个必含测试方法清单）
    - .planning/phases/03-package-feature-enhancement/03-RESEARCH.md §Code Examples FEAT-03（MakeModelCommandTest 示例 + Wave 0 Gaps 清单）
    - packages/filament-admin/src/Commands/MakeFilamentAdminModelCommand.php（Task 1 产物，被测命令）
  </read_first>
  <action>
    在 `registerCommands()` 的 `$this->commands([...])` 数组追加四项：`Commands\MakeFilamentAdminModelCommand::class`、`Commands\MakeFilamentAdminResourceCommand::class`、`Commands\MakeFilamentAdminMigrationCommand::class`、`Commands\MakeFilamentAdminTestCommand::class`（保持现有三命令不动）。

    创建四个测试类（`namespace FilamentAdmin\Tests\Unit;`），全量复用 PublishCommandTest 的 Orchestra Testbench 隔离骨架：`getPackageProviders()` 返回 `[FilamentAdminServiceProvider::class]`，`getApplicationBasePath()` 返回 `$this->tempBase`，setUp 创建临时目录树（含 app/Models、app/Filament、database/migrations、tests/Feature、bootstrap/cache、storage/* 等，对齐 PublishCommandTest 第 60-76 行目录列表），tearDown 用原生 deleteDirectoryNative 清理（复制第 99-126 行）。每个测试类含四个测试方法：
    - `test_{type}_generates_file_with_correct_namespace`：artisan 命令 + assertExitCode(SUCCESS) + assertTrue(File::exists) + assertStringContainsString 命名空间/类名（Model 验 `namespace App\Models;` + `class Product extends Model`；Resource 验 Resource 文件存在 + 3 Page 文件存在；Migration 验 `database/migrations` 下匹配 `*_create_products_table.php` 文件存在 + 含 `Schema::create('products'`；Test 验 `tests/Feature/ProductResourceTest.php` 存在）
    - `test_{type}_skips_existing_without_force`：预置同名文件 → expectsOutputToContain('Skipped:') + assertExitCode(SUCCESS) + 文件内容未变
    - `test_{type}_overwrites_existing_with_force`：预置占位文件 → --force → 内容已替换为真实 stub 渲染
    - `test_{type}_rejects_invalid_name`：传 'product'（小写）→ assertExitCode(FAILURE)
    Migration 测试断言文件存在时用 glob/File::glob 匹配时间戳前缀文件（不硬编码 timestamp）。
  </action>
  <verify>
    <automated>cd packages/filament-admin && vendor/bin/phpunit --filter "MakeModelCommandTest|MakeResourceCommandTest|MakeMigrationCommandTest|MakeTestCommandTest" && vendor/bin/phpunit --filter PublishCommandTest</automated>
  </verify>
  <acceptance_criteria>
    - 四命令在 ServiceProvider 注册：`grep -c 'MakeFilamentAdmin.*Command::class' src/FilamentAdminServiceProvider.php` 返回 4
    - 现有三命令仍注册：`grep -c 'PublishCommand::class\|CleanActivityLogs::class\|CleanLoginLogs::class' src/FilamentAdminServiceProvider.php` 返回 3
    - 四个测试类存在且每类 ≥4 个 test 方法：`grep -c 'public function test' tests/Unit/MakeModelCommandTest.php` ≥ 4（其余三个同理）
    - `vendor/bin/phpunit --filter "MakeModelCommandTest|MakeResourceCommandTest|MakeMigrationCommandTest|MakeTestCommandTest"` 全部通过
    - `vendor/bin/phpunit --filter PublishCommandTest` 仍全绿（注册数组改动未破坏回归）
  </acceptance_criteria>
  <done>四命令注册到 ServiceProvider，四测试类各 ≥4 测试全绿，PublishCommandTest 回归继续全绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artisan name 参数 → 类名/文件名 | name 拼入命名空间与文件路径，是注入面 |
| artisan --path 参数 → 写文件位置 | 路径上溯攻击面 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-03 | Tampering | make:* name 参数 | mitigate | 每个命令 handle() 入口 `preg_match('/^[A-Z][A-Za-z0-9]*$/', $name)`，非法名拒绝并返回 FAILURE（V5 输入验证，由 test_{type}_rejects_invalid_name 覆盖） |
| T-03-04 | Tampering | make:* --path 参数 | mitigate | 委托 StubGenerator::validatePath 拒绝 `..`/绝对路径/非 app 前缀（V5 路径上溯防护） |
| T-03-SC | Tampering | composer 安装 | accept | 本 plan 不引入新 composer 包（纯主包内部新增类），无供应链面 |
</threat_model>

<verification>
- `vendor/bin/phpunit`（位于 packages/filament-admin/）全套绿，含四个新测试 + PublishCommandTest 回归
- `vendor/bin/phpstan analyse --level=6` 零错误
- `vendor/bin/pint --test` 零格式错误
- 手动 smoke（演示项目根）：`php artisan make:filament-admin-model Product` 在 app/Models 生成文件
</verification>

<success_criteria>
- 四个命令名严格为 make:filament-admin-{model,resource,migration,test}，无多余后缀
- 各命令生成对应类型可运行空骨架到正确目录，命名空间正确
- 四命令各有 ≥4 个 PHPUnit 测试覆盖（生成/skip/force/非法名）全绿
- PublishCommandTest 回归全绿（Success Criteria 3 字面达成）
</success_criteria>

<artifacts_produced>
## Artifacts this phase produces（本 plan 部分）

- 新 CLI 命令：`make:filament-admin-model`、`make:filament-admin-resource`、`make:filament-admin-migration`、`make:filament-admin-test`
- 新类：`MakeFilamentAdminModelCommand`、`MakeFilamentAdminResourceCommand`、`MakeFilamentAdminMigrationCommand`、`MakeFilamentAdminTestCommand`
- 新测试类：`MakeModelCommandTest`、`MakeResourceCommandTest`、`MakeMigrationCommandTest`、`MakeTestCommandTest`
- 修改：`FilamentAdminServiceProvider::registerCommands()` 注册数组新增四项
</artifacts_produced>

<output>
Create `.planning/phases/03-package-feature-enhancement/03-02-SUMMARY.md` when done
</output>
