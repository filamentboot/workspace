---
phase: 05-demo-site
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/filament-admin/database/seeders/DemoSeeder.php
  - packages/filament-admin/src/Commands/DemoReset.php
  - packages/filament-admin/src/FilamentAdminServiceProvider.php
  - routes/console.php
  - database/seeders/DatabaseSeeder.php
  - config/app.php
  - .env.example
  - tests/Feature/Demo/DemoResetCommandTest.php
  - tests/Feature/Demo/DemoSeederTest.php
autonomous: true
requirements: [DEMO-02, DEMO-03]
must_haves:
  truths:
    - "运行 demo:reset 后数据库回到初始演示状态：仅 demo 账号存在、roles/permissions/menus 结构保留、login_logs/activity_log 清空"
    - "在非演示环境（APP_DEMO 未开启且无 --force）执行 demo:reset 返回 FAILURE 且不触碰任何数据"
    - "DemoSeeder 创建可登录的演示账号 demo@example.com / demo123"
    - "Laravel scheduler 注册了每日 04:00 执行 demo:reset 的任务"
  artifacts:
    - path: "packages/filament-admin/src/Commands/DemoReset.php"
      provides: "demo:reset 控制台命令（含生产环境护栏）"
      contains: "demo:reset"
    - path: "packages/filament-admin/database/seeders/DemoSeeder.php"
      provides: "演示账号与角色种子"
      contains: "demo@example.com"
    - path: "tests/Feature/Demo/DemoResetCommandTest.php"
      provides: "reset 行为与护栏的 Feature 测试"
    - path: "tests/Feature/Demo/DemoSeederTest.php"
      provides: "演示账号种子的 Feature 测试"
  key_links:
    - from: "routes/console.php"
      to: "FilamentAdmin\\Commands\\DemoReset"
      via: "Schedule::command(...)->dailyAt('04:00')"
      pattern: "DemoReset.*dailyAt\\('04:00'\\)"
    - from: "packages/filament-admin/src/FilamentAdminServiceProvider.php"
      to: "FilamentAdmin\\Commands\\DemoReset"
      via: "registerCommands() 数组注册"
      pattern: "Commands\\\\DemoReset::class"
---

<objective>
实现演示站的数据重置核心链路：新建幂等的 DemoSeeder（演示账号 demo@example.com / demo123），新建 demo:reset 控制台命令（含 [BLOCKING] 生产环境护栏，per D-05-04），注册命令到主包 ServiceProvider，并在 routes/console.php 注册每日凌晨 04:00 的 cron（per D-05-04）。同时引入 APP_DEMO 环境标志供护栏与下游 DEMO-03 屏蔽逻辑复用。

Purpose: DEMO-02（数据重置 + cron）落地，且 DemoSeeder 是 DEMO-03（演示账号 + 高危屏蔽，Plan 05-02）的共享前置依赖——本 plan 必须先产出 demo 账号，Plan 05-02 的 Gate 测试才能验证。

Output: DemoSeeder.php、DemoReset.php、ServiceProvider 命令注册、console cron、DatabaseSeeder 演示链路、config/app.php demo 标志、.env.example 占位、2 个 Feature 测试文件（同时充当 Wave 0 测试脚手架）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-demo-site/05-RESEARCH.md
@.planning/phases/05-demo-site/05-PATTERNS.md
@.planning/phases/05-demo-site/05-VALIDATION.md
</context>

<artifacts_this_phase_produces>
本 plan 产出/引入的符号（供下游 plan 与 executor 引用）：

- **类**：`FilamentAdmin\Database\Seeders\DemoSeeder`、`FilamentAdmin\Commands\DemoReset`
- **命令签名**：`demo:reset {--force : 跳过环境护栏（仅测试用）}`
- **新账号**：`demo@example.com` / `demo123`（account=`demo`，nickname=`演示账号`）
- **角色决策（本 plan 锁定）**：演示账号挂 `super_admin` 角色（展示后台全貌），写操作的屏蔽由 Plan 05-02 的 Gate::before 演示拒绝分支负责（须先于超管放行执行）。本 plan 不创建独立 demo 角色。
- **env 变量**：`APP_DEMO`（默认 false），消费方：`config('app.demo')` → DemoReset 护栏 + Plan 05-02 的 isDemoUser/banner
- **config 键**：`config/app.php` 新增 `'demo' => env('APP_DEMO', false)`
- **cron**：`routes/console.php` 中 `Schedule::command(DemoReset::class)->dailyAt('04:00')`
- **测试文件**：`tests/Feature/Demo/DemoResetCommandTest.php`、`tests/Feature/Demo/DemoSeederTest.php`
</artifacts_this_phase_produces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新建 DemoSeeder + APP_DEMO 配置 + DatabaseSeeder 演示链路</name>
  <files>packages/filament-admin/database/seeders/DemoSeeder.php, database/seeders/DatabaseSeeder.php, config/app.php, .env.example, tests/Feature/Demo/DemoSeederTest.php</files>
  <read_first>
    - packages/filament-admin/database/seeders/SuperAdminSeeder.php（analog：firstOrCreate 幂等 + forgetCachedPermissions + assignRole 模式，PATTERNS.md 第 79-130 行）
    - database/seeders/DatabaseSeeder.php（现有 $this->call([...]) 链路，需追加 DemoSeeder）
    - config/app.php 末尾（在 maintenance 段后新增 demo 键）
    - .env.example（追加 APP_DEMO 占位）
    - .planning/phases/05-demo-site/05-RESEARCH.md §2「DemoSeeder」+ §5（APP_DEMO 标志）
  </read_first>
  <behavior>
    - Test 1（DemoSeederTest）：运行 DemoSeeder 后，AdminUser 中存在 email=demo@example.com 的记录，account=demo
    - Test 2：该 demo 账号密码 demo123 通过 Hash::check 校验为真（验证 hashed cast 生效）
    - Test 3：该 demo 账号已分配 super_admin 角色（hasRole('super_admin') 为真，guard=admin）
    - Test 4：重复运行 DemoSeeder 两次不产生重复账号（firstOrCreate 幂等）
  </behavior>
  <action>
    新建 `FilamentAdmin\Database\Seeders\DemoSeeder`，结构镜像 SuperAdminSeeder：开头 `app(PermissionRegistrar::class)->forgetCachedPermissions()`；用 `Role::firstOrCreate(['name' => config('filament-admin.super_admin_role','super_admin'), 'guard_name' => 'admin'])` 取 super_admin 角色（与 SuperAdminSeeder 同角色，复用，不新建 demo 角色——角色决策见 artifacts 章节）；用 `AdminUser::firstOrCreate(['email' => 'demo@example.com'], ['account' => 'demo', 'nickname' => '演示账号', 'password' => 'demo123', 'email_verified_at' => now()])`（password 走 hashed cast 自动哈希，传明文）；`$admin->assignRole($role)`；结尾 `$this->command->info('演示账号已创建：demo@example.com / demo123')`。所有 PHPDoc/注释用简体中文，遵循 PSR-12。
    在 `database/seeders/DatabaseSeeder.php` 的 `$this->call([...])` 数组中，仅当 `config('app.demo')` 为真时追加 `DemoSeeder::class`（用条件判断 push，避免生产环境播种 demo 账号）；import `FilamentAdmin\Database\Seeders\DemoSeeder`。
    在 `config/app.php` 末尾返回数组内新增键 `'demo' => env('APP_DEMO', false)`。
    在 `.env.example` 追加 `APP_DEMO=false` 占位行（附中文注释「演示站标志：true 时启用 demo 账号播种与 demo:reset 护栏放行」）。
    新建 `tests/Feature/Demo/DemoSeederTest.php`，使用 RefreshDatabase，实现 behavior 四个断言。
  </action>
  <verify>
    <automated>php artisan test --filter=DemoSeeder</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=DemoSeeder` 全绿（VALIDATION.md 任务 5-XX-06）
    - DemoSeeder 运行后 `AdminUser::where('email','demo@example.com')->exists()` 为真，且 `Hash::check('demo123', $user->password)` 为真
    - demo 账号 `hasRole('super_admin')` 为真
    - 二次运行 DemoSeeder 后 demo 账号数量仍为 1（幂等）
    - `config('app.demo')` 默认返回 false；`.env.example` 含 `APP_DEMO=false`
  </acceptance_criteria>
  <done>DemoSeeder 幂等创建可登录的 super_admin demo 账号；APP_DEMO 配置链路就位；DatabaseSeeder 仅演示环境播种 demo 账号；DemoSeederTest 全绿。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 新建 demo:reset 命令（含生产护栏）+ ServiceProvider 注册 + cron</name>
  <files>packages/filament-admin/src/Commands/DemoReset.php, packages/filament-admin/src/FilamentAdminServiceProvider.php, routes/console.php, tests/Feature/Demo/DemoResetCommandTest.php</files>
  <read_first>
    - packages/filament-admin/src/Commands/CleanActivityLogs.php（analog：$signature / $description / handle(): int 返回 self::SUCCESS 模式，PATTERNS.md 第 28-73 行）
    - packages/filament-admin/src/FilamentAdminServiceProvider.php 第 77-89 行 registerCommands()（在数组末尾追加 DemoReset）
    - routes/console.php（现有两条 dailyAt 模板，第 13-16 行）
    - .planning/phases/05-demo-site/05-RESEARCH.md §2「重置策略 方案 A」+ §2 landmine（[BLOCKING] 护栏）
    - Task 1 产出的 DemoSeeder（reset 链路依赖其重建 demo 账号）
  </read_first>
  <behavior>
    - Test 1（DemoResetCommandTest，护栏）：在 APP_DEMO=false 且不带 --force 调用 demo:reset，命令返回 FAILURE（断言 artisan 返回码非 0），且数据库未被清空（已有数据条数不变）
    - Test 2（reset 行为，APP_DEMO=true 或带 --force）：执行后仅 demo 账号存在于 admin_users（admin@example.com 不存在，因为演示环境 DatabaseSeeder 只播 demo），roles/permissions/menus 表非空（结构保留），login_logs/activity_log 表为空（清空）
    - Test 3（cron 源断言）：routes/console.php 经 schedule:list 后含 demo:reset 任务且时间为 04:00
  </behavior>
  <action>
    新建 `FilamentAdmin\Commands\DemoReset`，继承 `Illuminate\Console\Command`，`$signature = 'demo:reset {--force : 跳过环境护栏（仅测试用）}'`，`$description = '重置演示站数据到初始演示状态'`，`handle(): int`。handle 开头 [BLOCKING] 护栏：`if (! $this->option('force') && ! config('app.demo', false)) { $this->error('仅演示环境可执行（APP_DEMO=true），生产环境请勿运行。'); return self::FAILURE; }`。护栏通过后执行重置（方案 A，per RESEARCH §2）：`$this->call('migrate:fresh', ['--seed' => true, '--force' => true])` 然后 `$this->call('cache:clear')`，结尾 `$this->info('演示数据已重置。'); return self::SUCCESS;`。重置依赖 DatabaseSeeder 在演示环境播种 DemoSeeder（Task 1 已建链路）。PHPDoc/注释简体中文，PSR-12。
    在 `FilamentAdminServiceProvider::registerCommands()` 的 `$this->commands([...])` 数组末尾追加 `Commands\DemoReset::class`（保持在 `if ($this->app->runningInConsole())` 内）。
    在 `routes/console.php` 顶部追加 `use FilamentAdmin\Commands\DemoReset;`，并在现有 dailyAt 调度后追加 `Schedule::command(DemoReset::class)->dailyAt('04:00');`，附中文注释「每日凌晨 4 点重置演示数据（仅演示环境生效，命令内有护栏）」。
    新建 `tests/Feature/Demo/DemoResetCommandTest.php`（RefreshDatabase），实现 behavior 三个断言；护栏测试用 `config(['app.demo' => false])` 后断言 `Artisan::call('demo:reset')` 返回非 0 且数据不变；reset 测试用 `--force` 或 `config(['app.demo' => true])`。
  </action>
  <verify>
    <automated>php artisan test --filter=DemoReset</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan test --filter=DemoReset` 全绿（VALIDATION.md 任务 5-XX-01/02/03）
    - [BLOCKING] 护栏：非演示环境（APP_DEMO 未开 + 无 --force）执行 demo:reset 返回 FAILURE，数据库数据不变（断言条数前后一致）
    - reset 后 `AdminUser::where('email','demo@example.com')->exists()` 为真且为唯一账号，roles/permissions/menus 非空，login_logs/activity_log 为空
    - `php artisan schedule:list` 输出含 `demo:reset` 且执行时间 04:00（`grep -q "demo:reset" routes/console.php` 且 `grep -q "dailyAt('04:00')" routes/console.php`）
    - `grep -q "Commands\\\\DemoReset::class" packages/filament-admin/src/FilamentAdminServiceProvider.php` 命中
  </acceptance_criteria>
  <done>demo:reset 命令落地且带生产环境护栏（[BLOCKING]）；命令已注册；cron 04:00 已配置；DemoResetCommandTest 全绿。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 运维/cron → 数据库 | demo:reset 执行 migrate:fresh 会 drop 全部表，跨越运维操作与生产数据边界 |
| 部署环境 → 命令护栏 | APP_DEMO 标志决定命令是否放行清库，环境配置错误可致越界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-02 | Denial / Tampering | `demo:reset` 命令 | mitigate | handle() 开头护栏：`config('app.demo')` 为假且无 `--force` 时返回 FAILURE 且不触碰数据；[BLOCKING] Feature 测试断言非演示环境数据不变 |
| T-5-04 | Information Disclosure | 演示环境配置 | mitigate | `.env.example` 引入 APP_DEMO；演示站须 APP_ENV=production + APP_DEBUG=false（DEMO-01 deploy 负责），本 plan 提供 demo 标志与生产隔离前提 |
| T-5-SC | Tampering | 无新增 npm/composer 依赖 | accept | 本 plan 不引入任何外部包，无供应链风险 |
</threat_model>

<verification>
- `php artisan test --filter=Demo` 全绿（覆盖 DemoSeeder + DemoReset）
- `composer test` 全套绿（无回归）
- `vendor/bin/pint --test` 对新建/修改文件零警告
</verification>

<success_criteria>
- DEMO-02 落地：demo:reset 可重置数据、含生产护栏、cron 04:00 已注册
- DemoSeeder 产出可登录的 demo@example.com / demo123（DEMO-03 账号前置）
- 两个 Feature 测试文件存在且全绿（同时满足 VALIDATION.md Wave 0 要求）
</success_criteria>

<output>
Create `.planning/phases/05-demo-site/05-01-SUMMARY.md` when done
</output>
