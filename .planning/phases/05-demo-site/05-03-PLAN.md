---
phase: 05-demo-site
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - deploy.sh
  - .gitee/workflows/deploy.yml
autonomous: false
requirements: [DEMO-01]
user_setup:
  - service: gitee-pipelines
    why: "Gitee Pipelines 自动部署需要部署私钥与目标机连接信息（私密变量，不入仓库）"
    env_vars:
      - name: DEPLOY_KEY
        source: "Gitee 仓库 → 流水线 → 环境变量/密钥管理（私密类型），对应公钥放目标机 ~/.ssh/authorized_keys"
      - name: DEPLOY_HOST
        source: "固定值 118.25.27.49"
      - name: DEPLOY_USER
        source: "目标机部署用户名"
    dashboard_config:
      - task: "DNS 解析 demo.xitongapp.com → 118.25.27.49，Nginx 站点根 /data/filament-admin/public + HTTPS 证书"
        location: "DNS 服务商 + 目标服务器 Nginx"
      - task: "目标机 .env 就位：APP_ENV=production、APP_DEBUG=false、APP_DEMO=true、APP_KEY、DB、Redis 正确"
        location: "服务器 /data/filament-admin/.env"
      - task: "首次上线手动播种：cd /data/filament-admin && php artisan migrate:fresh --seed --force（建立初始演示态含 DemoSeeder）"
        location: "目标服务器 shell"
      - task: "系统 crontab 调起 Laravel scheduler：* * * * * cd /data/filament-admin && php artisan schedule:run >> /dev/null 2>&1"
        location: "目标服务器 crontab"
      - task: "确认目标机存在 composer2 命令（deploy.sh 使用 composer2 而非 composer）"
        location: "目标服务器 shell"
must_haves:
  truths:
    - "deploy.sh 语法正确（bash -n 通过）"
    - "deploy.sh 在 composer install 之后、migrate 之前构建前端资产（npm ci + npm run build）"
    - "仓库不再存在 Gitee Go 不识别的遗留冲突文件 .gitee/workflows/deploy.yml"
    - "push main 触发 Gitee 流水线 SSH 部署（由现有 .workflow/master-pipeline.yml 提供，本 plan 不改）"
  artifacts:
    - path: "deploy.sh"
      provides: "补全前端构建步骤的部署脚本"
      contains: "npm run build"
  key_links:
    - from: ".workflow/master-pipeline.yml"
      to: "deploy.sh"
      via: "SSH 远程执行 bash /data/filament-admin/deploy.sh（现有，校验勿改）"
      pattern: "deploy.sh"
---

<objective>
完成 DEMO-01 演示站自动部署的代码侧工作：补全 deploy.sh（在 composer install 之后追加前端构建 npm ci + npm run build，因 Filament 5 + Vite/Tailwind 需编译资产，per RESEARCH §0 前端构建决策），清理 Gitee Go 不识别的遗留冲突文件 .gitee/workflows/deploy.yml（GitHub Actions 语法 + tag-gated，与现有 .workflow/master-pipeline.yml 的 push-main 触发重复且冲突，per RESEARCH §1.4）。部署主体（push main + SSH）已由现有 .workflow/master-pipeline.yml 提供，校验勿改（per D-05-01/02）。

Purpose: DEMO-01 部署落地。本 plan 不是从零搭 CI——CI 流水线与 SSH 部署链路已存在；要做的是补全编译资产步骤、清理冲突文件，以及标注一系列只能人工完成的环境前置（Gitee secrets、DNS/HTTPS、首次播种、系统 crontab），故 autonomous: false。

Output: 修改后的 deploy.sh（含前端构建）+ 删除 .gitee/workflows/deploy.yml + 一个汇总人工前置步骤的 checkpoint。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-demo-site/05-RESEARCH.md
@.planning/phases/05-demo-site/05-PATTERNS.md
@.planning/phases/05-demo-site/05-VALIDATION.md
</context>

<artifacts_this_phase_produces>
本 plan 产出/变更：

- **修改**：`deploy.sh` —— 在 `composer2 install` 之后、`php artisan migrate --force` 之前插入前端构建段（`npm ci --omit=dev` + `npm run build`），保持 `echo "[deploy] ..."` 前缀风格
- **删除**：`.gitee/workflows/deploy.yml`（语法冲突遗留文件）
- **校验勿改（已就绪）**：`.workflow/master-pipeline.yml`（push main + SSH 部署）
- **人工前置（checkpoint，非代码）**：Gitee secrets（DEPLOY_KEY/HOST/USER）、DNS+HTTPS、目标机 .env（APP_DEMO=true）、首次 migrate:fresh --seed --force、系统 crontab schedule:run、composer2 存在性
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: 补全 deploy.sh 前端构建 + 清理冲突文件</name>
  <files>deploy.sh, .gitee/workflows/deploy.yml</files>
  <read_first>
    - deploy.sh（全文，现有步骤：down → git pull → composer2 install → migrate --force → 权限 → config/route/view:cache → supervisorctl restart → up）
    - .planning/phases/05-demo-site/05-PATTERNS.md 第 271-303 行（deploy.sh 修改位置与 echo 前缀格式约定）
    - .planning/phases/05-demo-site/05-RESEARCH.md §0（前端构建决策）+ §1.4（清理 .gitee/workflows/deploy.yml 的理由）
    - .gitee/workflows/deploy.yml（确认其为 GitHub Actions 语法 tag-gated 遗留文件后删除）
  </read_first>
  <action>
    在 deploy.sh 的 `COMPOSER_ALLOW_SUPERUSER=1 composer2 install ...` 行之后、`php artisan migrate --force` 之前，插入前端构建段：`echo "[deploy] 构建前端资产"` + `npm ci --omit=dev` + `npm run build`，与现有 `echo "[deploy] ..."` 中文前缀风格一致。文件顶部已有 `set -e`，构建失败将自动中断部署，无需额外处理。不改动维护模式、缓存、supervisor 等现有步骤。
    删除 `.gitee/workflows/deploy.yml`（Gitee Go 不识别 GitHub Actions 语法，且其 tag-gated 触发与 .workflow/master-pipeline.yml 的 push-main 触发冲突，per RESEARCH §1.4）。删除前先确认该文件确为遗留冲突文件（语法为 GitHub Actions `on:`/`jobs:` 风格）。
  </action>
  <verify>
    <automated>bash -n deploy.sh && grep -q "npm run build" deploy.sh && grep -q "migrate --force" deploy.sh && test ! -f .gitee/workflows/deploy.yml && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `bash -n deploy.sh` 语法通过（VALIDATION.md 任务 5-XX-07）
    - `grep -q "npm run build" deploy.sh` 命中，且该步骤位于 composer install 之后、migrate 之前（行号确认）
    - `grep -q "migrate --force" deploy.sh` 命中（现有步骤保留）
    - `.gitee/workflows/deploy.yml` 已删除（`test ! -f .gitee/workflows/deploy.yml`）
    - `.workflow/master-pipeline.yml` 未被修改
  </acceptance_criteria>
  <done>deploy.sh 含前端构建步骤且语法正确；遗留冲突文件已清理；现有 .workflow 流水线未动。</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: 人工环境前置确认（Gitee secrets / DNS / HTTPS / 首次播种 / crontab）</name>
  <action>以下为只能人工完成的环境前置（Claude 无法代办），由用户在 Gitee 控制台与目标服务器逐项配置并确认；执行器在此任务暂停，等待用户完成后回报。</action>
  <what-built>deploy.sh 已补全前端构建，遗留冲突文件已清理，现有 .workflow/master-pipeline.yml（push main + SSH 部署）就绪。</what-built>
  <how-to-verify>
    以下为只能人工完成的环境前置（Claude 无法代办），逐项完成并确认：
    1. **Gitee 私密变量**：在 Gitee 仓库 → 流水线 → 环境变量/密钥管理 配置 `DEPLOY_KEY`（部署私钥，私密类型）、`DEPLOY_HOST=118.25.27.49`、`DEPLOY_USER`；对应公钥已放目标机 `~/.ssh/authorized_keys`。
    2. **DNS + HTTPS**：demo.xitongapp.com 解析到 118.25.27.49；Nginx 站点根指向 `/data/filament-admin/public`，HTTPS 证书已配置。
    3. **目标机 .env**：`/data/filament-admin/.env` 中 APP_ENV=production、APP_DEBUG=false、APP_DEMO=true、APP_KEY 已生成、DB/Redis 连接正确（注意 config:cache 后改 .env 须重跑 config:cache）。
    4. **首次播种**：在目标机执行 `cd /data/filament-admin && php artisan migrate:fresh --seed --force` 建立初始演示态（含 DemoSeeder 创建 demo@example.com / demo123，依赖 APP_DEMO=true）。
    5. **系统 crontab**：目标机 `crontab -l` 含 `* * * * * cd /data/filament-admin && php artisan schedule:run >> /dev/null 2>&1`（否则 demo:reset 与日志清理 cron 均不执行）。
    6. **composer2 存在性**：目标机 `which composer2` 有结果（deploy.sh 用 composer2）。
    7. **触发验证**：push main 后观察 Gitee 流水线绿；`curl -I https://demo.xitongapp.com` 返回 200；浏览器用 demo@example.com / demo123 登录进首页。
  </how-to-verify>
  <verify>
    <human-check>用户确认 7 项人工前置全部完成：Gitee secrets 已配（私密类型）、DNS+HTTPS 就绪、目标机 .env 含 APP_DEMO=true 且 APP_DEBUG=false、首次播种完成、系统 crontab 含 schedule:run、composer2 存在、push main 后 curl 返回 200 且 demo 账号可登录。</human-check>
  </verify>
  <done>用户回报 7 项环境前置全部完成，演示站可访问且 demo 账号可登录。</done>
  <resume-signal>输入「已完成」或描述遇到的部署问题</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Gitee 流水线 → 目标服务器 | 通过 SSH 私钥跨网络部署，私钥泄漏即服务器沦陷 |
| 演示站 → 公网访问者 | 调试信息/堆栈可能向公网泄漏 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-5-03 | Information Disclosure | 部署私钥/secrets | mitigate | DEPLOY_KEY 等以 Gitee「私密」类型变量存储，绝不入仓库；checkpoint 显式要求私密类型配置 |
| T-5-04 | Information Disclosure | 演示站公网暴露调试信息 | mitigate | checkpoint 要求目标机 .env 设 APP_ENV=production + APP_DEBUG=false，避免堆栈泄漏 |
| T-5-SC | Tampering | npm ci 安装前端依赖 | accept | 使用项目既有 package-lock.json 锁定版本，无新增包引入；npm ci 严格按 lock 安装，无供应链新增面 |
</threat_model>

<verification>
- `bash -n deploy.sh` 通过
- `grep` 断言前端构建步骤就位、冲突文件已删
- 人工 checkpoint 确认 Gitee secrets / DNS / HTTPS / 首次播种 / crontab / composer2 全部就绪（VALIDATION.md Manual-Only Verifications）
</verification>

<success_criteria>
- DEMO-01 代码侧：deploy.sh 含前端构建、语法正确、冲突文件已清
- DEMO-01 环境侧（人工）：push main → Gitee 流水线绿 → curl 200 → demo 账号可登录
</success_criteria>

<output>
Create `.planning/phases/05-demo-site/05-03-SUMMARY.md` when done
</output>
