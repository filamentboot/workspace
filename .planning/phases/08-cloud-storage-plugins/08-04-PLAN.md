---
phase: 08-cloud-storage-plugins
plan: 04
type: execute
wave: 2
depends_on: ["08-01", "08-02"]
files_modified:
  - composer.json
  - config/settings.php
  - tests/Feature/CloudStoragePluginsIntegrationTest.php
autonomous: true
requirements: [CLOUD-01, CLOUD-02]
must_haves:
  truths:
    - "根 composer.json repositories 含 filament-admin-oss 与 filament-admin-cos 两个 path 入口"
    - "config/settings.php 的 settings 数组注册 OssSettings 与 CosSettings"
    - "OSS/COS settings 迁移路径被 spatie/laravel-settings 加载（migrations_paths 含两个包的 database/settings）"
    - "composer 安装后 OSS/COS 包通过 Laravel 自动发现注册其 ServiceProvider"
  artifacts:
    - path: "composer.json"
      provides: "monorepo 本地包路径注册（OSS + COS）"
      contains: "packages/filament-admin-oss"
    - path: "config/settings.php"
      provides: "OSS/COS Settings 类注册 + 迁移路径"
      contains: "OssSettings"
    - path: "tests/Feature/CloudStoragePluginsIntegrationTest.php"
      provides: "演示项目层集成验证（包加载 + 磁盘注入 + settings 注册）"
      contains: "filesystems.disks"
  key_links:
    - from: "composer.json"
      to: "packages/filament-admin-oss & packages/filament-admin-cos"
      via: "repositories path symlink 入口"
      pattern: "filament-admin-(oss|cos)"
    - from: "config/settings.php"
      to: "LaravelStack\\FilamentAdminOss\\Settings\\OssSettings & CosSettings"
      via: "settings[] 数组注册 + migrations_paths 追加包内 settings 目录"
      pattern: "OssSettings|CosSettings"
---

<objective>
将两个云存储插件包接入演示项目（monorepo）：根 composer.json 注册本地包路径、`config/settings.php` 注册 OSS/COS Settings 类与迁移路径、安装依赖并验证插件可被 Laravel 自动发现且磁盘配置可注入。

Purpose: 满足 CLOUD-01/02 集成闭环 — 让 OSS/COS 包在演示项目中真正可装、可迁移、可被 plugin:scan 发现、磁盘可注入（Pitfall 4：新增包必须更新根 repositories；config/settings.php settings[] + migrations_paths 必须包含插件 Settings）。
Output: 根 composer.json + config/settings.php 更新 + 集成测试 + composer install 验证两包加载成功。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cloud-storage-plugins/08-RESEARCH.md
@.planning/phases/08-cloud-storage-plugins/08-PATTERNS.md
@.planning/phases/08-cloud-storage-plugins/08-CONTEXT.md
</context>

## Artifacts this phase produces

本 Plan 创建以下全新符号（供 review 阶段排除 drift 检测）：

- 测试类：`Tests\Feature\CloudStoragePluginsIntegrationTest`
- 修改（非新建）：根 `composer.json` 的 `repositories` 数组（追加 2 个 path 入口）+ `require` 追加 `laravelstack/filament-admin-oss @dev`、`laravelstack/filament-admin-cos @dev`
- 修改（非新建）：`config/settings.php` 的 `settings[]` 数组（追加 OssSettings/CosSettings）与 `migrations_paths`（追加两个包的 database/settings 目录）

<tasks>

<task type="auto">
  <name>Task 1: 根 composer.json 注册本地包路径并安装依赖</name>
  <files>
    composer.json
  </files>
  <read_first>
    - composer.json（根，现有 repositories 仅含 packages/filament-admin；require 段含 laravelstack/filament-admin @dev）
    - .planning/phases/08-cloud-storage-plugins/08-RESEARCH.md（Code Examples Monorepo 根 composer.json 新增包路径；Pitfall 4）
    - .planning/phases/08-cloud-storage-plugins/08-PATTERNS.md（根 composer.json repositories 段修改后结构）
  </read_first>
  <action>
    修改根 `composer.json`：
    - `repositories` 数组追加两个 path 入口：`{"type": "path", "url": "packages/filament-admin-oss", "options": {"symlink": true}}` 与 `{"type": "path", "url": "packages/filament-admin-cos", "options": {"symlink": true}}`（保留现有 packages/filament-admin 入口）。
    - `require` 段追加 `"laravelstack/filament-admin-oss": "@dev"` 与 `"laravelstack/filament-admin-cos": "@dev"`（与现有 `laravelstack/filament-admin: @dev` 同风格，sort-packages 会排序）。
    - 然后运行 `composer update laravelstack/filament-admin-oss laravelstack/filament-admin-cos --no-interaction` 安装两个本地包及其传递依赖（iidestiny/flysystem-oss、overtrue/laravel-filesystem-cos 及底层 SDK）。
    注意：本任务会触发 composer 网络安装；若 iidestiny/flysystem-oss 或 overtrue/laravel-filesystem-cos 安装失败（网络/版本冲突），记录到 SUMMARY 并停止，不得降级跳过——这两个包是 CLOUD-01/02 的硬依赖。
  </action>
  <verify>
    <automated>cd /home/john/projects/personal/filament-admin && php -r "\$c=json_decode(file_get_contents('composer.json'),true); \$urls=array_column(\$c['repositories'],'url'); exit((in_array('packages/filament-admin-oss',\$urls) && in_array('packages/filament-admin-cos',\$urls) && isset(\$c['require']['laravelstack/filament-admin-oss']) && isset(\$c['require']['laravelstack/filament-admin-cos']))?0:1);" && composer show laravelstack/filament-admin-oss >/dev/null 2>&1 && composer show laravelstack/filament-admin-cos >/dev/null 2>&1</automated>
  </verify>
  <acceptance_criteria>
    - 根 `composer.json` `repositories` 含 url `packages/filament-admin-oss` 与 `packages/filament-admin-cos`
    - 根 `composer.json` `require` 含 `laravelstack/filament-admin-oss` 与 `laravelstack/filament-admin-cos`
    - `composer show laravelstack/filament-admin-oss` 与 `composer show laravelstack/filament-admin-cos` 均退出码 0（包已安装）
    - `iidestiny/flysystem-oss` 与 `overtrue/laravel-filesystem-cos` 出现在 vendor（`composer show iidestiny/flysystem-oss` 退出码 0）
  </acceptance_criteria>
  <done>两个插件包及其适配器依赖在演示项目中安装成功，可被 Laravel 自动发现</done>
</task>

<task type="auto">
  <name>Task 2: config/settings.php 注册 OSS/COS Settings 类与迁移路径 + 集成测试</name>
  <files>
    config/settings.php,
    tests/Feature/CloudStoragePluginsIntegrationTest.php
  </files>
  <read_first>
    - config/settings.php（settings[] 数组 + migrations_paths + auto_discover_settings 现状）
    - .planning/phases/08-cloud-storage-plugins/08-RESEARCH.md（Open Question 3：Settings 类注册方式；Validation Architecture Phase gate）
    - packages/filament-admin-oss/src/Settings/OssSettings.php（08-01 产物，确认 FQCN）
    - packages/filament-admin-cos/src/Settings/CosSettings.php（08-02 产物，确认 FQCN）
    - tests/Feature 现有用例（演示项目 Feature 测试引导方式，确认 TestCase 基类）
  </read_first>
  <action>
    修改 `config/settings.php`：
    - 顶部 `use` 追加 `LaravelStack\FilamentAdminOss\Settings\OssSettings;` 与 `LaravelStack\FilamentAdminCos\Settings\CosSettings;`。
    - `settings` 数组追加 `OssSettings::class` 与 `CosSettings::class`（在现有 4 个 Settings 之后）。
    - `migrations_paths` 数组追加两个包的 settings 迁移目录：`base_path('packages/filament-admin-oss/database/settings')` 与 `base_path('packages/filament-admin-cos/database/settings')`（保留现有 `database_path('settings')`）。这样 `php artisan migrate` 会运行两个包的 settings 迁移，初始化 oss/cos 分组默认值。
    创建 `tests/Feature/CloudStoragePluginsIntegrationTest.php`（namespace `Tests\Feature`，extends 演示项目 Feature TestCase 基类）：
    - test_oss_and_cos_packages_are_discovered：断言 `LaravelStack\FilamentAdminOss\OssServiceProvider` 与 `LaravelStack\FilamentAdminCos\CosServiceProvider` 类存在（class_exists），且 `OssPlugin`/`CosPlugin` 实现 `Filament\Contracts\Plugin`。
    - test_settings_classes_registered：断言 `config('settings.settings')` 数组含 OssSettings::class 与 CosSettings::class。
    - test_oss_disk_injected_when_credentials_present：在测试中 `app(OssSettings::class)` 设置 access_key_id 与 bucket 非空并 save（或用 config 模拟），重新 boot OssServiceProvider 后断言 `config('filesystems.disks.oss.driver')` === 'oss'。若 settings 持久化在测试中不便，可改为直接调用 provider 的注入逻辑或 mock OssSettings，核心断言：凭证完整时 oss 磁盘配置被注入。
    - test_app_boots_without_cloud_credentials：断言在无 oss/cos 凭证（默认空）时 `php artisan` 级别应用 boot 不抛异常（用 `$this->artisan('config:clear')->assertExitCode(0)` 或断言容器解析 OssServiceProvider 不抛 Throwable）。
  </action>
  <verify>
    <automated>cd /home/john/projects/personal/filament-admin && php artisan config:clear && php artisan test --filter=CloudStoragePluginsIntegrationTest</automated>
  </verify>
  <acceptance_criteria>
    - `config/settings.php` `settings` 数组含 `OssSettings::class` 与 `CosSettings::class`（grep `OssSettings` 与 `CosSettings` 命中）
    - `config/settings.php` `migrations_paths` 含 `packages/filament-admin-oss/database/settings` 与 `packages/filament-admin-cos/database/settings`
    - `CloudStoragePluginsIntegrationTest` 含至少 4 个测试方法，覆盖：包发现、settings 注册、磁盘注入、无凭证不崩溃
    - `php artisan test --filter=CloudStoragePluginsIntegrationTest` 退出码 0
  </acceptance_criteria>
  <done>OSS/COS Settings 类与迁移路径已注册，集成测试验证包发现 + 磁盘注入 + 安全降级，全绿</done>
</task>

<task type="auto">
  <name>Task 3: 运行 settings 迁移并验证 plugin:scan 发现两个插件</name>
  <files>
    tests/Feature/CloudStoragePluginsIntegrationTest.php
  </files>
  <read_first>
    - app/Services/PluginManager.php（syncFromInstalled / plugin:scan 读取 extra.filament-admin 逻辑，确认 source=official_listed 被信任）
    - app/Console/Commands 或 routes/console.php（plugin:scan 命令注册位置，STATE.md 记录用 Artisan::registerCommand）
    - .planning/phases/08-cloud-storage-plugins/08-RESEARCH.md（Validation Architecture Phase gate：plugin:scan 能扫到两个插件）
    - .planning/phases/08-cloud-storage-plugins/08-VALIDATION.md（Phase gate 验收项）
  </read_first>
  <action>
    运行 `php artisan migrate --force`（执行 OSS/COS settings 迁移，初始化 oss/cos 分组默认值；幂等，已跑过的不重复，Pitfall 5）。
    运行 `php artisan plugin:scan`（若命令名不同，从 PluginManager/routes/console.php 确认实际命令签名），使 syncFromInstalled 将两个包写入 plugins 表。
    在 `CloudStoragePluginsIntegrationTest` 追加 test_plugin_scan_discovers_cloud_plugins：调用 `app(\App\Services\PluginManager::class)->syncFromInstalled()`（或对应方法），断言返回/数据库结果含 slug `filament-admin-oss` 与 `filament-admin-cos`，且其 plugin_class/service_provider 字段非空、compatibility 含 laravel/framework ^13.0。
    若 plugin:scan 命令在测试环境因依赖未装而无法运行，则该断言直接调用 PluginManager 的扫描方法（构造已安装包列表）验证 extra.filament-admin 解析正确。
  </action>
  <verify>
    <automated>cd /home/john/projects/personal/filament-admin && php artisan migrate --force >/dev/null 2>&1; php artisan test --filter='CloudStoragePluginsIntegrationTest::test_plugin_scan_discovers_cloud_plugins'</automated>
  </verify>
  <acceptance_criteria>
    - `php artisan migrate --force` 退出码 0（OSS/COS settings 迁移执行无错）
    - `test_plugin_scan_discovers_cloud_plugins` 断言扫描结果含 slug `filament-admin-oss` 与 `filament-admin-cos`
    - 两个插件记录的 `service_provider` 与 `plugin_class` 字段非空
    - `php artisan test --filter='CloudStoragePluginsIntegrationTest::test_plugin_scan_discovers_cloud_plugins'` 退出码 0
  </acceptance_criteria>
  <done>settings 迁移执行成功，plugin:scan 发现两个云存储插件并写入正确契约元信息</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin:scan → composer 已安装包 extra | 读取并信任 extra.filament-admin 元信息写入 plugins 表 |
| migrate → settings 表 | 执行包内 SettingsMigration 初始化分组默认值 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Spoofing | plugin:scan 信任 extra.filament-admin.source | accept | PluginManager 仅对 source ∈ {official_trusted, official_listed} 信任执行；两包 source=official_listed 由本项目自发布 |
| T-08-08 | Tampering | settings 迁移重复执行（add 非幂等，Pitfall 5） | mitigate | 仅用 Laravel migration 记录保障幂等（migrate --force 跳过已跑文件）；不在 Seeder 重复调用 migrator->add |
| T-08-09 | Elevation of Privilege | OSS/COS SettingsPage 访问控制 | mitigate | SettingsPage 继承 Filament 权限控制，super_admin Gate::before 放行/普通用户走 Policy（依赖 ASVS V4，主包既有机制） |
| T-08-SC | Tampering | composer update 拉取 OSS/COS 适配器及传递依赖 | accept | RESEARCH Package Legitimacy Audit 已通过 Packagist 验证全部传递依赖（aliyuncs/oss-sdk-php 官方、overtrue/qcloud-cos-client 等）；非 [ASSUMED]/[SUS] |
</threat_model>

<verification>
- `php artisan test --filter=CloudStoragePluginsIntegrationTest` 全绿
- `composer show laravelstack/filament-admin-oss` 与 `...-cos` 退出码 0
- `php artisan migrate --force` 退出码 0
- 全量 `php artisan test` 无回归（Wave 合并 gate）
</verification>

<success_criteria>
- 两个插件包在演示项目安装、自动发现、settings 迁移执行成功
- config/settings.php 注册 OSS/COS Settings 与迁移路径
- plugin:scan 发现两个插件并写入正确契约元信息
- 无凭证时应用正常启动（不崩溃）
- 集成测试退出码 0
</success_criteria>

<output>
Create `.planning/phases/08-cloud-storage-plugins/08-04-SUMMARY.md` when done
</output>
