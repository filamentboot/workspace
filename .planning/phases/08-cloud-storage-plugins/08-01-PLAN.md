---
phase: 08-cloud-storage-plugins
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/filament-admin-oss/composer.json
  - packages/filament-admin-oss/phpunit.xml.dist
  - packages/filament-admin-oss/src/Settings/OssSettings.php
  - packages/filament-admin-oss/src/Filament/Pages/OssSettingsPage.php
  - packages/filament-admin-oss/src/OssPlugin.php
  - packages/filament-admin-oss/src/OssServiceProvider.php
  - packages/filament-admin-oss/database/settings/2026_06_12_000001_create_oss_settings.php
  - packages/filament-admin-oss/tests/Unit/OssPackageMetadataTest.php
  - packages/filament-admin-oss/tests/Unit/OssSettingsTest.php
  - packages/filament-admin-oss/tests/Unit/OssServiceProviderTest.php
autonomous: true
requirements: [CLOUD-01]
must_haves:
  truths:
    - "OSS 插件包 composer.json 含 extra.filament-admin 契约块，plugin:scan 可识别"
    - "OssSettings 声明 access_key_secret 为加密字段"
    - "OssServiceProvider boot() 注册 'oss' Flysystem 驱动且 settings 表缺失时不抛异常"
    - "OssServiceProvider boot() 仅在插件 is_enabled=true 时注入磁盘配置（插件禁用后磁盘不注入）"
    - "OssSettingsPage 提供 AccessKeyId/Secret/Bucket/Endpoint/Region 五个表单字段"
  artifacts:
    - path: "packages/filament-admin-oss/composer.json"
      provides: "包元信息 + extra.filament-admin 插件契约 + iidestiny/flysystem-oss 依赖"
      contains: "laravelstack/filament-admin-oss"
    - path: "packages/filament-admin-oss/src/OssServiceProvider.php"
      provides: "Storage::extend('oss') 注册 + 插件启用时 Settings 注入 filesystems.disks.oss"
      contains: "Storage::extend"
    - path: "packages/filament-admin-oss/src/Settings/OssSettings.php"
      provides: "OSS 凭证 Settings 类（access_key_secret 加密）"
      contains: "encrypted"
    - path: "packages/filament-admin-oss/src/Filament/Pages/OssSettingsPage.php"
      provides: "OSS 后台配置页"
      contains: "SettingsPage"
    - path: "packages/filament-admin-oss/database/settings/2026_06_12_000001_create_oss_settings.php"
      provides: "oss settings 分组初始化（addEncrypted）"
      contains: "addEncrypted"
  key_links:
    - from: "packages/filament-admin-oss/src/OssServiceProvider.php"
      to: "packages/filament-admin-oss/src/Settings/OssSettings.php"
      via: "app(OssSettings::class) 读取凭证注入 config(filesystems.disks.oss)"
      pattern: "OssSettings::class"
    - from: "packages/filament-admin-oss/src/OssServiceProvider.php"
      to: "plugins 表 is_enabled 字段"
      via: "boot() 注入磁盘前查询 plugins.is_enabled（try/catch 守卫，D-08-10）"
      pattern: "is_enabled"
    - from: "packages/filament-admin-oss/composer.json"
      to: "packages/filament-admin-oss/src/OssServiceProvider.php"
      via: "extra.laravel.providers 声明，Laravel 自动发现"
      pattern: "OssServiceProvider"
---

<objective>
构建阿里云 OSS Filament 插件包 `laravelstack/filament-admin-oss`，提供凭证后台配置（加密存储）、Flysystem `oss` 磁盘驱动动态注册、符合 Phase 6 插件市场契约。

Purpose: 满足 CLOUD-01 — 让后台媒体管理可选阿里云 OSS 作为存储磁盘，凭证由超管在后台配置而非运维改 .env。
Output: `packages/filament-admin-oss/` 完整插件包（composer.json + Settings + SettingsPage + Plugin + ServiceProvider + SettingsMigration + 3 个单元测试）。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-cloud-storage-plugins/08-RESEARCH.md
@.planning/phases/08-cloud-storage-plugins/08-PATTERNS.md
@.planning/phases/08-cloud-storage-plugins/08-CONTEXT.md
</context>

## Artifacts this phase produces

本 Plan 创建以下全新符号（供 review 阶段排除 drift 检测）：

- 命名空间：`LaravelStack\FilamentAdminOss\`（PSR-4 根 `src/`），`LaravelStack\FilamentAdminOss\Tests\`（`tests/`）
- 类：`OssPlugin`、`OssServiceProvider`、`OssSettings`、`OssSettingsPage`
- 测试类：`OssPackageMetadataTest`、`OssSettingsTest`、`OssServiceProviderTest`
- Flysystem 磁盘驱动名：`oss`（通过 `Storage::extend('oss', ...)`）
- Settings 分组：`oss`；字段 `access_key_id`、`access_key_secret`（加密）、`bucket`、`endpoint`、`region`
- 配置键：`filesystems.disks.oss`
- composer 包名：`laravelstack/filament-admin-oss`
- extra.filament-admin slug：`filament-admin-oss`
- 外部新依赖：`iidestiny/flysystem-oss ^4.8`（含底层 `aliyuncs/oss-sdk-php`）

<tasks>

<task type="auto">
  <name>Task 1: 创建 OSS 包骨架（composer.json + phpunit + 插件市场契约）</name>
  <files>
    packages/filament-admin-oss/composer.json,
    packages/filament-admin-oss/phpunit.xml.dist
  </files>
  <read_first>
    - packages/filament-admin/composer.json（主包字段范本：name/type/license/keywords/authors/support/autoload；注意主包 require-dev 中 orchestra/testbench 的实际版本约束，新包对齐之）
    - packages/filament-admin/phpunit.xml.dist（testsuite 结构范本）
    - .planning/phases/08-cloud-storage-plugins/08-PATTERNS.md（extra.filament-admin 契约 + require 依赖声明段）
    - app/Services/PluginManager.php（syncFromInstalled 读取的 extra.filament-admin 字段：slug/name/type/source/plugin_class/service_provider/description/requires/compatibility）
  </read_first>
  <action>
    创建 `packages/filament-admin-oss/composer.json`：
    - `name` = `laravelstack/filament-admin-oss`，`type` = `library`，`license` = `MIT`，`description` 描述阿里云 OSS 存储插件。
    - `require`：`php` ^8.3、`iidestiny/flysystem-oss` ^4.8、`spatie/laravel-settings` ^3.9、`filament/spatie-laravel-settings-plugin` ^5.6、`laravelstack/filament-admin` ^0.5。
    - `require-dev`：`orchestra/testbench` ^10.0（对应 Laravel 13；^11.0 对应 Laravel 14，本项目锁定 Laravel ^13.8，必须用 ^10.0）、`phpunit/phpunit` ^12.5.12、`larastan/larastan` ^3.0、`laravel/pint` ^1.27。
    - `autoload.psr-4`：`LaravelStack\\FilamentAdminOss\\` → `src/`。
    - `autoload-dev.psr-4`：`LaravelStack\\FilamentAdminOss\\Tests\\` → `tests/`。
    - `extra.laravel.providers` = `["LaravelStack\\FilamentAdminOss\\OssServiceProvider"]`。
    - `extra.filament-admin` 块声明：`slug`=`filament-admin-oss`、`name`=`阿里云 OSS 存储`、`type`=`package`、`source`=`official_listed`、`plugin_class`=`LaravelStack\\FilamentAdminOss\\OssPlugin`、`service_provider`=`LaravelStack\\FilamentAdminOss\\OssServiceProvider`、`description`（OSS 存储驱动说明）、`requires`=`[]`、`compatibility`={`laravel/framework`:`^13.0`,`filament/filament`:`^5.0`}。
    - `config.sort-packages`=true、`config.allow-plugins.pestphp/pest-plugin`=true（若用 pest，否则可省 pest 项）。
    - `minimum-stability`=`stable`，`prefer-stable`=true。
    创建 `packages/filament-admin-oss/phpunit.xml.dist`：复制主包 phpunit.xml.dist 结构（bootstrap=vendor/autoload.php，testsuite name="Unit" directory=tests/Unit）。注意：包独立运行测试时 bootstrap 指向各自 vendor，但本 monorepo 中包依赖通过根 vendor 提供，执行命令统一用根 `php artisan test --filter`（见 verify）。
  </action>
  <verify>
    <automated>cd /home/john/projects/personal/filament-admin && php -r "\$c=json_decode(file_get_contents('packages/filament-admin-oss/composer.json'),true); exit((\$c['name']==='laravelstack/filament-admin-oss' && \$c['extra']['filament-admin']['slug']==='filament-admin-oss' && in_array('LaravelStack\\\\FilamentAdminOss\\\\OssServiceProvider',\$c['extra']['laravel']['providers']) && isset(\$c['require']['iidestiny/flysystem-oss']) && str_starts_with(\$c['require-dev']['orchestra/testbench'],'^10'))?0:1);"</automated>
  </verify>
  <acceptance_criteria>
    - `packages/filament-admin-oss/composer.json` 是合法 JSON 且 `name` 严格等于 `laravelstack/filament-admin-oss`
    - `extra.filament-admin.slug` 等于 `filament-admin-oss`，`plugin_class` 与 `service_provider` 均为 `LaravelStack\FilamentAdminOss\*` 全限定类名
    - `extra.laravel.providers` 数组包含 `LaravelStack\FilamentAdminOss\OssServiceProvider`
    - `require` 含 `iidestiny/flysystem-oss`、`spatie/laravel-settings`、`filament/spatie-laravel-settings-plugin`、`laravelstack/filament-admin`
    - `require-dev.orchestra/testbench` 约束以 `^10` 开头（对应 Laravel 13，不得为 ^11.0）
    - `phpunit.xml.dist` 含 `<testsuite name="Unit">` 指向 `tests/Unit`
  </acceptance_criteria>
  <done>composer.json 含完整 extra.filament-admin 契约与 OSS 适配器依赖，testbench 锁 ^10.0；phpunit 配置就位</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OssSettings 加密凭证类 + SettingsMigration + 测试骨架</name>
  <files>
    packages/filament-admin-oss/src/Settings/OssSettings.php,
    packages/filament-admin-oss/database/settings/2026_06_12_000001_create_oss_settings.php,
    packages/filament-admin-oss/tests/Unit/OssPackageMetadataTest.php,
    packages/filament-admin-oss/tests/Unit/OssSettingsTest.php
  </files>
  <read_first>
    - packages/filament-admin/src/Settings/SecuritySettings.php（Settings 类结构 + group() 范本）
    - packages/filament-admin/src/Settings/UploadSettings.php（字段 PHPDoc 风格范本）
    - database/settings/2026_06_03_000001_create_initial_settings.php（SettingsMigration up() + migrator->add 范本）
    - .planning/phases/08-cloud-storage-plugins/08-RESEARCH.md（Pattern 2 + Pitfall 3：addEncrypted 必须用于加密字段）
    - packages/filament-admin/tests/Unit/PackageMetadataTest.php（包元信息测试断言风格）
  </read_first>
  <behavior>
    - OssSettingsTest::test_encrypted_fields_declared：`OssSettings::encrypted()` 返回数组包含 `access_key_secret`
    - OssSettingsTest::test_group_name：`OssSettings::group()` 返回 `'oss'`
    - OssPackageMetadataTest::test_extra_filament_admin_has_required_fields：composer.json `extra.filament-admin` 含 slug/name/plugin_class/service_provider 且 slug===`filament-admin-oss`
    - OssPackageMetadataTest::test_laravel_provider_is_declared：`extra.laravel.providers` 含 `LaravelStack\FilamentAdminOss\OssServiceProvider`
  </behavior>
  <action>
    创建 `OssSettings`（namespace `LaravelStack\FilamentAdminOss\Settings`，extends `Spatie\LaravelSettings\Settings`）：
    - public 属性：`access_key_id`=''、`access_key_secret`=''、`bucket`=''、`endpoint`='oss-cn-hangzhou.aliyuncs.com'、`region`='cn-hangzhou'（均 string 类型，中文 PHPDoc 注释）。
    - `public static function group(): string` 返回 `'oss'`。
    - `public static function encrypted(): array` 返回 `['access_key_secret']`（PHPDoc `@return list<string>`）。
    创建 SettingsMigration `2026_06_12_000001_create_oss_settings.php`（匿名类 extends `Spatie\LaravelSettings\Migrations\SettingsMigration`，up() 方法）：
    - `$this->migrator->add('oss.access_key_id', '')`
    - `$this->migrator->addEncrypted('oss.access_key_secret', '')` ← 加密字段必须用 addEncrypted（Pitfall 3）
    - `$this->migrator->add('oss.bucket', '')`
    - `$this->migrator->add('oss.endpoint', 'oss-cn-hangzhou.aliyuncs.com')`
    - `$this->migrator->add('oss.region', 'cn-hangzhou')`
    创建 `OssSettingsTest`（namespace `LaravelStack\FilamentAdminOss\Tests\Unit`，extends `PHPUnit\Framework\TestCase`）：实现上述 behavior 两个方法，直接调用 `OssSettings::encrypted()` 与 `OssSettings::group()` 静态方法断言（纯 TestCase，不引导 Laravel app）。
    创建 `OssPackageMetadataTest`（同 namespace，extends `PHPUnit\Framework\TestCase`）：setUp 读取 `__DIR__.'/../../composer.json'`，实现上述 behavior 两个方法。
  </action>
  <verify>
    <automated>cd /home/john/projects/personal/filament-admin && php artisan test --filter='OssSettingsTest|OssPackageMetadataTest'</automated>
  </verify>
  <acceptance_criteria>
    - `OssSettings.php` 含 `public static function encrypted(): array` 返回值包含字符串 `access_key_secret`
    - `OssSettings::group()` 返回 `oss`
    - SettingsMigration 文件对 `oss.access_key_secret` 使用 `addEncrypted`（grep `addEncrypted` 命中），其余 4 字段使用 `add`
    - `php artisan test --filter='OssSettingsTest|OssPackageMetadataTest'` 退出码 0
  </acceptance_criteria>
  <done>OssSettings 声明加密字段，迁移用 addEncrypted 初始化，两个测试绿</done>
</task>

<task type="auto">
  <name>Task 3: OssPlugin + OssServiceProvider（Storage::extend 注册 + 启用状态守卫）+ ServiceProvider 测试</name>
  <files>
    packages/filament-admin-oss/src/OssPlugin.php,
    packages/filament-admin-oss/src/OssServiceProvider.php,
    packages/filament-admin-oss/src/Filament/Pages/OssSettingsPage.php,
    packages/filament-admin-oss/tests/Unit/OssServiceProviderTest.php
  </files>
  <read_first>
    - packages/filament-admin/src/FilamentAdminPlugin.php（Plugin 契约：make()/getId()/register()/boot() 范本）
    - packages/filament-admin/src/FilamentAdminServiceProvider.php（ServiceProvider register()/boot()/loadMigrationsFrom 结构 + runningInConsole 守卫）
    - packages/filament-admin/src/Filament/Pages/Settings/UploadSettingsPage.php（SettingsPage 子类完整结构 + navigationGroup '系统配置' + form(Schema) 范本）
    - app/Providers/Filament/AdminPanelProvider.php（registerEnabledPlugins：plugins 表 is_enabled 查询 + try/catch + Cache::remember 守卫范本，约第 120-138 行）
    - .planning/phases/08-cloud-storage-plugins/08-RESEARCH.md（Pattern 1 Storage::extend OSS 完整代码 + Code Examples OSS ServiceProvider；Pitfall 2/5 try/catch 守卫）
    - .planning/phases/08-cloud-storage-plugins/08-PATTERNS.md（OssServiceProvider boot() 完整模式 + OssSettingsPage 字段定义）
  </read_first>
  <action>
    创建 `OssPlugin`（namespace `LaravelStack\FilamentAdminOss`，implements `Filament\Contracts\Plugin`）：
    - `public static function make(): static { return app(static::class); }`
    - `public function getId(): string { return 'filament-admin-oss'; }`（必须与 extra.filament-admin.slug 一致）
    - `register(Panel $panel): void` 调用 `$panel->pages([OssSettingsPage::class])`
    - `boot(Panel $panel): void` 空实现（注释说明 Storage::extend 在 ServiceProvider::boot 执行）
    创建 `OssSettingsPage`（namespace `LaravelStack\FilamentAdminOss\Filament\Pages`，extends `Filament\Pages\SettingsPage`）：
    - `protected static string $settings = OssSettings::class;`
    - `$navigationIcon`='heroicon-o-cloud-arrow-up'、`$navigationLabel`='OSS 配置'、`$navigationGroup`='系统配置'、`$navigationSort`=10、`$slug`='settings/oss'（属性签名照抄 UploadSettingsPage 的 BackedEnum/UnitEnum 联合类型）。
    - `form(Schema $schema): Schema` 返回 5 个 TextInput：access_key_id（label AccessKeyId,required）、access_key_secret（label AccessKeySecret,password,revealable,required）、bucket（label Bucket 名称,required）、endpoint（label Endpoint,required,helperText 示例 oss-cn-hangzhou.aliyuncs.com）、region（label Region,helperText 示例 cn-hangzhou）。
    创建 `OssServiceProvider`（namespace `LaravelStack\FilamentAdminOss`，extends `Illuminate\Support\ServiceProvider`）：
    - `boot()`：调用 `Storage::extend('oss', function (Application $app, array $config) {...})`，闭包内构造 `Iidestiny\Flysystem\Oss\OssAdapter`（参数 access_key/secret_key/endpoint/bucket/isCName/root）并返回 `Illuminate\Filesystem\FilesystemAdapter`（包 `League\Flysystem\Filesystem`），照抄 RESEARCH Pattern 1。`Storage::extend` 注册驱动名本身无副作用，始终执行。
    - 磁盘配置注入受「插件启用状态」与「凭证完整性」双重守卫（D-08-10：插件禁用后磁盘不注入）。在 boot() 内用单层 `try { ... } catch (\Throwable) {}` 包裹下述逻辑（try/catch 既防 settings 表未迁移 Pitfall 2，也防 plugins 表首次 migrate 前不存在）：
      1. 先检查插件启用状态：查询 `plugins` 表 `where('slug', 'filament-admin-oss')->where('is_enabled', true)->exists()`（用 `Illuminate\Support\Facades\DB::table('plugins')`，不依赖演示项目的 `App\Models\Plugin`，因为本包是 library 不能反向依赖宿主）；若未启用（不存在该启用记录），直接 return，不注入磁盘。注：plugins 表查询可加 `app('cache')->remember('plugins.enabled_list', 30, ...)` 或直接查（boot 仅在请求/命令启动跑一次，简单 exists 查询可接受），与 AdminPanelProvider::registerEnabledPlugins 的 is_enabled 语义一致。
      2. 再读凭证：`$settings = app(OssSettings::class); if (! empty($settings->access_key_id) && ! empty($settings->bucket)) { config(['filesystems.disks.oss' => [...]]); }` 注入磁盘配置（driver=oss/access_key/secret_key/endpoint/bucket/region/isCName=false）。
      整个块在 `catch (\Throwable) {}` 下静默降级，settings 表或 plugins 表缺失时不阻断应用启动。
    - `registerMigrations()` 受保护方法：`if ($this->app->runningInConsole()) { $this->loadMigrationsFrom(__DIR__.'/../database/settings'); }`，boot() 末尾调用。
    创建 `OssServiceProviderTest`（namespace `LaravelStack\FilamentAdminOss\Tests\Unit`）：使用 Orchestra Testbench（extends `Orchestra\Testbench\TestCase`，getPackageProviders 返回 [OssServiceProvider::class]）。断言：(a) boot 后 `Storage::extend` 已注册 'oss' 驱动名可被 `array_key_exists` 或调用 `Storage::disk('oss')` 时不报 driver-not-found（注入伪 config 后）；(b) settings 表/plugins 表不存在时 boot 不抛异常（核心断言：实例化 provider 并调用 boot() 不抛 Throwable）；(c) 插件未启用（plugins 表无 is_enabled=true 记录或表缺失）时 `config('filesystems.disks.oss')` 为 null（磁盘未注入）。
  </action>
  <verify>
    <automated>cd /home/john/projects/personal/filament-admin && php artisan test --filter='OssServiceProviderTest'</automated>
  </verify>
  <acceptance_criteria>
    - `OssServiceProvider.php` 含字符串 `Storage::extend('oss'`（grep 命中）
    - `OssServiceProvider.php` boot() 中磁盘注入逻辑被 `try`/`catch (\Throwable)` 包裹（grep `catch (\Throwable` 命中）
    - `OssServiceProvider.php` boot() 在注入磁盘前检查 `plugins` 表 `is_enabled` 状态（grep `is_enabled` 命中），未启用时跳过注入
    - `OssPlugin::getId()` 返回 `filament-admin-oss`
    - `OssSettingsPage` 的 `$settings` 属性等于 `OssSettings::class`，form 含 5 个 make() 字段（access_key_id/access_key_secret/bucket/endpoint/region）
    - `php artisan test --filter='OssServiceProviderTest'` 退出码 0；测试断言 (settings/plugins 表缺失时 boot() 不抛异常) 且 (未启用时 oss 磁盘未注入)
  </acceptance_criteria>
  <done>OSS 驱动通过 Storage::extend 注册，仅在插件启用且凭证完整时注入磁盘，表缺失时安全降级，配置页 5 字段就位，测试绿</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 后台超管 → SettingsPage | 凭证（AccessKeySecret）经表单写入 settings 表 |
| 应用启动 → settings 表 | ServiceProvider boot() 读取数据库凭证 |
| 应用启动 → plugins 表 | ServiceProvider boot() 读取插件启用状态决定是否注入磁盘 |
| Flysystem → 阿里云 OSS | 上传/读取/删除文件跨网络边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Information Disclosure | OssSettings.access_key_secret 存储 | mitigate | Settings `encrypted()` 声明 + SettingsMigration `addEncrypted()`，凭证 AES-256-CBC 加密存于 settings 表（Task 2 测试断言加密字段声明） |
| T-08-02 | Denial of Service | OssServiceProvider::boot() 首次迁移前读 settings/plugins | mitigate | `try/catch (\Throwable)` 包裹 Settings 与 plugins 表读取，表缺失时静默跳过不阻断应用启动（Task 3 测试断言不抛异常） |
| T-08-10 | Elevation of Privilege | 插件禁用后磁盘仍可被使用 | mitigate | boot() 注入磁盘前检查 plugins.is_enabled，禁用插件不注入 oss 磁盘配置（Task 3 测试断言未启用时磁盘未注入，D-08-10） |
| T-08-04 | Spoofing/Tampering | extra.filament-admin 契约字段被 plugin:scan 信任 | accept | source=official_listed 由本项目自发布，非第三方提交；plugin:scan 仅信任 official_trusted/official_listed |
| T-08-SC | Tampering | composer require iidestiny/flysystem-oss | accept | RESEARCH Package Legitimacy Audit 已通过 Packagist 直接验证（633K 下载，6 年历史，github.com/iiDestiny/flysystem-oss），slopcheck [SLOP] 为 npm 注册表误判；非 [ASSUMED]/[SUS]，无需阻断 checkpoint |
</threat_model>

<verification>
- `php artisan test --filter='Oss'` 全绿（3 个测试类）
- `php -l` 对 4 个新 PHP 源文件无语法错误
- composer.json 合法 JSON 且含 extra.filament-admin 契约，testbench=^10.0
</verification>

<success_criteria>
- OSS 插件包目录结构完整，可被 Laravel 自动发现（extra.laravel.providers）
- Storage::extend('oss') 注册成功；插件启用且凭证完整时 filesystems.disks.oss 被注入，插件禁用时不注入
- access_key_secret 加密存储
- 三个单元测试退出码 0
</success_criteria>

<output>
Create `.planning/phases/08-cloud-storage-plugins/08-01-SUMMARY.md` when done
</output>
