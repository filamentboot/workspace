---
phase: 04-release-automation
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - tests/Unit/Release/DemoAppPackageBridgeTest.php
  - AGENTS.md
  - packages/filament-admin/README.md
autonomous: true
requirements: [RELEASE-03, RELEASE-04, RELEASE-05]
user_setup:
  - service: github
    why: "根 CI 测试运行需要合法 APP_KEY（D-44 替换硬编码占位符）"
    env_vars:
      - name: CI_APP_KEY
        source: "本地执行 php artisan key:generate --show 生成 base64:XXX，存入主仓库 GitHub Settings → Secrets and variables → Actions → New repository secret"
    dashboard_config:
      - task: "添加 CI_APP_KEY secret（值为 php artisan key:generate --show 输出）"
        location: "GitHub → 主仓库 Settings → Secrets and variables → Actions"

must_haves:
  truths:
    - "根 CI 在每次 PR/push 运行后，composer audit --abandoned=report 步骤结果在 Actions summary 可见（失败为 warning 不阻塞）"
    - "根 CI 与包 CI 中不再有 APP_KEY: base64:AAAA... 硬编码占位符"
    - "PHPUnit 测试断言根 CI 的 APP_KEY 行不含 base64:AAAA 硬编码"
    - "包 README Codecov 徽章位置含 TODO 占位注释（RELEASE-05 本期跳过）"
    - "AGENTS.md 含发版流程段，写明 Gitee 同步为人工步骤"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "根 CI 补 composer audit + APP_KEY secret 引用"
      contains: "secrets.CI_APP_KEY"
    - path: "tests/Unit/Release/DemoAppPackageBridgeTest.php"
      provides: "RELEASE-04 文件内容断言（APP_KEY 非硬编码）"
      contains: "test_root_ci"
    - path: "AGENTS.md"
      provides: "发版流程段（D-43 Gitee 人工步骤说明）"
      contains: "发版流程"
    - path: "packages/filament-admin/README.md"
      provides: "Codecov 徽章 TODO 占位（D-46）"
      contains: "TODO"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "secrets.CI_APP_KEY"
      via: "env.APP_KEY 引用（D-44）"
      pattern: "APP_KEY:\\s*\\$\\{\\{ secrets.CI_APP_KEY"
    - from: "tests/Unit/Release/DemoAppPackageBridgeTest.php"
      to: ".github/workflows/ci.yml"
      via: "file_get_contents + 断言无 base64:AAAA"
      pattern: "ci.yml"
---

<objective>
完成根 CI 安全审计补全（RELEASE-03）、APP_KEY 硬编码清理（RELEASE-04）、Codecov 跳过确认（RELEASE-05），并补 AGENTS.md 发版流程说明（D-43）与 RELEASE-04 自动化测试断言。具体：根 CI `ci.yml` 在 pint:test 后追加 `composer audit --abandoned=report`（continue-on-error），并把第 33 行 `APP_KEY: base64:AAAA...` 改为 `${{ secrets.CI_APP_KEY }}`（D-44，包 CI 不动 per D-45）；扩展 `DemoAppPackageBridgeTest.php` 断言根 CI 无硬编码 APP_KEY；包 README Codecov 徽章位置加 TODO 占位（D-46）；AGENTS.md 新增发版流程段。

Purpose: 消除 CI secret 硬编码安全隐患、补齐安全审计可见性、显式记录 Codecov 跳过与 Gitee 人工同步约定。覆盖 RELEASE-03/04/05 + D-43。
Output: 修改 ci.yml / DemoAppPackageBridgeTest.php / AGENTS.md / 包 README.md。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-release-automation/04-CONTEXT.md
@.planning/phases/04-release-automation/04-RESEARCH.md
@.planning/phases/04-release-automation/04-PATTERNS.md
</context>

<artifacts_produced>
## Artifacts this phase produces

本计划新建/修改的符号（drift 校验时排除）：
- 修改文件：`.github/workflows/ci.yml`（新增 audit step + 改 APP_KEY env 行）
- 修改文件：`tests/Unit/Release/DemoAppPackageBridgeTest.php`（新增测试方法）
- 修改文件：`AGENTS.md`（新增 `## 发版流程（Release）` 段）
- 修改文件：`packages/filament-admin/README.md`（新增 Codecov TODO 占位注释）
- 新测试方法：`test_root_ci_app_key_is_not_hardcoded`（断言根 CI 无 base64:AAAA）
- 新引用的 secret：`CI_APP_KEY`（由 user_setup 配置，本计划仅引用）
- 新 CI step：根 ci.yml 的 `composer audit --abandoned=report`（继承包 CI 已有模式）
</artifacts_produced>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 改根 CI（audit + APP_KEY）并扩展 PHPUnit 断言</name>
  <files>.github/workflows/ci.yml, tests/Unit/Release/DemoAppPackageBridgeTest.php</files>
  <read_first>
    - .github/workflows/ci.yml（根 CI，第 33 行 APP_KEY 硬编码、第 64 行 pint:test step）
    - packages/filament-admin/.github/workflows/ci.yml（包 CI 第 45-47 行 audit step 模式，直接复用）
    - tests/Unit/Release/DemoAppPackageBridgeTest.php（现有纯 PHPUnit TestCase，新增方法风格参照现有两个方法）
    - .planning/phases/04-release-automation/04-RESEARCH.md（§Code Examples RELEASE-03/04 + §Validation RELEASE-04 测试映射）
    - .planning/phases/04-release-automation/04-PATTERNS.md（§ci.yml 修改两处）
  </read_first>
  <behavior>
    - Test 1: test_root_ci_app_key_is_not_hardcoded — 读取根 .github/workflows/ci.yml，断言不含字符串 `base64:AAAA`，且含 `secrets.CI_APP_KEY`
    - Test 2: test_root_ci_has_security_audit_step — 断言根 ci.yml 含 `composer audit --abandoned=report`
  </behavior>
  <action>
    先写测试（RED）：在 `DemoAppPackageBridgeTest.php` 新增两个方法。`test_root_ci_app_key_is_not_hardcoded`：`$ci = file_get_contents(__DIR__.'/../../../.github/workflows/ci.yml')`；`assertStringNotContainsString('base64:AAAA', $ci)`；`assertStringContainsString('secrets.CI_APP_KEY', $ci)`。`test_root_ci_has_security_audit_step`：`assertStringContainsString('composer audit --abandoned=report', $ci)`。中文 PHPDoc 注释，PSR-12，return 前空行。
    再改实现（GREEN）：
    (RELEASE-04 / D-44) 根 ci.yml 第 33 行 `APP_KEY: base64:AAAA...` 改为 `APP_KEY: ${{ secrets.CI_APP_KEY }}`。保留第 58 行 `php artisan key:generate` step 不变（写入 .env，流程正确）。仅替换 env 块里的 APP_KEY 行。包 CI 不动（D-45：包测试不依赖 APP_KEY）。
    (RELEASE-03) 在根 ci.yml 末尾 `composer pint:test`（第 64 行）之后追加 audit step（复用包 CI 第 45-47 行模式）：`- name: 安全审计（仅警告，不阻塞 CI）` + `run: composer audit --abandoned=report --no-interaction` + `continue-on-error: true`。
    运行 `composer test --filter=DemoAppPackageBridgeTest` 确认两新测试 GREEN。
  </action>
  <verify>
    <automated>composer test -- --filter=DemoAppPackageBridgeTest 2>&1 | tail -5; python3 -c "import yaml; yaml.safe_load(open('.github/workflows/ci.yml')); print('ci.yml yaml ok')"</automated>
  </verify>
  <acceptance_criteria>
    - `composer test -- --filter=DemoAppPackageBridgeTest` 退出 0（含两个新测试）
    - 根 `ci.yml` 不含 `base64:AAAA`（`grep -v '^#' .github/workflows/ci.yml | grep -c 'base64:AAAA'` 为 0）
    - `ci.yml contains "APP_KEY: ${{ secrets.CI_APP_KEY }}"`
    - `ci.yml contains "composer audit --abandoned=report"` 且其 step 含 `continue-on-error: true`
    - 根 `ci.yml` 仍含 `php artisan key:generate`（未误删）
    - 包 CI `packages/filament-admin/.github/workflows/ci.yml` 未被修改（D-45）
    - `ci.yml` 为合法 YAML（python3 yaml.safe_load 不抛异常）
  </acceptance_criteria>
  <done>根 CI APP_KEY 改为 secrets.CI_APP_KEY 引用（base64:AAAA 已消除），pint:test 后补 composer audit（warning-only）；DemoAppPackageBridgeTest 两个新断言方法 GREEN；包 CI 未动。</done>
</task>

<task type="auto">
  <name>Task 2: AGENTS.md 发版流程段（D-43）+ 包 README Codecov TODO 占位（D-46）</name>
  <files>AGENTS.md, packages/filament-admin/README.md</files>
  <read_first>
    - AGENTS.md（现有结构，「常用命令（项目特定）」段在第 41 行，发版段追加于其前）
    - .planning/phases/04-release-automation/04-PATTERNS.md（§AGENTS.md 追加段落内容模式 + 位置说明）
    - packages/filament-admin/README.md（第 7-12 行徽章区，Codecov TODO 占位插入位置）
    - .planning/phases/04-release-automation/04-CONTEXT.md（D-43 Gitee 人工同步、D-46 Codecov 跳过）
  </read_first>
  <action>
    (D-43) AGENTS.md 在「## 常用命令（项目特定）」段之前新增 `## 发版流程（Release）` 段（中文，参照 PATTERNS.md 内容模式）：写明 **Gitee 同步是人工步骤**，release.yml 只自动推 GitHub 包仓库不推 Gitee；每次发版须手动执行 `scripts/release-package.sh vX.Y.Z`（含 Gitee 推送），并列出 `verify-package-install.sh` / `release-rollback.sh` 用途；说明 CI release.yml 在 push v* tag 时自动触发的链路（subtree split → 推 GitHub → 打 tag → gh release create → verify）；列出所需 GitHub Secrets：`PACKAGE_GITHUB_TOKEN`（Fine-grained PAT，scope john-captain/filament-admin contents:write）与 `CI_APP_KEY`（php artisan key:generate --show 生成）。
    (D-46) 包 README.md 在第 12 行 Tests 徽章之后、第 13 行 `---` 之前，插入 Codecov 徽章 TODO 占位注释：HTML 注释形式，写明"RELEASE-05 本期跳过，Codecov 徽章待将来注册账号后接入"，不放真实徽章链接（避免显示 404 或无效覆盖率）。不修改 README 其它内容。
  </action>
  <verify>
    <automated>grep -c '发版流程' AGENTS.md; grep -c 'release-package.sh' AGENTS.md; grep -ci 'codecov' packages/filament-admin/README.md; grep -ci 'TODO' packages/filament-admin/README.md</automated>
  </verify>
  <acceptance_criteria>
    - `AGENTS.md contains "## 发版流程（Release）"`
    - `AGENTS.md contains "scripts/release-package.sh"` 且含 "Gitee 同步是人工步骤"（或等义明确表述 Gitee 人工）
    - `AGENTS.md` 同时提及 `PACKAGE_GITHUB_TOKEN` 与 `CI_APP_KEY`
    - 包 `README.md` Codecov TODO 为 HTML 注释占位（`README.md contains "<!--"` 且同一注释含 `Codecov` 与 `TODO`）
    - 包 README 第 8-12 行原有 5 个徽章未被删除（`grep -c 'img.shields.io' packages/filament-admin/README.md` >= 5）
  </acceptance_criteria>
  <done>AGENTS.md 新增发版流程段（Gitee 人工同步 + 三脚本用途 + release.yml 自动链路 + 两个 Secrets 说明）；包 README Codecov 徽章位置加 TODO HTML 注释占位，原 5 徽章保留。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI secret 存储 → ci.yml env 块 | APP_KEY 由 secret 注入替代硬编码 |
| 上游依赖供应链 → 项目 composer 依赖 | composer audit 检测已知 CVE/废弃包 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02 | Information Disclosure | ci.yml APP_KEY 硬编码 | mitigate | D-44 改为 `${{ secrets.CI_APP_KEY }}`，PHPUnit 断言强制无 `base64:AAAA`（test_root_ci_app_key_is_not_hardcoded） |
| T-04-AUD | Tampering / 供应链 | composer 依赖 CVE 可见性 | mitigate | RELEASE-03 补 `composer audit --abandoned=report`，warning-only 保持可见不阻塞主线（D-18 一致） |
| T-04-DOC | Repudiation | 发版操作约定不明 | mitigate | AGENTS.md 记录 Gitee 人工同步约定（D-43），明确 force push 为 CI 独占计划内行为 |
| T-04-PKG | Tampering | npm/pip/cargo installs | accept | 本计划无新包安装（改 YAML/MD/PHP 测试），无 slopcheck 适用对象 |
</threat_model>

<verification>
- `composer test -- --filter=DemoAppPackageBridgeTest` 退出 0
- 根 ci.yml：APP_KEY 引用 secret、含 audit step、保留 key:generate、合法 YAML
- 包 CI 未修改（D-45）
- AGENTS.md 发版流程段含 Gitee 人工 + 两 secrets
- 包 README Codecov TODO 占位存在，5 徽章保留
</verification>

<success_criteria>
- RELEASE-03（audit 可见）、RELEASE-04（APP_KEY 非硬编码 + 测试断言）、RELEASE-05（Codecov 跳过 + TODO 占位）全部落地
- D-43 Gitee 人工同步约定写入 AGENTS.md
- D-45 包 CI 保持不变
</success_criteria>

<output>
Create `.planning/phases/04-release-automation/04-03-SUMMARY.md` when done
</output>
