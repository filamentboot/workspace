---
phase: 04-release-automation
plan: 04
type: execute
wave: 2
depends_on: ["04-01", "04-02", "04-03"]
files_modified:
  - /tmp/v0.5-acceptance-log.md
autonomous: false
requirements: [RELEASE-06]

must_haves:
  truths:
    - "在 /tmp/v0.5-acceptance 干净 Laravel 13 环境以新用户身份完成全部 7 项 acceptance 检查"
    - "release.yml dry-run（或真实 push tag）已通过 + scripts 三件套已手测通过（前置条件）"
    - "/tmp/v0.5-acceptance-log.md 记录全过程且无 blocker"
    - "不动用户工作区任何目录（隔离在 /tmp）"
  artifacts:
    - path: "/tmp/v0.5-acceptance-log.md"
      provides: "RELEASE-06 acceptance 全过程记录 + 每步卡点"
      contains: "acceptance"
  key_links:
    - from: "wiki/installation.md"
      to: "/tmp/v0.5-acceptance 干净环境"
      via: "新用户严格按文档逐步执行（不跳步）"
      pattern: "installation"
---

<objective>
执行 RELEASE-06：v0.5 出版前手动接收测试。这是 **v0.5 出版闸门**——在 `/tmp/v0.5-acceptance` 隔离的干净 Laravel 13 环境，以"新用户第一次接触"身份严格按 `wiki/installation.md` 走完整路径，逐项验证 7 项 acceptance 检查，全过程写入 `/tmp/v0.5-acceptance-log.md`。**此项不通过不能打 v0.5.0 正式 tag。**

该任务无法自动化（需真实干净环境 + 人工判断文档清晰度与实际行为一致性），由人工执行。前置条件：04-01（release.yml）、04-02（scripts 三件套）、04-03（CI 修改）全部完成，且 release.yml 已 dry-run 通过 + scripts 手测通过。

Purpose: 在真实"新用户"路径上验证 v0.5 整体可用性，捕获文档与实际行为的所有不一致，作为正式发版的最终闸门。
Output: `/tmp/v0.5-acceptance-log.md` 全过程记录。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-release-automation/04-CONTEXT.md
@wiki/installation.md
</context>

<artifacts_produced>
## Artifacts this phase produces

本计划新建的符号（drift 校验时排除）：
- 新文件：`/tmp/v0.5-acceptance-log.md`（acceptance 全过程记录，位于 /tmp 隔离区，不进仓库）
- 临时环境：`/tmp/v0.5-acceptance`（composer create-project 起的干净 Laravel 13 项目，验收后可删）
</artifacts_produced>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: v0.5 出版闸门 — 干净环境 7 项 acceptance 人工验收</name>
  <files>/tmp/v0.5-acceptance-log.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md（§RELEASE-06，含执行步骤与 7 项 acceptance 验证清单、核心约束、触发条件）
    - wiki/installation.md（新用户安装唯一权威源，严格逐步执行依据）
    - .planning/phases/04-release-automation/04-CONTEXT.md（D-43 Gitee 人工同步 + 所需 Secrets）
  </read_first>
  <action>
    人工执行 RELEASE-06 出版闸门验收（manual-only，无法自动化）。前置确认 04-01/04-02/04-03 完成且 release.yml dry-run 通过 + scripts 三件套手测通过。按 REQUIREMENTS.md §RELEASE-06 执行步骤：在 /tmp/v0.5-acceptance 起干净 Laravel 13 项目（`composer create-project laravel/laravel /tmp/v0.5-acceptance`）→ `composer require laravelstack/filament-admin:^0.5` → 严格逐步照 wiki/installation.md 走（不跳步、不凭记忆）→ 逐项验证 7 项 acceptance（见 how-to-verify）→ 全过程含每处文档/行为不一致写入 /tmp/v0.5-acceptance-log.md。核心约束：全部隔离在 /tmp，不动用户工作区任何目录。任一项失败：log 记 blocker，回对应 phase 修复后重跑整个 acceptance（非单点局部验证）。
  </action>
  <what-built>
    Phase 4 已完成：release.yml 全自动发版 workflow（04-01）、发版脚本三件套（04-02）、根 CI 安全审计 + APP_KEY 清理 + AGENTS.md 发版说明（04-03）。本任务在干净环境对整个 v0.5 主包做端到端 acceptance。
  </what-built>
  <how-to-verify>
    **核心约束：不动用户工作区任何目录，全部在 /tmp 隔离环境进行。前置：release.yml dry-run 通过 + scripts 三件套手测通过。**

    1. `composer create-project laravel/laravel /tmp/v0.5-acceptance && cd /tmp/v0.5-acceptance`
    2. `composer require laravelstack/filament-admin:^0.5`（或 dev-main / v0.5.0-rc.1）
    3. 严格按 `wiki/installation.md` 逐步执行 — 不偷懒、不跳步、不凭记忆，完全模拟"新用户第一次接触"。wiki 每一行不清楚的指令、每一个不合理的默认值、每一处实际行为与文档不符处，**全部写入 `/tmp/v0.5-acceptance-log.md`**。

    **7 项 acceptance 验证（全部通过才算 RELEASE-06 完成）：**
    1. `php artisan vendor:publish --tag=filament-admin-config / migrations / views / lang / stubs` 5 个 tag 各自落地对应资源（验 COMPLY-01）
    2. `php artisan migrate` 跑通；`php artisan db:seed --class="FilamentAdmin\\Database\\Seeders\\SuperAdminSeeder"` 创建默认管理员
    3. `admin@example.com / password` 能登录后台首页（路径 /admin，需先按 wiki 配 AdminPanelProvider）
    4. AdminUserResource 列表页点"模拟登录"，能切换身份且顶栏显示"结束模拟"（验 FEAT-01）
    5. 访问 /docs/api 返回 Scramble 生成的 OpenAPI 3.0 文档界面（验 FEAT-02）
    6. `php artisan make:filament-admin-resource Product` 在用户项目正确位置生成 Resource 文件（验 FEAT-03）
    7. `php artisan filament-admin:publish --model=Product --all` 输出 Model + Resource + Migration + Test 四件套（验 COMPLY-02）

    **产出：** `/tmp/v0.5-acceptance-log.md` — 全过程记录 + 每个步骤卡点。
    **若有 blocker：** 回相应 phase 修复后**重新整个 acceptance 流程**（不只单点验证，因为修一个 bug 可能引入另一个）。
  </how-to-verify>
  <verify>
    <human-check>7 项 acceptance 全部通过且 /tmp/v0.5-acceptance-log.md 记录无 blocker；任一项失败则回相应 phase 修复后重跑整个流程</human-check>
  </verify>
  <acceptance_criteria>
    - `/tmp/v0.5-acceptance-log.md` 存在且记录了 7 项检查逐项结果
    - 7 项 acceptance 验证全部标记为通过（无 blocker）
    - 测试在 /tmp/v0.5-acceptance 隔离环境进行，未改动用户工作区任何目录
    - 任一项失败时：log 记录 blocker，回对应 phase 修复后重跑整个 acceptance（不可单点局部验证后放行）
  </acceptance_criteria>
  <resume-signal>输入 "acceptance passed" 确认 7 项全通过且无 blocker（可打 v0.5.0 正式 tag）；或描述 blocker（回对应 phase 修复后重跑）</resume-signal>
  <done>7 项 acceptance 在干净环境全部通过，/tmp/v0.5-acceptance-log.md 记录无 blocker，v0.5 出版闸门放行。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 发布的 Packagist 包 → 干净 Laravel 13 新项目 | 新用户首次 composer require 引入主包到隔离环境 |
| acceptance 测试环境 → 用户工作区 | 严格隔离在 /tmp，不得污染工作区 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-ACC | Tampering | acceptance 污染工作区 | mitigate | 核心约束：全部在 /tmp/v0.5-acceptance 隔离，REQUIREMENTS.md 明确不动工作区任何目录 |
| T-04-GATE | Repudiation | 未通过即发版 | mitigate | 出版闸门：blocking-human checkpoint，7 项任一失败不放行；blocker 须回 phase 修复后重跑整个流程 |
| T-04-PKG | Tampering | npm/pip/cargo installs | accept | acceptance 安装的是本项目自身发布包（laravelstack/filament-admin），非引入新第三方依赖；属验证目标本身 |
</threat_model>

<verification>
- 本任务为 manual-only（无法自动化）：需真实干净 Laravel 13 环境 + 人工判断文档/行为一致性
- 7 项 acceptance 逐项通过为唯一验收依据
- /tmp/v0.5-acceptance-log.md 记录全过程
</verification>

<success_criteria>
- RELEASE-06 完成：7 项 acceptance 全通过，无 blocker
- v0.5 出版闸门放行（可打 v0.5.0 正式 tag）
- 测试隔离未污染工作区
</success_criteria>

<output>
Create `.planning/phases/04-release-automation/04-04-SUMMARY.md` when done
</output>
