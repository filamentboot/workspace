---
phase: 07-quality-foundation
plan: 03
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - packages/filament-admin/src/Filament/Resources/Menus/MenuResource.php
  - packages/filament-admin/src/Filament/Resources/Concerns/ReorderableWithLog.php
  - packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php
  - packages/filament-admin/src/Filament/Resources/Departments/Pages/ViewDepartment.php
  - tests/Feature/AdminFoundation/MenuBulkActionPermissionTest.php
  - tests/Feature/AdminFoundation/DepartmentReorderLogTest.php
autonomous: true
requirements: [FIX-03, FIX-05, FIX-06]
must_haves:
  truths:
    - "MenuResource 批量启用/禁用 BulkAction 增加 ->visible(fn () => can('update_menu')) 权限检查，无权管理员看不到批量启停（FIX-03）"
    - "DepartmentResource 的 rememberReorderSnapshot/logReorderActivity/buildReorderSnapshot 抽取为 ReorderableWithLog Trait 并被 DepartmentResource use（FIX-05）"
    - "DepartmentResource 排序后仍正确记录 reordered 操作日志（抽 Trait 后行为不回归）（FIX-05）"
    - "DepartmentResource::getPages() 含 view 路由，ViewDepartment 页面类存在且可访问（FIX-06）"
  artifacts:
    - path: "packages/filament-admin/src/Filament/Resources/Concerns/ReorderableWithLog.php"
      provides: "可复用的排序日志 Trait"
      contains: "trait ReorderableWithLog"
    - path: "packages/filament-admin/src/Filament/Resources/Departments/Pages/ViewDepartment.php"
      provides: "部门查看页"
      contains: "ViewRecord"
    - path: "packages/filament-admin/src/Filament/Resources/Menus/MenuResource.php"
      provides: "带权限的批量启停"
      contains: "update_menu"
  key_links:
    - from: "packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php"
      to: "FilamentAdmin\\Filament\\Resources\\Concerns\\ReorderableWithLog"
      via: "use ReorderableWithLog"
      pattern: "ReorderableWithLog"
    - from: "packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php"
      to: "ViewDepartment::route('/{record}')"
      via: "getPages() 注册 view"
      pattern: "ViewDepartment"
---

<objective>
FIX 批第二组（Wave 2，依赖 07-02 释放 MenuResource.php）：FIX-03 菜单批量启停加权限、FIX-05 抽取 ReorderableWithLog Trait、FIX-06 新建 ViewDepartment 页面并注册 view 路由。

Purpose: FIX-03 与 FIX-02 同改 MenuResource.php，故排在 07-02 之后（Wave 2）避免并行覆盖。FIX-05/06 集中收口部门资源（同改 DepartmentResource.php，合并进同一 plan 串行安全）。
Output: ReorderableWithLog Trait + ViewDepartment 页面 + MenuResource 批量启停权限 + DepartmentResource 改用 Trait 与注册 view + 2 个测试。
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-quality-foundation/07-CONTEXT.md
@packages/filament-admin/src/Filament/Resources/Menus/MenuResource.php
@packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php
@packages/filament-admin/src/Filament/Resources/LoginLogs/Pages/ViewLoginLog.php
@packages/filament-admin/src/Services/ActivityLogger.php
</context>

## 关键约束（来自 CONTEXT / ROADMAP / 现状核查）

- **FIX-03：** MenuResource 第 190-205 行 bulkActions 里 `BulkAction::make('enable')` 与 `BulkAction::make('disable')` 各加 `->visible(fn (): bool => auth('admin')->user()?->can('update_menu') ?? false)`。注意现状 ToggleColumn(is_active) 第 170 行已有 `update_menu` disabled 判定，权限点一致。
- **FIX-05：** 把 DepartmentResource 第 163-216 行三个私有静态方法 `rememberReorderSnapshot`/`logReorderActivity`/`buildReorderSnapshot` 抽到新 Trait `FilamentAdmin\Filament\Resources\Concerns\ReorderableWithLog`（新建 `Concerns/` 目录）。Trait 内方法保持 protected static、保留中文 PHPDoc 与逻辑；DepartmentResource `use ReorderableWithLog;`，删除原内联方法，table() 中 `beforeReordering`/`afterReordering` 回调不变（仍调 static::rememberReorderSnapshot / logReorderActivity）。**MenuResource 用独立 MenuTree 页面、无 Filament 拖拽排序，不适用此 Trait**（CONTEXT D 明确）。Trait 中对 `Department::query()` 的依赖需泛化：buildReorderSnapshot 当前硬编码 Department——抽取时改为依赖资源 `static::getModel()`（Filament Resource 提供）以解耦，request attributes key 用资源相关命名（如 `static::getModel().'_reorder_before'` 或保留 department_reorder_before 但记入仅 Department 使用的说明）。**优先采用 `static::getModel()` 泛化** + snapshot 列集合从固定 ['id','parent_id','name','sort','is_active'] 保留（Department 现有列），并在 Trait PHPDoc 注明该列集合面向树形可排序资源。
- **FIX-06：** DepartmentResource `getPages()`（第 149-156 行）补 `'view' => Pages\ViewDepartment::route('/{record}')`；新建 `Pages/ViewDepartment.php`（extends ViewRecord，参考 ViewLoginLog 范本，`protected static string $resource = DepartmentResource::class;`）。

<tasks>

<task type="auto">
  <name>Task 1: FIX-03 菜单批量启停权限 + FIX-06 ViewDepartment 页面</name>
  <files>packages/filament-admin/src/Filament/Resources/Menus/MenuResource.php, packages/filament-admin/src/Filament/Resources/Departments/Pages/ViewDepartment.php, packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php, tests/Feature/AdminFoundation/MenuBulkActionPermissionTest.php</files>
  <read_first>
    - packages/filament-admin/src/Filament/Resources/Menus/MenuResource.php（第 168-206 行 ToggleColumn update_menu 判定 + bulkActions）
    - packages/filament-admin/src/Filament/Resources/LoginLogs/Pages/ViewLoginLog.php（ViewRecord 最小范本）
    - packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php（getPages 第 149-156 行）
    - tests/Feature/Permissions 或 AdminFoundation 下权限测试（建带/不带 update_menu 权限管理员的写法）
  </read_first>
  <behavior>
    - 拥有 update_menu 权限的管理员：MenuResource 批量启用/禁用 BulkAction visible 为 true
    - 不拥有 update_menu 权限的管理员：两个 BulkAction visible 为 false
    - DepartmentResource 存在 view 路由，ViewDepartment 可解析
  </behavior>
  <action>
    **FIX-03（行为变更，先写失败测试 D-11）：** 先写 `tests/Feature/AdminFoundation/MenuBulkActionPermissionTest.php`：
    - 用例 A：建拥有 update_menu 权限的管理员，断言 `auth('admin')->user()->can('update_menu')` 为 true（作为 visible 判定来源的真值），并可附加断言（若可解析 Resource bulkActions）visible 闭包返回 true。
    - 用例 B：建无该权限管理员，断言 can('update_menu') 为 false。
    （若直接断言 Filament BulkAction visible 闭包成本高，则以权限点真值 + grep visible 闭包存在 双重保证。）
    然后修改 MenuResource：给 `BulkAction::make('enable')` 和 `BulkAction::make('disable')` 各加 `->visible(fn (): bool => auth('admin')->user()?->can('update_menu') ?? false)`。
    **FIX-06（机械新建）：** 创建 `Pages/ViewDepartment.php`（namespace FilamentAdmin\Filament\Resources\Departments\Pages，extends `Filament\Resources\Pages\ViewRecord`，`protected static string $resource = DepartmentResource::class;`，getHeaderActions 返回含 EditAction（参考资源可编辑场景）或空数组，参考 ViewLoginLog）。在 DepartmentResource `getPages()` 增加 `'view' => Pages\ViewDepartment::route('/{record}')`，并确保 import `use FilamentAdmin\Filament\Resources\Departments\Pages\ViewDepartment;`（或沿用现有 Pages 命名空间导入风格）。
    （注意：本任务对 DepartmentResource.php 仅追加 view 路由 + import，与 Task 2 抽 Trait 的改动在同一 plan 内串行执行、同一文件——按任务顺序先后编辑，无并行冲突。）
  </action>
  <verify>
    <automated>php -l packages/filament-admin/src/Filament/Resources/Menus/MenuResource.php && php -l packages/filament-admin/src/Filament/Resources/Departments/Pages/ViewDepartment.php && php -l packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php && grep -q "update_menu" packages/filament-admin/src/Filament/Resources/Menus/MenuResource.php && grep -A6 "BulkAction::make('enable')" packages/filament-admin/src/Filament/Resources/Menus/MenuResource.php | grep -q "visible" && grep -q "ViewDepartment::route" packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php && grep -q "ViewRecord" packages/filament-admin/src/Filament/Resources/Departments/Pages/ViewDepartment.php && composer test -- --filter=MenuBulkActionPermissionTest 2>&1 | tail -8 && echo FIX0306-OK</automated>
  </verify>
  <done>菜单批量启用/禁用各带 update_menu visible 判定；ViewDepartment 页面就绪、DepartmentResource 注册 view 路由；MenuBulkActionPermissionTest 双向绿。</done>
</task>

<task type="auto">
  <name>Task 2: FIX-05 抽取 ReorderableWithLog Trait + DepartmentResource 改用</name>
  <files>packages/filament-admin/src/Filament/Resources/Concerns/ReorderableWithLog.php, packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php, tests/Feature/AdminFoundation/DepartmentReorderLogTest.php</files>
  <read_first>
    - packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php（第 122-127 beforeReordering/afterReordering 回调 + 第 158-216 三个私有方法）
    - packages/filament-admin/src/Services/ActivityLogger.php（logChanges / currentCauser 签名，确认 Trait 内调用不变）
    - 现有 tests/Feature 下若有部门排序/ActivityLog 相关测试（确认建部门 + 触发 reorder + 断言 activity_log 的写法）
  </read_first>
  <behavior>
    - DepartmentResource use ReorderableWithLog 后，调用 rememberReorderSnapshot 再 logReorderActivity，对一组部门排序写入一条 action='reordered' 的活动日志，before/after 含 order 快照
    - 无 causer 或 record 找不到时不写日志（保留原 guard）
    - 抽 Trait 前后行为一致（无回归）
  </behavior>
  <action>
    先写 `tests/Feature/AdminFoundation/DepartmentReorderLogTest.php`（D-11 TDD，验证抽 Trait 后行为不回归）：
    - 建一个登录管理员（causer）+ 两个部门，手动调用 `DepartmentResource::rememberReorderSnapshot([$a->id,$b->id])` 然后 `DepartmentResource::logReorderActivity([$b->id,$a->id])`（静态方法经 Trait 暴露），断言 activity_log 表新增一条 `description/event = reordered`（按 ActivityLogger 实际落库字段断言）且关联 subject 为 Department。
    - 断言 before/after properties 含 order 数组。
    然后创建 Trait `packages/filament-admin/src/Filament/Resources/Concerns/ReorderableWithLog.php`（namespace FilamentAdmin\Filament\Resources\Concerns，`trait ReorderableWithLog`）：把 DepartmentResource 的 rememberReorderSnapshot/logReorderActivity/buildReorderSnapshot 三方法移入；**用 `static::getModel()` 替换硬编码 `Department`**（logReorderActivity 内 `static::getModel()::query()->find(...)`，buildReorderSnapshot 内 `static::getModel()::query()->whereKey(...)`），request attributes key 改为带模型区分（如 `static::getModel().'_reorder_before'`），snapshot 列集合保留 ['id','parent_id','name','sort','is_active']（Trait PHPDoc 注明面向树形可排序资源）。中文 PHPDoc 保留。
    修改 DepartmentResource：`use FilamentAdmin\Filament\Resources\Concerns\ReorderableWithLog;` 并在 class 内 `use ReorderableWithLog;`（trait use），**删除原第 158-216 行三个内联方法**；table() 的 beforeReordering/afterReordering 回调不变（仍调 static::rememberReorderSnapshot / static::logReorderActivity）。确认 ActivityLogger / Department import 在 Trait 内补齐、DepartmentResource 内若不再直接用则由 Pint 清理。
  </action>
  <verify>
    <automated>php -l packages/filament-admin/src/Filament/Resources/Concerns/ReorderableWithLog.php && php -l packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php && grep -q "trait ReorderableWithLog" packages/filament-admin/src/Filament/Resources/Concerns/ReorderableWithLog.php && grep -q "use ReorderableWithLog" packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php && grep -q "static::getModel()" packages/filament-admin/src/Filament/Resources/Concerns/ReorderableWithLog.php && ! grep -q "protected static function buildReorderSnapshot" packages/filament-admin/src/Filament/Resources/Departments/DepartmentResource.php && composer test -- --filter=DepartmentReorderLogTest 2>&1 | tail -8 && echo FIX05-OK</automated>
  </verify>
  <done>ReorderableWithLog Trait 创建（getModel() 泛化）；DepartmentResource use 该 Trait 且原三个内联方法已移除；排序日志行为无回归（DepartmentReorderLogTest 绿）。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| 管理员 → 菜单批量操作 | 无 update_menu 权限者不应执行批量启停 |
| 管理员 → 部门排序 | 排序操作需被审计记录 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-06 | Elevation of Privilege | 无权用户批量启停菜单 | mitigate | 两个 BulkAction ->visible(can('update_menu'))；MenuBulkActionPermissionTest 双向断言 |
| T-07-07 | Repudiation | 部门排序无审计 | mitigate | ReorderableWithLog 经 ActivityLogger 写 reordered 日志（DepartmentReorderLogTest 验证不回归） |
| T-07-08 | Information Disclosure | ViewDepartment 越权查看 | mitigate | ViewRecord 经 Resource Policy（view_department）授权，沿用 Filament 默认授权链 |
</threat_model>

<verification>
- `composer test -- --filter=MenuBulkActionPermissionTest` 绿（FIX-03）
- `composer test -- --filter=DepartmentReorderLogTest` 绿（FIX-05 无回归）
- grep：MenuResource enable/disable BulkAction 含 visible；ReorderableWithLog 为 trait 且用 getModel()；DepartmentResource use Trait 且无内联 buildReorderSnapshot；DepartmentResource 含 ViewDepartment::route；ViewDepartment extends ViewRecord
- `composer pint:test` 通过
- 手测（归 VALIDATION）：无 update_menu 权限管理员看不到批量启停；部门拖拽排序后操作日志有 reordered；部门列表点查看进入 ViewDepartment
</verification>

<success_criteria>
- FIX-03：菜单批量启停带权限可见性
- FIX-05：排序日志逻辑抽为 ReorderableWithLog Trait（getModel 泛化），DepartmentResource 改用且行为无回归
- FIX-06：ViewDepartment 页面 + view 路由就绪
- 两测试绿、Pint 通过
</success_criteria>

<output>
Create `.planning/phases/07-quality-foundation/07-03-SUMMARY.md` when done
</output>
