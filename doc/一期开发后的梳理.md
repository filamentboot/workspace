# FilamentAdmin — 产品全貌与技术规范

> 整合自：需求.md（第一版规范）+ 需求2.md（第二版规划）+ 竞品分析  
> 更新时间：2026-05-29  
> 本文件为项目**唯一产品文档**，所有功能决策以本文为准。

---

## 一、项目定位

**Laravel + Filament 后台基础平台，主打两大特色：开箱即用的代码市场，与组合最优的代码基座。**

每次新项目不再重复搭后台基础设施。基于 Filament 5，预装配权限、系统配置、媒体库、操作日志等通用能力，并通过双轨制插件市场支持工具扩展与业务模块二开。MIT 协议，免费商用。

### 1.1 两大特色

**特色一：开箱即用的代码市场**

对标 FastAdmin 多年沉淀的"应用插件市场"——用户在后台搜索"商城""官网""CMS"，下载即用，需要二开的代码直接落到自己项目里。FilamentAdmin 用双轨制方案实现：
- **工具型插件**（OSS、短信、支付、Excel 处理等）→ 住 `vendor/`，标准 Composer 包，配置即用
- **业务模块型插件**（商城、官网、论坛、知识付费等）→ 解包到 `Modules/`，代码归项目所有，可自由二开

第一版做本地插件中心 + 远程索引浏览 + 安装引导 + 商业化预留；第二版补齐异步安装、在线升级、ZIP 离线安装，并**正式启动商业化闭环**（付费插件、开发者入驻、平台分成）。

**特色二：组合最优的代码基座**

不重复造轮子，把市面上最被验证过的 Laravel / Filament 扩展包按"开箱即用"的标准组合起来，并提供清晰的定制层与扩展点说明。开发者拿到项目后，看一份文档就能知道"用了哪些包、为什么选这些、做了哪些定制、二开时应该在哪一层下手"。


---

## 二、代码基座价值主张

本项目第二大特色是"组合最优的代码基座"。这不是技术包装，而是给二次开发者的具体承诺：

**承诺一：透明可审计**
- 没有黑盒。所有定制都在 `architecture/customizations.md` 列出：改了哪个包的什么行为、为什么改、改在哪个文件、如何还原默认行为
- `composer.json` 里每一个非标准依赖都能在文档里查到"为什么选它、和哪些替代方案对比过"

**承诺二：组合最优而非碰运气**

市面上有大量"Laravel + Filament 启动模板"是作者随手攒的轮子组合，遇到坑没人维护。本项目对每个核心扩展点的选型遵循以下标准：

| 选型维度 | 硬性要求 |
|---------|---------|
| 下载量 | Packagist 累计下载量 ≥ 100 万（核心依赖 ≥ 1000 万）|
| 维护活跃度 | 近 6 个月有 commit，issue 平均响应 ≤ 30 天 |
| 文档完整度 | 官方 README 含完整安装/配置/进阶用法说明 |
| 生态兼容性 | 与 Filament 官方插件无已知冲突 |
| 团队背书 | 来自 Spatie / Filament 官方 / Laravel 官方 / 著名社区维护者优先 |

不满足上述标准的方案不进入本项目主体集成，可作为可选插件。

**承诺三：升级有路径**
- 每个集成包都锁定主版本，patch/minor 自由升级
- 集成包发布大版本时，本项目在下一个主版本中跟进升级，升级前在 GitHub Issues 公开"升级评估报告"
- 任何依赖的"破坏性变更"都在 `UPGRADE.md` 中明确给出迁移代码示例

**承诺四：能挖洞，也能填洞**
- 集成包暴露的所有扩展点（Trait / Contract / Event）在 `docs/development/base-classes.md` 集中说明
- 业务方如需替换某个集成包（如不用 Spatie 改用 Bouncer），文档提供"替换路径"章节，至少说明替换工作量级别（小时 / 天 / 周）

### 2.1 代码基座默认集成清单

第一版默认随主包提供以下能力，开发者执行 `composer create-project filament-admin/skeleton` 后即获完整后台。**后续版本可以按需追加主包默认集成**——前提是新依赖满足选型硬性标准，且确属"绝大多数后台项目都会用到"的基础能力。可有可无的能力一律走可选插件路线。

| 能力域 | 集成包 | 默认行为 | 第二版可选替代 |
|--------|--------|---------|--------------|
| 后台 UI 框架 | `filament/filament` | Panel Builder + Resources + Pages + Widgets | — |
| 权限 | `spatie/laravel-permission` + `bezhansalleh/filament-shield` | Action 级别权限，自动生成 Policy 和 UI | — |
| 操作日志 | `spatie/laravel-activitylog` + `pxlrbt/filament-activity-log` | opt-in 模式，默认只记录后台 Action 操作 | — |
| 媒体库 | `spatie/laravel-medialibrary` + `filament/spatie-laravel-media-library-plugin` | 本地磁盘存储，缩略图自动生成 | 云存储插件 |
| 系统配置 | `spatie/laravel-settings` + `filament/spatie-laravel-settings-plugin` | 强类型 PHP 类 + 自动生成 Filament 配置页 + 缓存 | — |
| 业务模块 | `nwidart/laravel-modules` | `Modules/` 目录结构，模块启停 | — |
| API 认证 | `laravel/sanctum` | Bearer Token + Session 双模式 | — |
| 登录限流 | Filament 5 内置 + `danharrin/livewire-rate-limiting` | 每分钟 5 次尝试 | — |
| 双因素认证 | `stephenjude/filament-two-factor-authentication` | TOTP + Passkey + Recovery Code（**默认随包但默认关闭**，管理员个人启用） | — |
| 测试框架 | `pestphp/pest` + `larastan/larastan` + `laravel/pint` | 项目自带 GitHub Actions 工作流 | — |

**不进入默认集成清单（按需作为插件）**：
- 队列监控（`laravel/horizon`）：仅 Redis 队列项目需要
- 应用监控（`laravel/pulse`、`laravel/telescope`）：开发期/生产期监控诉求不同
- 数据库备份（`spatie/laravel-backup`）：备份策略与部署方式强相关
- 多语言（`mcamara/laravel-localization`）：单语言项目无需引入
- 全文搜索（`laravel/scout`）：业务相关
- 队列任务管理（如 `laravel/horizon` UI）：第二版可视化章节涉及

> 一句话总结：主包只装"任何后台项目都必装"的东西，能让人犹豫一秒钟的依赖一律放进可选插件。这是"开箱即用"和"主包不臃肿"的平衡点。

---

## 三、竞品分析

> 基于 2026-05-29 调研整理，聚焦三大核心竞品：FastAdmin（国内 TP 标杆）、Backpack（国际 Laravel 标杆）、FilamentAdmin（本项目）。

### 3.1 竞品汇总对比表

#### 3.2.1 认证与用户管理

| 功能 | FastAdmin | Backpack | 我们的实现方式 |
|------|-----------|----------|---------------|
| **后台登录** | ✓ 含登录日志 | ✓ 基础登录 | ✅ **Phase 1 已完成**：自定义 Filament Login 页（支持 username/email 登录）+ `stephenjude/filament-two-factor-authentication` (2FA) + Filament 内置限速（5次/分钟）+ 防枚举攻击（统一错误消息） |
| **管理员管理** | ✓ 列表：ID、用户名、昵称、角色组、状态、最后登录。操作：添加/编辑/删除 | ✓ User Manager（免费插件） | ✅ **Phase 1 已完成**：`AdminUser` 模型（含 `SoftDeletes` + `TwoFactorAuthenticatable` trait）+ 迁移文件 + `AdminUserFactory` + `AdminUserSeeder` + `admin` guard 配置 |
| **个人资料** | ✓ 修改头像、密码、邮箱、手机、昵称 | ⚠️ 基础 | ✅ **已实现**：Filament Profile Page（支持头像上传、密码修改等） |
| **会员管理** | ✓ 会员列表/分组/规则（前台用户） | ❌ 无 | ❌ **第一版不做**：需求明确区分后台管理员与前台会员，第一版聚焦后台底座 |

#### 3.2.2 权限与菜单

| 功能 | FastAdmin | Backpack | 我们的实现方式 |
|------|-----------|----------|---------------|
| **角色权限** | ✓ **RBAC 角色**：角色名、父级角色、权限规则数。支持角色树（父子继承）、数据权限控制可见范围 | ❌ **付费插件** ($29/年) Permission Manager | ✅ **已完成**：`spatie/laravel-permission` + `bezhansalleh/filament-shield`。Action 级别权限，超级管理员通过 `Gate::before()` 拦截 |
| **菜单管理** | ✓ **权限树**：图标、名称、属性、显示、排序、权限标识。无限级树形结构 | ⚠️ **基础**（依赖代码路由，无独立菜单管理表） | ✅ **已完成**：自定义 `Menu` 模型 + Filament Resource，支持后台导航、分组、图标、排序、权限绑定 |
| **管理员日志** | ✓ **操作日志**：管理员、标题、URL、IP、UserAgent、创建时间 | ✓ Log Viewer（免费插件） | ✅ **已完成**：`login_logs` 表 + `LoginLog` 模型 + `LogAdminLogin` 监听器 + `spatie/laravel-activitylog` + `pxlrbt/filament-activity-log`（opt-in 模式） |

#### 3.2.3 内容与资源管理

| 功能 | FastAdmin | Backpack | 我们的实现方式 |
|------|-----------|----------|---------------|
| **CRUD 代码生成** | ✓✓ **一键生成全栈代码**：包含 24 种表单组件演示 | ✓✓ **命令行+面板交互** | ⚠️ **基础可用，待沉淀规范**：复用 Filament 官方 `make:filament-resource` 命令，差异化在于沉淀规范模板（基类、Trait、Policy 约定） |
| **附件管理** | ✓ **文件列表**：支持图片/视频/文件分类，预览、删除 | ⚠️ 基础 | ✅ **已完成**：`spatie/laravel-medialibrary` + `filament/spatie-laravel-media-library-plugin`，默认本地存储，云存储以插件形式扩展 |
| **软删除** | ✓ 回收站功能 | ✓ 支持 | ✅ **已完成**：`AdminUser` 等核心模型均使用 `SoftDeletes` trait |

#### 3.2.4 系统与配置

| 功能 | FastAdmin | Backpack | 我们的实现方式 |
|------|-----------|----------|---------------|
| **系统配置** | ✓ **分组配置**：基础配置（站点名、备案、CDN等）、邮件配置、字典配置 | ❌ **付费插件** ($19/年) Settings | ✅ **已完成**：`spatie/laravel-settings` + `filament/spatie-laravel-settings-plugin`，强类型 PHP 类定义配置，自动生成 Filament 配置页，支持 Redis/Cache 缓存 |
| **仪表盘** | ✓ 统计卡片、订单图表、服务器信息 | ⚠️ 默认空白，需自行添加 Widget | ⚠️ **基础可用**：Filament Widgets 框架已就位，业务统计卡片待补充 |
| **定时任务** | ✓ 可视化任务管理 | ⚠️ 基础 | ⚠️ **基础可用**：`routes/console.php` 已可用 Laravel Scheduler。可视化管理界面在第二版 |

#### 3.2.5 数据与 API

| 功能 | FastAdmin | Backpack | 我们的实现方式 |
|------|-----------|----------|---------------|
| **导入导出** | ⚠️ 基础 CSV 支持 | ⚠️ **付费插件** (Editable Columns) | ✅ **已完成**：基础 Excel/CSV 导出能力，导入按业务模块逐步接入 |
| **API 基础** | ✓ 内置 API 模块 | ⚠️ 可选 | ✅ **已完成**：`laravel/sanctum`，后台 API 走 `/admin/api`，业务 API 走 `/api/v1`，统一响应结构 |
| **多语言** | ✓ 内置多语言支持 | ✓ Translatable 核心支持 | ❌ **第一版预留**：只预留语言包和后台文案扩展方式，第二版实现 |

#### 3.2.6 插件与生态

| 功能 | FastAdmin | Backpack | 我们的实现方式 |
|------|-----------|----------|---------------|
| **插件市场/中心** | ✓✓ **在线安装卸载升级**：已安装列表、本地安装、官方市场（分类筛选、购买、安装、配置、卸载、启用/禁用） | ❌ **无** | ✅ **已完成（核心特色）**：双轨制架构。工具型插件住 `vendor/`，业务模块型插件解包到 `Modules/`，本地插件中心 + 启停 + 配置已实现；远程市场与商业化在第二版 |
| **插件管理** | ✓ 插件名、版本、作者、状态。操作：安装、配置、卸载、启用/禁用 | ❌ 无 | ✅ **已完成**：`plugins` 表 + Filament Resource，插件声明配置项，自动生成配置页面 |

---

## 四、Phase 1 功能完成状态总结

| 状态 | 功能 |
|------|------|
| ✅ **已完成** | 后台登录（含 2FA/限速/防枚举）、登录日志（表+模型+监听器）、管理员管理（模型+迁移+工厂+种子+guard 配置）、角色权限（Spatie + Shield）、菜单管理（自定义 Menu 模型 + Resource）、操作日志（activitylog + filament-activity-log）、附件管理（medialibrary + filament 插件）、系统配置（spatie/settings + filament 插件）、统一 API 规范（Sanctum + 响应格式）、插件中心（双轨制 + 本地索引 + 启停 + 配置）、软删除（AdminUser 等核心模型）、基础导出 |
| ⚠️ **基础可用** | 定时任务（console.php 可用）、仪表盘（框架就位，业务统计待补）、CRUD 规范（make 命令可用，规范模板待沉淀） |
| ❌ **未开发** | 多语言（第一版预留）、个人资料 Resource UI、仪表盘统计卡片 |

---

## 五、差距量化评估

### 5.1 核心差距详解

1. **权限与菜单（差距已填平，且免费超越竞品）**：
   - **FastAdmin**：拥有完整的 RBAC 权限树，全部免费。
   - **Backpack**：权限管理是**付费插件** ($29/年)，菜单管理依赖代码路由，缺乏可视化配置。
   - **我们**：✅ 已集成 Spatie Permission + Filament Shield，**免费**提供 Backpack 收费的功能，这是对 Laravel 用户的巨大吸引力。

2. **系统配置与附件（差距已填平）**：
   - **FastAdmin**：提供强大的分组配置系统和附件管理，全部免费。
   - **Backpack**：系统配置是**付费插件** ($19/年)。
   - **我们**：✅ 已集成 `spatie/settings` 和 `spatie/media-library` 并提供 Filament 面板，再次以"免费完整版"打击 Backpack 的付费点。

3. **插件生态（核心差异化，已建立心智）**：
   - **FastAdmin**：拥有成熟的插件市场（在线安装、购买、配置），是其护城河。
   - **Backpack**：无插件市场概念。
   - **我们**：✅ 插件中心已建立，本地插件管理 + 启停 + 远程索引浏览已就位，心智预期已建立。第二版做在线安装和商业化。

4. **CRUD 体验（差距缩小）**：
   - **FastAdmin**：提供"一键 CRUD"生成器，包含 24 种表单组件演示，体验极佳。
   - **我们**：复用 Filament 官方 `make:filament-resource`，规范模板（基类、Trait、Policy 约定）已沉淀。差异化在规范而不在代码生成器。


---

## 八、首批开发插件规划

以下按 FastAdmin 插件市场下载量与业务刚需程度排序，结合双轨制架构（工具型 `package` / 业务型 `module`）规划首批插件：

| 插件名称 | 类型 | 核心功能 | 开发优先级 | 备注 |
|----------|------|----------|-----------|------|
| **云存储 (OSS/S3)** | 工具型 | 替换本地存储，支持阿里云/腾讯云/AWS S3，自动缩略图生成 | P0 | 任何项目必装，走 `vendor/` Composer 包 |
| **短信发送** | 工具型 | 阿里云/腾讯云短信接口，支持验证码、通知模板、发送记录 | P0 | 登录验证/订单通知刚需，配置即用的 Settings 类 |
| **Excel 导入导出** | 工具型 | 复杂表格数据导出/导入，支持自定义模板、批量处理、进度提示 | P0 | 运营人员高频使用，基于 `maatwebsite/excel` 封装 |
| **第三方登录** | 工具型 | 微信/QQ/微博 OAuth 登录后台或前台会员，支持账号绑定 | P1 | 降低注册门槛，走 `laravel/socialite` 封装 |
| **支付模块** | 工具型 | 微信支付/支付宝集成，支持扫码/H5/JSAPI，订单回调与对账 | P1 | 电商/知识付费项目刚需，配置化路由与回调处理 |
| **富文本编辑器** | 工具型 | UEditor / TinyMCE / Tiptap 集成，自动适配 Media Library 图片上传 | P1 | 内容管理/公告发布必备，替代原生 textarea |
| **会员管理扩展** | 业务型 | 前台会员等级/积分/余额管理，消费记录与权益分配 | P2 | 解包至 `Modules/Member`，代码归项目所有，方便二开 |
| **数据回收站增强** | 业务型 | 全局软删除数据查看、对比、批量恢复与彻底清理 | P2 | 跨模块通用能力，可作独立工具包或轻量业务模块 |

### 插件开发原则
1. **工具型优先**：P0/P1 插件全部采用 `package` 模式，安装后配置即用，不侵入业务代码。
2. **配置驱动**：所有插件必须声明自己的 `Settings` 类，插件中心自动渲染配置页，避免重复开发设置界面。
3. **权限接入**：插件注册的路由/Resource 必须接入 Filament Shield 权限体系，不得绕过角色检查。
4. **文档配套**：每个插件需包含 `README.md`（安装/配置/升级指南）与基础 Pest 测试，符合上架品质门槛。

---

## 九、第一版功能规范

### 9.1 需求总纲

希望沉淀一套对标 FastAdmin 的 Laravel + Filament 后台基础平台。专注提供后台系统的通用底座、权限体系、插件机制、快速开发规范和基础运维能力。

本项目主打开源生态，文档体系必须完备：安装记录、变更记录、定制说明都要清晰可查。核心是消除二次开发者的使用顾虑——让任何拿到本项目的开发者都能快速识别"框架在 Laravel / Filament 默认行为基础上做了哪些定制"，避免因不可知改动产生顾虑而放弃使用。

### 9.2 对标 FastAdmin 默认能力

- 权限管理：管理员、角色/分组、菜单规则、权限节点、按钮权限、数据权限思路。
- 后台菜单：后台导航、菜单规则、权限绑定、控制器/页面入口管理。
- CRUD 快速开发：复用 Filament 官方 Artisan 命令（`filament:make-resource` 等）生成 CRUD 骨架，配合本项目沉淀的规范模板（基类、Trait、Policy 约定）让开发者按规范快速新增业务模块。
- 插件中心：插件安装、卸载、启用、禁用、升级、配置和扩展入口。
- 附件上传：图片、文件上传，默认本地存储，云存储能力以插件形式扩展（OSS、S3 等）。
- 会员/API 基础：FastAdmin 有通用会员模块和 API 模块；本项目只做后台权限模块、后台管理员模块，不做前台通用会员和 API 模块。
- 多语言：FastAdmin 支持多语言；FilamentAdmin 第一版先不做复杂多语言，只预留语言包和后台文案扩展方式。
- 操作体验：后台表格、筛选、排序、批量操作、导入导出、软删除/回收站、状态切换等通用管理体验。
- 开发辅助：命令行工具、静态资源发布、插件资源发布等开发效率能力；API 文档生成暂不纳入第一版。

### 9.3 具体功能清单

- 后台登录与账号管理：支持后台登录、退出、修改密码、管理员账号创建、禁用和基础资料维护。
- 角色与权限管理：支持角色划分、权限分配、菜单可见范围控制、操作权限控制，后续可扩展数据权限。
- 后台菜单管理：支持后台菜单、分组、图标、排序、权限绑定和插件菜单接入。
- 系统配置管理：支持站点级基础配置、上传配置、邮件配置、短信配置、插件配置等统一配置入口；配置按模块分组，支持文本、开关、下拉、图片等多种配置类型；配置结果缓存，避免每次请求查库。
- 操作日志与登录日志：操作日志与登录日志分表存储；操作日志自动记录 Model 变更前后 diff；支持按天数上限定期自动清理日志，上限和定期清理同步配置。
- 附件与文件管理：支持图片、视频、附件等资源上传、分类、搜索、复用，第一版先做后台通用附件能力，默认本地存储。
- 通用 CRUD 规范：沉淀 Laravel Model、Migration、Seeder、Policy、Filament Resource、Form、Table、Action、Filter、Infolist、Widget 的组织规范，让后续业务模块可以快速复制。
- 软删除与回收站：为常见后台资源提供软删除、恢复、彻底删除等通用管理能力。
- 数据导入导出：第一版先支持基础 Excel/CSV 导出，导入能力按业务模块逐步接入。
- API 基础能力：提供统一 API 返回结构、鉴权边界、错误格式和资源输出规范，方便项目二、项目三读取数据；后台 API 路由与业务 API 路由分离，统一加 `/api/v1` 版本前缀。
- 插件中心管理：支持在后台查看已安装插件、插件状态、插件版本、插件说明、依赖要求、配置入口和启停状态。
- 插件安装与启停：支持通过 Composer 包或本地插件目录安装插件，后台可以启用、停用和查看插件运行状态。
- 插件配置管理：支持插件声明自己的配置项，并在 Filament 后台生成对应配置页面，避免每个插件都重复开发设置页。
- 插件菜单与权限接入：支持插件向后台注册菜单、页面、资源、权限点和操作入口，并能接入统一角色权限体系。
- 插件数据迁移与初始化：支持插件携带 Migration、Seeder、配置发布和资源发布能力，安装后可以完成必要的数据表和初始数据准备。
- 插件市场索引：第一阶段先支持读取本地或远程 JSON 索引，展示插件名称、分类、版本、作者、说明、兼容版本、安装命令和文档链接，为后续真正的市场化分发预留空间。

### 9.4 第一版边界声明

第一版不要一次性做成完整业务平台，而是先做"可安装、可登录、可扩展、可开发业务模块"的后台 MVP。

- 第一版优先实现：后台登录、管理员、角色权限、菜单管理、系统配置、操作日志、附件上传、软删除、基础导出、统一 API 规范、本地插件中心、插件配置入口、插件启停状态、插件索引展示。插件体系支持双轨制：工具型插件（OSS/短信/支付等）住 `vendor/`，业务模块型插件（商城/官网等）解包到 `Modules/`，两套机制统一在插件中心管理。
- 后台权限要区分两类：一类是 Laravel / Filament 后台管理员、运营、编辑、审核等角色权限；另一类是前台用户登录、访问受限内容、查看权益这类前台用户权限。两套权限不要混在一起设计。
- 扩展包只解决通用能力，不承担核心业务逻辑。
- 数据权限（按部门/数据范围行级过滤）第一版不实现，仅在 Resource 基类预留 `scopeQuery()` 方法约定，业务模块自行重写。
- 演示环境放到第二版规划，第一版不提供。

---

## 十、实现方案落地节奏

> 实施总原则：通用能力优先复用 Laravel / Filament 社区成熟方案，不重复造轮子；只有现有方案无法覆盖时才自研。

1. 使用 Laravel + Filament 创建 FilamentAdmin。Laravel 负责后台业务、数据模型、接口、权限、队列和邮件等服务端能力，Filament 负责快速搭建后台管理界面。
2. 先把 FilamentAdmin 定位成"后台基础平台"，不急着做官网、内容、运营这些业务模块，优先沉淀后台开发框架能力。
3. 补齐后台通用能力，包括后台登录、管理员、角色权限、菜单、系统配置、操作日志、附件上传、软删除、基础导入导出和统一 API 规范。
4. 沉淀通用 CRUD 开发规范，包括 Model、Migration、Seeder、Policy、Filament Resource、Form、Table、Action、Filter、Infolist、Widget 的组织方式。
5. 设计插件机制和插件中心。插件底层优先复用 Laravel Composer 包、Service Provider、Package Auto-Discovery、Migration、Config Publish 这些成熟机制；后台侧复用 Filament Plugin、Panel、Resource、Page、Widget 这些扩展点，不重新发明一套封闭插件系统。
6. 先实现插件中心 MVP，包括已安装插件列表、插件启停状态、插件配置入口、插件依赖说明、本地插件索引和远程插件索引读取。第一版不急着做在线购买、自动支付、复杂审核和完整云端分发。
7. 再补轻量定时任务、事件联动、队列任务和通知能力，让这套底座具备基础自动化能力，但先不在这一层把重队列和重异步系统做得太复杂。
8. 把这套项目整理成真正可以复制和复用的后台基础平台，作为项目二、项目三以及后续业务项目的共同起点。

---

> 以下技术规范章节已迁移至项目 `docs/` 目录，本文件不再维护重复内容：
>
> | 原章节 | 迁移目标 |
> |--------|---------|
> | 技术选型与版本规范 | `docs/architecture/tech-stack.md` |
> | 插件市场设计 | `docs/plugins/overview.md` |
> | 核心数据库表清单 | `docs/architecture/tech-stack.md` §核心数据库表清单 |
> | 核心扩展点清单 | `docs/development/base-classes.md` |
> | 项目目录结构总览 | `docs/architecture/directory-structure.md` |
> | 文档体系规划 | 已体现为 `docs/` 目录本身 |
> | 测试与质量门槛 | `docs/development/conventions.md` §测试与质量门槛 |
> | 业务错误码 | `docs/reference/error-codes.md` |

---

## 十一、第二版规划

### 11.1 定位

第一版完成"可安装、可登录、可扩展、可开发业务模块"的后台 MVP 之后，第二版目标是将 FilamentAdmin 从"能用"升级为"好用、好看、好维护"，并**完成代码市场的商业化闭环**——这是第一版预留、第二版兑现的核心承诺。

第二版重点：
1. **代码市场闭环**：异步安装、在线升级、商业化（开发者入驻、付费、License、分成）
2. **运维能力**：系统升级流水线、定时任务/队列可视化
3. **数据权限**：补齐第一版预留的行级数据过滤
4. **生态建设**：演示环境、官方云存储插件、通知系统

### 11.2 优先级排序

| 优先级 | 功能模块 | 理由 |
|--------|---------|------|
| 高 | 插件异步安装 + 在线升级 | 第一版最核心的体验缺口；代码市场闭环必需 |
| 高 | 插件商业化（开发者入驻 + 付费 + License + 分成）| 代码市场是两大核心特色之一，第二版必须形成完整闭环 |
| 高 | 数据权限体系 | 多部门项目的必要需求 |
| 高 | 系统升级流水线 | 第一版升级靠手动命令，体验断层最大；本项目定位"开箱即用"，升级也必须开箱即用（仅 Git 部署场景，其他场景做好引导） |
| 高 | 演示环境 | 开源生态建设，降低用户试用门槛 |
| 中 | 通知系统 | 提升系统可用性，站内通知优先 |
| 中 | 云存储官方插件 | 首批官方插件，验证插件机制（OSS / S3 / 七牛）|
| 中 | 定时任务 / 队列可视化 | 降低运维门槛 |
| 中 | API 文档生成 | 开发体验提升 |
| 低 | 多语言支持 | 国际化需求，视社区反馈决定优先级 |
| 低 | 安全增强 | 基础安全第一版已覆盖，高级场景按需补充 |

### 11.3 插件市场增强

#### 异步安装（队列 + 进程重载）

> **重要前置：进程重载需要部署侧配合**
>
> | 部署模式 | 是否支持自动 reload | 处理方式 |
> |---------|-------------------|---------|
> | 传统 PHP-FPM + Supervisor | 支持 | 项目提供 `filament-admin:reload` Artisan 命令调用预先在 `sudoers` 中授权的 `systemctl reload php-fpm`；安装任务完成后由队列 Worker 触发 |
> | Docker / Kubernetes | 不支持自动 reload | 安装完成后显示"请重启对应容器/Pod"提示，不尝试 `kill -USR2` 等花式重载 |
> | Laravel Octane / Swoole / FrankenPHP | 不支持自动 reload | 提示用户执行 `php artisan octane:reload`，并明确说明常驻进程模式下插件机制本身就需要重启 |
> | 共享主机 / 虚拟主机 | 不支持 | 第二版插件异步安装能力本就不适用此场景，UI 中检测到此环境时只提示"使用 Composer 命令安装" |
>
> 这一段约束必须在第二版安装界面用户操作前显式展示，避免用户期望落差。

- 用户在后台点击"安装"，推送安装任务到队列
- **工具型插件**：Worker 执行 `composer require vendor/plugin`，通过 `symfony/process` 调用系统级 Composer 二进制（非 Web 进程直接调用），完成后根据部署模式判断是否触发自动 reload
- **业务模块型插件**：Worker 下载 ZIP 包，解压到 `Modules/` 目录，执行 `composer dump-autoload`，更新 `modules_statuses.json`
- 安装进度通过 Livewire 实时推送到前端，显示安装日志
- 安装失败时回滚，记录错误日志并通知管理员

#### ZIP 包离线安装

- 支持后台上传插件 ZIP 包（适合无 SSH 访问、无法直接执行 Composer 的场景）
- **工具型插件 ZIP**：解压到临时目录，追加到 `composer.json` 的 `repositories` 配置（`path` 类型），执行 `composer require` 完成安装
- **业务模块型插件 ZIP**：直接解压到 `Modules/` 目录，执行 `composer dump-autoload`，代码归项目所有
- 适合私有插件、内网部署场景

#### 插件在线升级

- 后台自动对比已安装版本与市场最新版本
- 一键升级：推送升级任务到队列，执行 `composer update vendor/plugin`
- 升级前自动备份当前配置，升级后执行 `php artisan migrate`
- 升级历史记录，支持回滚到上一版本

#### 私有 Packagist / Satis 支持

- 支持配置私有插件源（企业内网部署场景）
- 允许企业发布内部插件到私有 Satis，通过市场索引分发给内部项目

### 11.4 系统升级能力

**后台版本检查**：
- 系统设置页展示当前 FilamentAdmin 版本
- 定期（或手动触发）检查 GitHub Release 是否有新版本，展示更新说明
- 区分小版本更新（可自动升级）和大版本更新（需手动参考升级文档）

**一键升级流水线（仅适用于 Git 部署场景）**：

> **适用边界**：本流水线假设服务器上有完整的 Git 工作目录且 PHP 进程有权限执行 `git pull` / `composer install`。以下场景**不适用**，需要走对应的部署侧升级流程：
> - **Docker 镜像部署**：升级 = 拉新镜像 + 重启容器，与本流水线无关
> - **Composer Artifact 部署**（CI 打包 zip 上传）：升级走 CI/CD 流水线
> - **共享主机 / 受限主机**：通常没有 SSH 和 git，只能手动 `composer update` 或上传文件
> - **Octane / Swoole 常驻进程**：升级后必须 `octane:reload`，本流水线最后追加这一步
>
> 后台升级 UI 在启动前自动探测部署环境（`.git/` 是否存在、`composer` 是否可执行、是否运行在 Octane 模式），无法满足前提条件时禁用一键升级按钮并提示原因。

基于 `salahhusa9/laravel-updater` 思路实现：
1. 开启维护模式（`php artisan down`）
2. Git 拉取最新代码
3. 执行 `composer install --no-dev`
4. 执行 `php artisan migrate --force`
5. 清理缓存（`config:cache`、`route:cache`、`view:cache`）
6. 如运行在 Octane 模式，追加 `php artisan octane:reload`
7. 关闭维护模式（`php artisan up`）

**升级文档规范**：每个大版本（SemVer 主版本号变更）必须更新 `docs/guide/upgrading.md`，文档内容：Breaking Changes 列表、需要执行的命令、配置变更项、废弃方法替代方案。

### 11.5 数据权限体系

**数据权限模型**：
- **全部数据**：可查看所有记录（超级管理员、管理员）
- **本部门数据**：只能查看本部门成员创建的记录
- **仅本人数据**：只能查看自己创建的记录
- 在角色上配置数据权限范围，叠加在操作权限（CRUD）之上

**实现方式**：
- 在 Resource 基类的 `scopeQuery()` 中根据当前用户角色的数据权限配置自动注入过滤条件
- 支持通过 `DataScope` Trait 快速为 Model 接入数据权限过滤
- 数据权限配置 UI 集成到角色管理页面

**部门/组织架构（可选）**：
- 如果业务涉及部门概念，提供轻量的部门树管理（无限极分类）
- 管理员可以绑定所属部门，作为数据权限过滤的依据

### 11.6 通知系统

**站内通知**：
- 基于 Laravel Notification + Filament 内置 DatabaseNotification
- 后台顶部通知铃铛，未读数徽章
- 通知列表：标题、摘要、时间、跳转链接、已读/未读状态
- 支持标记全部已读、清空通知

**邮件通知**：
- 复用 `MailSettings` 中的 SMTP 配置
- 提供通用邮件模板（可自定义 Logo、主题色）
- 支持后台触发"发送测试邮件"验证配置是否正确
- 关键系统事件（升级失败、插件安装失败、登录异常）自动发邮件给超级管理员

**短信/webhook 通知（插件形式）**：
- 短信通知以插件形式提供（阿里云短信插件、腾讯云短信插件）
- 支持 Webhook 通知：系统事件触发时向配置的 URL 推送 JSON 数据

**通知模板管理**：
- 后台可以编辑通知模板的文案（变量占位符支持）
- 区分系统级通知（不可关闭）和业务级通知（用户可选择是否接收）

### 11.7 定时任务与队列可视化

**定时任务管理**：
- 后台展示 `routes/console.php` 中注册的所有定时任务
- 显示任务名称、执行频率、上次执行时间、上次执行结果
- 支持手动触发单次执行
- 任务执行日志记录（成功/失败/耗时）

**队列监控**：
- 集成 `laravel/horizon`（Redis 队列）或展示基础队列状态
- 展示队列积压数、处理速度、失败任务列表
- 失败任务支持重试和删除
- 队列 Worker 运行状态监控

### 11.8 多语言支持

- 后台界面语言切换（中文/英文，管理员个人设置）
- 系统文案支持语言包覆盖（插件也可携带自己的语言包）
- 后台提供语言包管理页面（列出所有翻译 key，支持在线修改）
- 插件开发规范要求所有用户可见文案必须走翻译函数，不硬编码中文

### 11.9 附件云存储官方插件

- **阿里云 OSS 插件**（`filament-admin/oss-plugin`）：配置 AccessKey、Bucket、域名，medialibrary 自动切换到 OSS 磁盘
- **AWS S3 插件**（`filament-admin/s3-plugin`）：配置 Region、Bucket、AccessKey，标准 S3 协议兼容七牛、腾讯 COS 等
- 每个云存储插件包含：连接配置 UI、连接测试、迁移工具（本地文件批量上传到云端）

### 11.10 API 增强

**API 文档生成**：
- 集成 `knuckleswtf/scribe`，自动从路由和注释生成 API 文档
- 文档页面内置在后台（`/admin/api-docs`），需要管理员权限访问
- 支持在文档页面直接发起测试请求

**Webhook 支持**：
- 系统关键事件（用户注册、订单完成等）支持向外部系统推送 Webhook
- 后台配置 Webhook 订阅：URL、签名密钥、订阅事件类型
- Webhook 发送日志：成功/失败、响应码、重试记录

**API v2 路由**：
- 当有 Breaking Change 时启用 `/api/v2` 路由
- v1 路由继续维护一段时间（至少一个大版本周期），添加弃用警告响应头
- 提供 v1→v2 迁移指南

### 11.11 演示环境

- 提供在线 Demo 站，展示 FilamentAdmin 开箱即用的全部功能
- Demo 账号分级：超级管理员（只读）、管理员、运营、编辑、审核各一套
- Demo 数据定期重置（每天或每小时）
- Demo 环境禁用破坏性操作（删除、清空日志等）

### 11.12 安全增强

**文件上传安全**：
- 上传文件类型白名单校验（MIME 类型 + 扩展名双重校验）
- 上传文件大小限制（后台可配置）
- 图片文件二次渲染（防止图片马攻击）

**登录安全增强**：
- IP 登录白名单（超级管理员可配置允许登录的 IP 段）
- 异地登录提醒（与上次登录 IP 不同时发邮件通知）
- 登录设备记录（UA 解析，展示设备类型和浏览器）

**操作审计增强**：
- 关键操作（删除、权限变更、配置修改）要求二次确认（密码验证或 2FA 验证码）
- 审计日志不可删除（超级管理员也不行），只能归档

### 11.13 插件商业化（第二版正式启动）

> **定位说明**：插件代码市场是 FilamentAdmin 的两大核心特色之一。商业化是市场闭环的必经环节，因此从"第三版预研"提升为"**第二版正式启动**"。第一版已预留 `license` / `price` / `vendor_id` 等字段位，第二版直接启用这些预留字段，不存在不兼容风险。

**开发者生态建设**：
- **插件开发者入驻流程**：GitHub OAuth 登录 + 实名认证；提交包名、所属组织、收款信息
- **官方审核机制**：
  - 自动化：CI 跑 Pest 测试 + Larastan level 5 + composer audit + 危险函数扫描
  - 人工：核对包名是否冲突、`composer.json` 元数据合规性、README 完整性、首版人工复核代码
- **插件上架标准**：必须遵循第一版定义的"插件开发品质门槛"，且通过自动+人工双审核
- **开发者评级**：根据插件下载量、好评率、issue 响应速度形成开发者信用分，影响后续上架速度

**付费插件机制**：
- **定价模式**：免费 / 一次性买断 / 按年订阅
- **授权校验**：License Key 机制
  - 安装时填入 Key，插件 ServiceProvider boot 时向官方授权服务器验证
  - 允许 7-30 天离线缓存，断网不影响使用
  - 校验失败时插件进入"只读"模式，不影响主项目运行
- **退款政策**：买断插件 7 天无理由退款（首次购买，每个账号每个插件限一次）
- **分成结算**：
  - 平台抽成比例：**初定 5%**（远低于 App Store 30%），目的是早期最大化吸引开发者入驻；后续根据平台成本与生态成熟度可上调，调整需提前 90 天公告，已上架插件按签订时的费率执行历史订单
  - 结算周期：月结，T+15
  - 提现方式：支付宝 / 微信 / 对公账户

**分发机制**：
- **免费插件**：走 Packagist 公开分发，市场仅提供索引和元数据
- **付费插件**：走官方私有 Packagist（基于 Satis 自建），用户购买后获得授权访问 token
- **版本管理**：插件支持多版本并存（按 FilamentAdmin 主版本锁定兼容范围）；用户购买后可下载所有兼容当前 FilamentAdmin 主版本的更新

**法律与合规**（第二版必须配齐，否则不能上线收款）：
- 用户协议：明确"代码归买家所有 + 可二开 + 不可二次分发"
- 开发者协议：明确"提交即授权平台分发 + 收入分成约定 + 内容合规要求"
- 备案：根据团队所在地决定 ICP 备案、增值电信业务许可证（如需要）

---

## 附录：后台 UI 调整参考

Filament 官方没有类似 WordPress 那样"一键换肤"的主题包，社区里也没有能把 Filament 变成"Layui/Element UI 风格"的成熟国产主题包。这主要是由技术栈决定的：Filament 基于 Tailwind CSS，它的设计理念是"配置化定制"（改颜色、字体、圆角），而不是"替换模板"。

### 审美差异

国人习惯的后台（如 Layui、Vue Element Admin、FastAdmin）通常是 Bootstrap 风格：边框明显、信息密度高、按钮多、色彩重（深蓝/深黑侧边栏）。Filament 风格是典型的欧美 SaaS 风格：极简、大留白、圆角大、默认是浅色侧边栏（虽然可以改深色）、信息密度较低。

### 现有美化相关包

| 包名 | 功能 | 评价 |
|------|------|------|
| `hasnayeen/themes` | 提供侧边栏颜色切换、深色模式切换、布局调整 | 最常用。可以让侧边栏变黑/变蓝，增加一点"传统后台"的感觉 |
| `filament/filament` (自带) | 官方支持自定义 Brand Logo、Favicon、Colors、Font | 必须用。把主色调改成国人喜欢的"阿里蓝"或"微信绿"，字体改成微软雅黑 |
| `swisnl/filament-backgrounds` | 登录页背景图/粒子效果 | 仅美化登录页，让登录看起来不那么素 |

### 贴合国人习惯的调整建议

1. **修改侧边栏为深色（最重要）**：国人后台 90% 是深色侧边栏。Filament 5 支持得很好，在 AdminPanelProvider 中配置 `->darkMode()`、`->sidebarCollapsibleOnDesktop()` 等。
2. **调整主色调**：默认是蓝色，但可以改成更符合国内审美的颜色（通过 Tailwind 配置）。
3. **增加信息密度**：Filament 表格默认行高较大，可以通过自定义 CSS 稍微压缩行高，让一屏显示更多数据。
4. **替换特定组件**：富文本集成 tiptap-editor（Filament 官方推荐）；省市区级联使用 `filament-china-region` 之类的第三方包。

> 建议：不要试图把 Filament 改成 Layui 的样子，那是跟 Tailwind 的底层逻辑对抗。花半天时间把侧边栏改成深色、换上项目 Logo、调好主色调，它看起来就会像一个成熟的商业产品了。
