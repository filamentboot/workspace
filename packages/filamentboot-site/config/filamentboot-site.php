<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 前台路由挂载模式
    |--------------------------------------------------------------------------
    |
    | prefix：官网挂在子路径下（默认，宿主已有前台业务时的安全模式），如 /site/about
    | root  ：官网接管根路径（项目本身就是官网时启用），如 /about
    | domain：官网绑定独立域名或子域名，如 www.example.com/about
    |
    | 默认不抢占宿主根路由。需要官网接管根路径时，在 .env 中设置
    | SITE_ROUTE_MODE=root。
    |
    */
    'route' => [
        'mode' => env('SITE_ROUTE_MODE', 'prefix'),

        /*
        | prefix 模式下的路径前缀，仅在 mode=prefix 时生效。
        */
        'prefix' => env('SITE_ROUTE_PREFIX', 'site'),

        /*
        | domain 模式下绑定的域名，仅在 mode=domain 时生效。
        | 未配置时自动降级为 prefix 模式，避免注册出无主机名的路由。
        */
        'domain' => env('SITE_ROUTE_DOMAIN'),

        /*
        |----------------------------------------------------------------------
        | 保留 slug
        |----------------------------------------------------------------------
        |
        | 动态页面路由 /{slug} 不得吞掉的固定路径。root 模式下尤其重要：
        | 没有这份清单，/admin、/sitemap.xml 等会被当作页面 slug 解析。
        |
        */
        'reserved_slugs' => [
            'admin',
            'api',
            'livewire',
            'storage',
            'sitemap.xml',
            'sitemap-content.xml',
            'sitemap-city.xml',
            'robots.txt',
            'llms.txt',
            'preview',
            'login',
            'logout',
            'search',
            'downloads',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 爬虫抓取统计
    |--------------------------------------------------------------------------
    |
    | filamentboot-site:crawler-stats 默认读取的访问日志。
    |
    | 站长平台的抓取数据滞后好几天，访问日志不滞后——放开收录之后想立刻知道
    | 「爬虫来没来、抓到的是 200 还是 404」，只能看它。AI 抓取器更是如此，
    | 那几家都不提供站长平台。
    |
    | 需要 nginx combined 格式。留空则必须用 --log= 指定。
    |
    */
    'crawler_stats' => [
        'access_log' => env('SITE_ACCESS_LOG', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | 站内搜索
    |--------------------------------------------------------------------------
    */
    'search' => [
        /*
        | 是否统计搜索词。
        |
        | 记的是「词 + 次数 + 最近一次结果条数」，**不记 IP、UA 与任何身份信息**，
        | 因此不构成个人信息收集。默认开启：访客亲手打进去的词是全站最真实的
        | 关键词需求来源，而结果为 0 的词直接就是内容缺口清单。
        |
        | 关掉只是不再写库，搜索功能本身不受影响。
        */
        'log_terms' => env('SITE_SEARCH_LOG_TERMS', true),
    ],

    /*
    |--------------------------------------------------------------------------
    | robots.txt
    |--------------------------------------------------------------------------
    |
    | 宿主 public/robots.txt 若存在会被 Web 服务器优先返回，本段随之失效——
    | 需要动态 robots 时要把那个静态文件删掉。
    |
    */
    'robots' => [
        /*
        | 通用段（User-agent: *）的 Disallow 路径。
        */
        'disallow' => [
            '/admin',
            '/livewire',
        ],

        /*
        |----------------------------------------------------------------------
        | AI 抓取器策略
        |----------------------------------------------------------------------
        |
        | allow     为下列 UA 单独开一组，规则与通用段相同（可抓正文、不可抓后台）
        | disallow  为下列 UA 单独开一组并整站禁止
        | omit      不输出 AI 段，行为回到「只有通用段」
        |
        | 默认 allow：它与「只有通用段」的**实际效果完全一致**（通配符本来就覆盖
        | 这些 UA），差别只在把立场写明。要真正拒绝就改成 disallow。
        |
        | ⚠️ 生成式引擎能不能引用你，前提是它的抓取器进得来。关掉等于放弃那条线，
        | 而抓取压力应该在 Web 服务器层限速解决，不是在这里一关了事。
        |
        */
        'ai_crawlers_policy' => env('SITE_ROBOTS_AI_POLICY', 'allow'),

        /*
        | AI 抓取器 UA 清单。
        |
        | Bytespider 字节（豆包）／PetalBot 华为（小艺）／Google-Extended 谷歌
        | （Gemini 训练与 AI 概览，**只影响 AI 用途，不影响搜索收录**）／
        | 其余为海外主流。国内三家里只有字节走公网抓取，另两家的检索源在各自
        | 的封闭生态里，robots 放不放它们都进不来。
        */
        'ai_crawlers' => [
            'Bytespider',
            'PetalBot',
            'GPTBot',
            'OAI-SearchBot',
            'ChatGPT-User',
            'ClaudeBot',
            'PerplexityBot',
            'Google-Extended',
            'Applebot-Extended',
            'CCBot',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 可选前台主题
    |--------------------------------------------------------------------------
    |
    | 键为主题目录名（resources/views/themes/{key}），值为后台显示名称。
    | 列在这里的主题必须覆盖控制器会请求的全部视图，否则前台会 500。
    | 白名单同时用于防止 active_theme 被写入非法值造成目录穿越。
    |
    */
    'themes' => [
        'decoration' => '科技装修（浅色）',
        'software'   => '软件产品（浅色）',
    ],

    /*
    |--------------------------------------------------------------------------
    | 默认主题
    |--------------------------------------------------------------------------
    |
    | 站点设置未配置或配置了非法主题时的兜底值，必须存在于上面的白名单中。
    |
    */
    'default_theme' => 'decoration',

    /*
    |--------------------------------------------------------------------------
    | 前端资源入口
    |--------------------------------------------------------------------------
    |
    | 主题 CSS 的 Vite 入口候选路径，{theme} 会被替换为当前主题目录名。
    | 按顺序在 Vite manifest 中查找，命中哪个用哪个：
    |
    |   1. 真实 Composer 安装（宿主把 vendor 路径加入 vite.config.js input）
    |   2. 宿主执行 vendor:publish --tag=filamentboot-site-assets 之后
    |   3. monorepo path 仓库（vendor/ 为符号链接，Vite 记录真实路径）
    |
    | 宿主用其它路径组织资源时，在此追加或替换即可。
    |
    */
    'assets' => [
        /*
        | 前台脚本入口（#29）。只有一份、不随主题变，所以模板里没有 {theme}。
        |
        | 它交付的是 Alpine：#29 之前 Alpine 是 Livewire 注入的 livewire.js 捎带进来的，
        | 而那个 script 标签带 data-csrf → 起 session → 公开页必然带 Set-Cookie。
        | 宿主必须把命中的那条路径加进 vite.config.js 的 input 并装 alpinejs，
        | 否则前台所有 x-data 都不工作（导航抽屉、询盘面板、二级下拉、图集轮播）。
        */
        'script_entries' => [
            'vendor/filamentboot/filamentboot-site/resources/js/site.js',
            'resources/js/vendor/filamentboot-site/site.js',
            'packages/filamentboot-site/resources/js/site.js',
        ],

        'vite_entries' => [
            'vendor/filamentboot/filamentboot-site/resources/css/themes/{theme}.css',
            'resources/css/vendor/filamentboot-site/themes/{theme}.css',
            'packages/filamentboot-site/resources/css/themes/{theme}.css',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 公开页缓存
    |--------------------------------------------------------------------------
    |
    | 内容页响应头 Cache-Control: public, max-age=N 的 N（秒）。设 0 或负数即关闭。
    |
    | 内容改动后旧页面最多再存活这么久。官网内容改动频率低，十分钟换取整页缓存划算；
    | 要更实时就调小，或在 CDN 侧配主动刷新。
    |
    | ⚠️ Cms\Routing\SiteCacheHeaders 只在响应确实没有 Set-Cookie 时才打这个头。
    | 一旦某个页面意外起了 session，它会退回不缓存——把带会话 Cookie 的响应标成
    | 公共可缓存，共享缓存会把一个访客的会话发给另一个。
    |
    */
    'cache' => [
        'public_max_age' => env('SITE_PUBLIC_MAX_AGE', 600),
    ],

    /*
    |--------------------------------------------------------------------------
    | 询盘
    |--------------------------------------------------------------------------
    */
    'contact' => [
        /*
        | 转化入口标识与中文名的映射，键即前台 CTA 的 data-contact-trigger 取值。
        |
        | 只影响后台展示：未列在这里的来源仍会正常入库，后台按原始 key 显示，
        | 因此新增 CTA 时忘了登记也不会导致询盘筛不出来。
        */
        'sources' => [
            'floating'        => '悬浮按钮',
            'mobile-bar'      => '移动端操作条',
            'hero'            => '首屏 Banner',
            'nav-desktop'     => '导航栏',
            'nav-mobile'      => '移动端菜单',
            'home-cta'        => '首页 CTA',
            'product-card'    => '产品卡片',
            'product-detail'  => '产品详情页',
            'solution-detail' => '方案详情页',
            'case-detail'     => '案例详情页',
            'news-detail'     => '资讯详情页',
            'city-detail'     => '城市页',
            'page-cta'        => '页面 CTA 区块',
            'landing-header'  => '落地页头部按钮',
            'search-empty'    => '搜索无结果',
            'gated-download'  => '资料索取',
            'package-empty'   => '套餐列表空态',
        ],

        /*
        | 来源标识的**前缀**映射，全等表（上面的 sources）没命中时按前缀匹配，
        | 渲染成「前缀名 · 后缀」。
        |
        | 存在的理由：有些 CTA 是**记录级**的，值的个数等于内容条数——
        | 城市页三百多个，一条条登记进上面那张表既不现实，也会在每次发布
        | 新城市时留下「忘了加就退化成一串英文」的坑。
        |
        |   city-{区划代码}  →  城市页 · 武汉      （后缀会去 site_regions 查真名）
        |   pkg-{slug}       →  套餐页 · villa-deluxe
        |
        | 匹配是**按声明顺序取第一个命中**，所以别把 `city-` 和 `city-d` 这种
        | 互为前缀的键放在一起。解析细节见 Services\ContactSourceLabel。
        */
        'source_prefixes' => [
            'city-' => '城市页',
            'pkg-'  => '套餐页',
        ],

        /*
        | 悬浮询盘面板的额外问题。
        |
        | 面板（shared/components/floating-contact）是全站唯一一份，导航栏、移动端
        | 操作条、各详情页的 CTA 都开它——所以**大部分线索都从这里来**。而在此之前
        | 它只能问姓名 / 电话 / 留言：`$extraFields` 只有页面里内嵌的询盘表单区块
        | 会传，面板拿不到，销售回访时手上没有任何上下文。
        |
        | 形状与 Cms\Blocks\ContactFormBlock 的「额外问题」完全一致，解析也复用
        | 它的 normalizedFields()——两处各写一份归一逻辑迟早漂移：
        |
        |   ['label' => '户型', 'type' => 'select', 'required' => false,
        |    'options' => "两室一厅\n三室一厅"]
        |
        | type 取 text / textarea / select；select 的 options 是「一行一个」的字符串。
        | 配错的条目会被 normalizedFields() 静默丢掉（宁可少一个问题，也不显示一个
        | 选不了的下拉），所以改完请到前台点开面板确认。
        |
        | 默认空：**这是站点内容，不是包的行为**。问什么问题取决于这家公司卖什么，
        | 包里写死「户型 / 面积 / 装修阶段」对一家做工业设备的下游就是噪音。
        |
        | ⚠️ 每多问一个问题转化率就掉一截。只问真正会改变跟进动作的那几个。
        */
        'panel_fields' => [],

        /*
        | 每 IP 的提交次数上限与窗口（秒）。见 Services\ContactSubmission 的常量注释。
        |
        | 默认 3 次 / 5 分钟。**站上转化钩子越多越该放宽**：钩子按上下文铺开之后，
        | 同一个真实访客在一次浏览里提交两三次是正常行为，被挡掉就是白丢线索，
        | 而且前台把「提交过于频繁」渲染在电话字段下面，读起来像号码有问题。
        */
        'rate_limit'  => env('SITE_CONTACT_RATE_LIMIT', 3),
        'rate_window' => env('SITE_CONTACT_RATE_WINDOW', 300),
    ],

    /*
    |--------------------------------------------------------------------------
    | 资料索取（gated content）
    |--------------------------------------------------------------------------
    |
    | 「手册换联系方式」：访客提交询盘后才拿到一条限时签名下载链接。
    |
    | ⚠️ disk **必须是非公开磁盘**。默认 local 指向 storage/app，在 Web 根之外，
    | 只能经 Http\Controllers\GatedDownloadController 下发。改成 public 会让文件
    | 多出一个人人可猜的 /storage/... 地址，这道门就形同虚设——而且不会有任何报错，
    | 表现只是「留资率莫名很低」。
    |
    | link_ttl 是下载链接的有效分钟数。给得短是因为链接可能被转发：
    | 三十分钟够一次正常下载，又不足以当成长期分发地址。
    |
    */
    'gated' => [
        'disk'     => env('SITE_GATED_DISK', 'local'),
        'link_ttl' => env('SITE_GATED_LINK_TTL', 30),
    ],

    /*
    |--------------------------------------------------------------------------
    | 发布前健康检查
    |--------------------------------------------------------------------------
    |
    | Services\SiteHealthCheck 逐项核站点设置，把「配置缺失导致前台静默少一块」
    | 变成后台看得见的待办。默认必填的八项写在那个类里。
    |
    | `optional_fields` 是**明确决定不填**的字段名单：列进来的项不再计入缺失，
    | 检查结果因此可以真的变绿。
    |
    | 为什么要有这个开关：不是每家公司都该填每一项。没有对外办公地址的团队、
    | 不做线下接待的品牌，`address_zh` 填了才是编。而一个**永远红着一格**的检查器
    | 比没有检查器更糟——它训练人忽略告警，下次真缺了 og:image 也没人看。
    |
    | ⚠️ 列进来之前先确认那是个决定，不是拖延。
    |
    */
    'health' => [
        'optional_fields' => [],
    ],

    /*
    |--------------------------------------------------------------------------
    | 地图嵌入
    |--------------------------------------------------------------------------
    |
    | 地图区块（Cms\Blocks\MapBlock）允许的 iframe 宿主域名。作者只填嵌入地址，
    | iframe 由前台视图自己拼——不接受整段 HTML，那等于在页面里开任意标签入口。
    |
    | 精确匹配、区分不了子域名：写 map.baidu.com 就只放行它。用「以某域名结尾」
    | 会被 map.baidu.com.evil.com 绕过，写 .baidu.com 又把整棵域名树都放进来。
    | 换服务商时在这里加一行，比放宽匹配规则安全得多。
    |
    | 只放行 https：http 的 iframe 在 https 页面上被当混合内容直接拦掉，
    | 放进来只会让作者以为是我们的 bug（判断在 Support\MapEmbed 里）。
    |
    */
    'map' => [
        'allowed_hosts' => [
            // 百度地图（国内主战场，「地图生成器」产出的 src）
            'map.baidu.com',
            'api.map.baidu.com',
            'j.map.baidu.com',
            // 高德
            'm.amap.com',
            'uri.amap.com',
            'www.amap.com',
            // 腾讯位置服务
            'apis.map.qq.com',
            'map.qq.com',
            // Google Maps（海外副线）
            'www.google.com',
            'maps.google.com',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | SEO
    |--------------------------------------------------------------------------
    */
    'seo' => [
        /*
        | 站点设置未填写默认描述时的最终兜底，确保 meta description 永不为空。
        */
        'fallback_description' => env(
            'SITE_SEO_FALLBACK_DESCRIPTION',
            '智能家居整体解决方案提供商，提供全屋智能设计、设备选型、施工落地与售后服务。'
        ),

        /*
        |----------------------------------------------------------------------
        | canonical 需剥离的查询参数
        |----------------------------------------------------------------------
        |
        | canonical 必须保留 page 等真正区分内容的参数，只剥掉广告与统计平台
        | 附加的追踪参数。此前 canonical 直接取 url()->current()（不含查询串），
        | /solutions?page=2 的 canonical 指向 /solutions，等于告诉搜索引擎
        | 列表页第 2 页往后都是第 1 页的副本，深层内容不会被索引。
        |
        | 宿主接入其它投放渠道时在此追加对应参数名即可。
        |
        */
        'canonical_ignored_params' => [
            'utm_source',
            'utm_medium',
            'utm_campaign',
            'utm_term',
            'utm_content',
            'gclid',
            'fbclid',
            'msclkid',
            'yclid',
            'bd_vid',
            '_bd_vid',
            'spm',
        ],

        /*
        |----------------------------------------------------------------------
        | 服务覆盖范围（schema.org areaServed）
        |----------------------------------------------------------------------
        |
        | 填了才输出，留空则相关节点里不出现这个属性——结构化数据宁可缺字段，
        | 也不该出现空值。
        |
        | 它解决的是「服务范围比经营场所大」这类站点的表达问题：声明服务到哪，
        | 而**不伪造本地存在**。没有实体经营场所就不该用 LocalBusiness——那个
        | 类型要求 address，填了等于编一个不存在的地址。
        |
        | area_served_type 取 schema.org 的地理类型：Country / State / City /
        | AdministrativeArea。默认 Country，对应「全国接单」这种最常见的情形。
        |
        | ⚠️ 宿主已发布 config/filamentboot-site.php 时**不要**在那边覆盖整个 seo
        | 段——mergeConfigFrom 是浅合并，写一个 'seo' => [...] 会把本段其余键
        | 全顶掉。要改值走下面这两个环境变量。
        |
        */
        'area_served'      => env('SITE_SEO_AREA_SERVED'),
        'area_served_type' => env('SITE_SEO_AREA_SERVED_TYPE', 'Country'),

        /*
        | 站点地图单次输出的每类内容上限，防止内容量大时超时。
        */
        'sitemap_limit' => env('SITE_SITEMAP_LIMIT', 2000),

        /*
        | /llms.txt 每类内容的条数上限。刻意远小于 sitemap_limit：
        | llms.txt 是给模型看的简明目录，全量输出就退化成第二个 sitemap 了。
        */
        'llms_limit' => env('SITE_LLMS_LIMIT', 50),
    ],

    /*
    |--------------------------------------------------------------------------
    | 页面版式
    |--------------------------------------------------------------------------
    |
    | 键为 site_pages.template 的存储值，值为后台 Select 显示名称。
    |
    | default 走 themes/{theme}/pages/show.blade.php；其它键由控制器解析成
    | themes/{theme}/pages/templates/{key}.blade.php，视图不存在时回退 show——
    | 一个模板缺失不该让已发布页面 404，也不该要求两套主题必须同步支持。
    |
    | landing 是 #28 交付的落地页极简版式：不 extends layouts.app，因此没有导航栏
    | 与完整页脚，出路收敛到询盘面板一个动作上。两套主题各有一份
    | pages/templates/landing.blade.php，并在各自的 theme.php 清单里声明支持。
    |
    | ⚠️ 新增版式必须同步改两套主题的 theme.php，否则主题切换预检查
    | （Cms\Themes\ThemeSwitchCheck）会把已支持的版式报成不支持。
    |
    */
    'page_templates' => [
        'default' => '标准页面',
        'landing' => '落地页（无导航，单一转化目标）',
    ],

    /*
    |--------------------------------------------------------------------------
    | 前台导航菜单
    |--------------------------------------------------------------------------
    */
    'menu' => [
        /*
        | route 型菜单项可指向的命名路由白名单，键为路由名、值为后台显示名称。
        |
        | 用白名单而不是让作者自由填路由名：route() 对未知名称会抛异常，
        | 而导航组件在每个页面都渲染——一个填错的路由名会让全站白屏。
        | 白名单同时让后台能给出下拉而不是让人背路由名。
        |
        | 宿主新增前台列表页时在此追加即可。
        */
        'allowed_routes' => [
            'site.home'            => '首页',
            'site.cases.index'     => '案例列表',
            'site.solutions.index' => '方案列表',
            // 套餐列表 3.5 期 A 段补：各主题的 nav / footer 兜底数组里一直有这一项，
            // 白名单里却没有——运营在后台配不出前台自己就在显示的入口。
            // 漏了不报错，只是这一项被 MenuResolver 静默丢掉。
            'site.packages.index' => '套餐列表',
            'site.products.index' => '产品列表',
            'site.news.index'     => '资讯列表',
            'site.city.index'     => '服务城市',
        ],
    ],

    /*
    |--------------------------------------------------------------------------
    | 城市页
    |--------------------------------------------------------------------------
    */
    'cases' => [
        /*
        |----------------------------------------------------------------------
        | 案例页声明
        |----------------------------------------------------------------------
        |
        | 一句话，渲染在案例详情页的正文之前。**默认空，什么都不渲染。**
        |
        | 存在的理由：「装修案例 + 现场照片 + 业主评价」这个组合本身就在向访客声称
        | 「这是我们交付过的一单」。真是的当然不用解释，**不是的就必须说**——访客
        | 没有别的线索可以判断。常见的两件事：配图是不是施工现场实拍、正文写的是
        | 具体一单还是这个户型的典型做法。
        |
        | 但这两件事都是**站点事实**，不是包的事实：另一家用这个包的公司可能每张
        | 都是自己拍的、每篇都对应真实工单，那就该留空。所以包只提供位置，
        | 话由宿主写。
        |
        | 只收纯文本（视图 e() 转义），不要写 HTML。
        |
        */
        'disclaimer' => '',
    ],

    'city_pages' => [
        /*
        |----------------------------------------------------------------------
        | 城市概况字段表
        |----------------------------------------------------------------------
        |
        | `site_city_pages.profile` 那个 JSON 列里能放哪些字段，由这份声明决定。
        | **后台表单与前台模板都从它生成**，所以包本身不认识「气候类型」「供暖方式」
        | 这些词——那是装修行业的口径，换个行业装这个包，字段表整个不一样。
        |
        | 默认空：没配就是没有概况表，城市页照样成立（标题 + 简介 + 下辖区县 +
        | 同省城市）。
        |
        | 每条形如：
        |
        |   ['key' => 'heating', 'label' => '供暖方式', 'type' => 'select',
        |    'options' => ['集中供暖', '自采暖', '混合'],
        |    'unit' => '', 'help' => '影响地暖与温控器选型']
        |
        | key    只允许小写字母、数字与下划线（它会拼进表单字段名与 JSON 键）
        | type   text / number / textarea / select，select 必须给 options
        | unit   显示在值后面的单位，如「℃」「%」
        | help   后台字段下方的说明，前台不显示
        |
        | 配错的条目会被 Modules\Corporate\Cities\CityProfile 静默丢掉——
        | 宁可少一个字段，也不要在页面上渲染一个坏掉的格子。改完到前台看一眼。
        |
        | ⚠️ **采不到就留空，模板整行跳过，不要填「暂无」或估算值。**
        | 渲染成「—」看起来像数据坏了，而实际上是「这个城市拿不到权威来源」。
        |
        | ⚠️ 宿主已发布 config 时不要在那边覆盖整个 city_pages 段——
        | mergeConfigFrom 是浅合并（同 seo 段的提醒）。本段目前只有这一个键，
        | 所以整段覆盖暂时是安全的，但日后加键就会踩到。
        |
        */
        'profile_fields' => [],
    ],

    /*
    |--------------------------------------------------------------------------
    | 内容版本快照保留条数
    |--------------------------------------------------------------------------
    |
    | 每条记录保留最近 N 条快照，超出的由 ContentRevisionObserver 在写入后删掉。
    | 批次 1.5c 起覆盖全部 7 类内容，共用同一份配置。
    |
    | 不加上限，高频编辑的记录会把 site_revisions 撑爆——每条快照都存一份
    | 正文全文，一篇长内容几十 KB，几百次编辑就是几十 MB。
    | 设为 0 或负数表示不裁剪（自行承担表膨胀）。
    |
    */
    'revisions_keep' => env('SITE_REVISIONS_KEEP', 50),

    /*
    |--------------------------------------------------------------------------
    | 前台富文本过滤画像
    |--------------------------------------------------------------------------
    |
    | 留空则用包自带的白名单（Support\RichText::defaultProfile()），它与后台
    | RichEditor 的默认工具栏对齐，不依赖宿主的 config/purifier.php 内容。
    |
    | 想自己定过滤策略时，在 config/purifier.php 的 settings 下加一段画像，
    | 再把画像名填到这里，包内白名单即让位。注意画像是整体替换而非合并，
    | 填了之后标签能不能活下来完全由那一段决定。
    |
    */
    'purifier_profile' => env('SITE_PURIFIER_PROFILE'),
];
