<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 超级管理员角色
    |--------------------------------------------------------------------------
    |
    | 拥有此角色的管理员将绕过所有权限检查（通过 Gate::before 实现）。
    | 可通过 .env 中的 SUPER_ADMIN_ROLE 覆盖。
    |
    */
    'super_admin_role' => env('SUPER_ADMIN_ROLE', 'super_admin'),

    /*
    |--------------------------------------------------------------------------
    | 日志保留天数（占位，后续功能域使用）
    |--------------------------------------------------------------------------
    */
    'log_retention_days' => env('LOG_RETENTION_DAYS', 90),

    /*
    |--------------------------------------------------------------------------
    | Composer 可执行路径（WR-04）
    |--------------------------------------------------------------------------
    |
    | 指定 composer 可执行文件的完整路径。留空（null）时自动探测
    | （which composer → /usr/local/bin/composer → 'composer'）。
    | 通过 config() 读取确保 config:cache 后仍生效。
    |
    */
    'composer_path' => env('COMPOSER_PATH', null),
];
